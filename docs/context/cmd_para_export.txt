--------------------------
***COMANDOS VENV - RUNSERVER
--------------------------

cd desktop
cd proyecto_django
source /Users/felipedides/Desktop/proyecto_django/venv/bin/activate

python manage.py runserver

--------------------------
***GENERACION ZIP 
--------------------------
cooking-pot
pill

AI
(Sin USDA - img - test)
./scripts/export_for_chatgpt.sh ai

FULL
(Sin USDA - img)
./scripts/export_for_chatgpt.sh full

COMPLETO
(Sin img)
./scripts/export_for_chatgpt.sh usda


--------------------------
***AUX COMMITS
--------------------------

git add .
git commit -m "Integracion comparadores"
git push origin main


git add .
git commit -m "Correccion colores btns profile"
git push origin main

-------------

REVERSE PATCH
patch -p1 -R < myscoope_home_stat_actions_split.patch

-------------


git branch --show-current

git status
git checkout main
git push origin main


git ls-files | grep "notas/actions"


git log --oneline -n 5 #ver ultimos commit
git checkout -b feature/ux-improvements #crear nuevo branch

git push origin refactor-header-actions-2
git merge refactor-header-actions-2
git switch -c refactor-header-actions-2 (crea y cambia de branch)




#LOCAL
python manage.py export_foods --output foods_export.csv
#PROD
python manage.py export_foods --output /tmp/foods_export.csv

#LOCAL
python manage.py export_foods_full --output foods_full_export.csv
#PROD
python manage.py export_foods_full --output /tmp/foods_full_export.csv



dides dides@gmail.com True True

from django.contrib.auth import get_user_model
User = get_user_model()

user = User.objects.get(email="dides@gmail.com")
user.set_password("CabrerA1988")
user.is_staff = True
user.is_superuser = True
user.save()



LIMPIEZA DE BRANCHS

git branch -D codex-experiments
git branch -D feature/improve-create-meal-from-dailyplan
git branch -D feature/mejora-interfaz
git branch -D feature/mejora-navegacion
git branch -D feature/mejora-viewmodel
git branch -D feature/sidebar-layout
git branch -D feature/ux-improvements
git branch -D refactor-header-actions-2
git branch -D rescue-72f8e48

git branch -r

#MALO
git push origin --delete origin/codex-experiments

CORRECTO!
git push origin --delete codex-experiments
git push origin --delete feature/sidebar-layout
git push origin --delete feature/ux-improvements
git push origin --delete refactor-header-actions-2
git push origin --delete rescue-72f8e48

LIMPIAR REFERENCIAS REMOTAS
git fetch --prune

Y VERIFICAR
git branch -r



git add docs/architecture/internal_ai_tools_layer.md
git commit -m "Todo ok previo a MCP server MVP"

Exponer read tools por protocolo MCP,
Exponer validation tool por protocolo MCP,
Exponer proposal tool por protocolo MCP


PYTHONPATH=mcp_server python -m unittest discover -s mcp_server/tests



## Variables de MCP inspector
Transport Type: STDIO
Command: /Users/felipedides/Desktop/proyecto_django/venv/bin/python
Arguments: -m myscoope_mcp.run_protocol_server

PYTHONPATH=mcp_server
MYSCOOPE_API_BASE_URL=http://127.0.0.1:8000/app







## Probar entorno local para MCP

cd desktop
cd proyecto_django
source /Users/felipedides/Desktop/proyecto_django/venv/bin/activate


export MYSCOOPE_INTERNAL_API_TOKEN="dev-mcp-token"
export MYSCOOPE_INTERNAL_API_USERNAME="felipe"
python manage.py runserver

PYTHONPATH=mcp_server python -m myscoope_mcp.run_protocol_server --check

MCP inspector

npx @modelcontextprotocol/inspector

PYTHONPATH=mcp_server python -m myscoope_mcp.run_protocol_server

Transport Type: STDIO
Command: /Users/felipedides/Desktop/proyecto_django/venv/bin/python
Arguments: -m myscoope_mcp.run_protocol_server

PYTHONPATH=mcp_server
MYSCOOPE_API_BASE_URL=http://127.0.0.1:8000/app
MYSCOOPE_API_AUTH_TOKEN=dev-mcp-token





git add .
git commit -m "Add proposal payload validators"





PYTHONPATH
mcp_server

MYSCOOPE_API_BASE_URL
http://127.0.0.1:8000/app

MYSCOOPE_API_AUTH_TOKEN
dev-mcp-token



MYSCOOPE_MCP_TRANSPORT=streamable-http
MYSCOOPE_MCP_HOST=0.0.0.0
MYSCOOPE_API_BASE_URL=https://www.myscoope.com/app
MYSCOOPE_API_AUTH_TOKEN=<token-interno-mcp-a-django>




MYSCOOPE_MCP_TRANSPORT
streamable-http

MYSCOOPE_MCP_HOST
0.0.0.0

MYSCOOPE_MCP_PUBLIC_URL
https://<url-del-servicio-mcp>.onrender.com

MYSCOOPE_MCP_EXTERNAL_AUTH_TOKEN
<token-externo-largo>

MYSCOOPE_API_BASE_URL
https://www.myscoope.com/app

MYSCOOPE_API_AUTH_TOKEN
<token-interno-mcp-a-django>

PYTHON_VERSION
3.13.5

---


python - <<'PY'
import secrets

print(secrets.token_urlsafe(48))
PY

git add .
git commit -m "Document remote MCP deployment and safety checklist"
git push origin main


python scripts/openai_remote_mcp_smoke.py





MYSCOOPE_MCP_RESOURCE_URL
https://myscoope-mcp.onrender.com

MYSCOOPE_OAUTH_ISSUER_URL
https://www.myscoope.com

MYSCOOPE_MCP_AUTH_ISSUER_URL
https://www.myscoope.com






This usually means your organization verification status hasn’t propagated to the API/app submission flow yet or you’re submitting under a different org/key.

Please try:

Wait up to 30 minutes for the status to update

Generate a new API key (old keys can keep the “not verified” state)

Refresh, log out/in, and confirm you’re on the correct organization in Platform settings

If it still shows “not verified,” tell me: are you submitting via platform.openai.com (API) or publishing a GPT in ChatGPT, and what exact error text do you see?



-----
ZIP antiguo

find . -type d -name "__pycache__" -exec rm -rf {} +
find . -type f -name "*.pyc" -delete
find . -name ".DS_Store" -delete
find . -name "__MACOSX" -type d -exec rm -rf {} +
rsync -av \
  --exclude '.git' \
  --exclude '__pycache__' \
  --exclude '*.pyc' \
  --exclude '.DS_Store' \
  --exclude '__MACOSX' \
  --exclude 'db.sqlite3' \
  --exclude 'venv' \
  --exclude '.venv' \
  --exclude 'staticfiles' \
  --exclude '*.zip' \
  ./ ../proyecto_django_export/
cd ..
zip -r proyecto_django_export.zip proyecto_django_export -x "*.DS_Store" "__MACOSX/*"
cd proyecto_django

--- 
