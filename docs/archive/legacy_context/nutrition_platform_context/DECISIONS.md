# Architecture Decisions

Accepted:
- Calories derived, not stored
- Allocation computed after aggregation
- Fork preserves lineage
- Copy creates independent entity
- Permissions enforced via helpers

Rejected:
- Storing calories in DB
- Averaging allocation
- Logic in templates
