# MCP Local Runtime Check

## Purpose

This document clarifies how to test the local MCP runtime safely.

The My Scoope MCP server currently supports two execution modes:

```bash
PYTHONPATH=mcp_server python -m myscoope_mcp.run_protocol_server --check