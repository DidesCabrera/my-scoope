diff --git a/notas/application/queries/proposal_queries.py b/notas/application/queries/proposal_queries.py
index 9d372bf..4501c6e 100644
--- a/notas/application/queries/proposal_queries.py
+++ b/notas/application/queries/proposal_queries.py
@@ -44,7 +44,7 @@ def get_available_proposal_queryset(user):
             | Q(dailyplan__created_by=user)
         )
         .distinct()
-        .order_by("-created_at", "-id")
+        .order_by("list_order", "-created_at", "-id")
     )
 
 
@@ -99,6 +99,7 @@ def _get_proposal_attachment(proposal: NutritionProposal) -> dict[str, str]:
 
 def _build_proposal_list_actions(proposal: NutritionProposal) -> list[dict]:
     detail_url = reverse("proposal_detail", args=[proposal.id])
+    delete_url = reverse("proposal_delete", args=[proposal.id])
 
     return [
         {
@@ -110,6 +111,15 @@ def _build_proposal_list_actions(proposal: NutritionProposal) -> list[dict]:
             "desktop_position": "inline",
             "mobile_position": "inline",
         },
+        {
+            "key": "delete",
+            "label": "Eliminar propuesta",
+            "icon": "trash-2",
+            "url": delete_url,
+            "method": "post",
+            "desktop_position": "menu",
+            "mobile_position": "menu",
+        },
     ]
 
 
diff --git a/notas/application/services/commands/proposal_commands.py b/notas/application/services/commands/proposal_commands.py
index 034e3e3..111b98c 100644
--- a/notas/application/services/commands/proposal_commands.py
+++ b/notas/application/services/commands/proposal_commands.py
@@ -144,6 +144,18 @@ def _ensure_can_cancel_proposal(
         raise ValueError("proposal_cancel_not_allowed")
 
 
+def _ensure_can_delete_proposal(
+    *,
+    user,
+    proposal: NutritionProposal,
+) -> None:
+    is_creator = proposal.created_by_id == user.id
+    is_dailyplan_owner = proposal.dailyplan.created_by_id == user.id
+
+    if not is_creator and not is_dailyplan_owner:
+        raise ValueError("proposal_delete_not_allowed")
+
+
 def _ensure_pending_review(
     proposal: NutritionProposal,
 ) -> None:
@@ -999,4 +1011,17 @@ def apply_approved_create_dailyplan_proposal(
     return NutritionProposalApplyCreateDailyPlanResult(
         proposal=proposal,
         dailyplan=dailyplan,
-    )
\ No newline at end of file
+    )
+
+@transaction.atomic
+def delete_proposal(
+    *,
+    user,
+    proposal: NutritionProposal,
+) -> None:
+    _ensure_can_delete_proposal(
+        user=user,
+        proposal=proposal,
+    )
+
+    proposal.delete()
diff --git a/notas/domain/models.py b/notas/domain/models.py
index 2198b92..25f61b1 100644
--- a/notas/domain/models.py
+++ b/notas/domain/models.py
@@ -1239,6 +1239,7 @@ class NutritionProposal(models.Model):
 
     title = models.CharField(max_length=160)
     summary = models.TextField(blank=True)
+    list_order = models.PositiveIntegerField(default=0)
 
     targets = models.JSONField(default=dict, blank=True)
     current_snapshot = models.JSONField(default=dict, blank=True)
diff --git a/notas/interface/views/proposals.py b/notas/interface/views/proposals.py
index 863895e..f93be05 100644
--- a/notas/interface/views/proposals.py
+++ b/notas/interface/views/proposals.py
@@ -2,6 +2,7 @@ from dataclasses import dataclass
 
 from django.contrib import messages
 from django.contrib.auth.decorators import login_required
+from django.http import HttpResponse, HttpResponseBadRequest
 from django.shortcuts import get_object_or_404, redirect, render
 from django.urls import reverse
 from django.views.decorators.http import require_POST
@@ -14,6 +15,7 @@ from notas.application.queries.proposal_queries import (
 from notas.application.services.commands.proposal_commands import (
     approve_proposal,
     cancel_proposal,
+    delete_proposal,
     reject_proposal,
     apply_approved_create_dailyplan_proposal,
     apply_approved_create_meal_proposal,
@@ -40,6 +42,7 @@ from notas.presentation.proposals.proposal_review_viewmodels import (
 class ProposalListContentVM:
     header: object
     proposals: list[dict]
+    list_mode: str = "list"
 
 
 @dataclass
@@ -76,7 +79,11 @@ class BreadcrumbParent:
 
 
 def _get_proposal_list_status_filter(request):
-    status = (request.GET.get("status") or "all").strip()
+    status = (
+        request.GET.get("status")
+        or request.POST.get("status")
+        or "all"
+    ).strip()
 
     if status in {"pending_review", "approved"}:
         return status
@@ -84,6 +91,104 @@ def _get_proposal_list_status_filter(request):
     return None
 
 
+def _normalize_proposal_list_mode(request):
+    mode = (request.GET.get("mode") or "list").strip()
+    return mode if mode in {"list", "reorder", "delete"} else "list"
+
+
+def _proposal_list_url(*, mode: str | None = None, status_filter: str | None = None):
+    base_url = reverse("proposal_list")
+    params = []
+
+    if mode and mode != "list":
+        params.append(f"mode={mode}")
+
+    if status_filter:
+        params.append(f"status={status_filter}")
+
+    if not params:
+        return base_url
+
+    return f"{base_url}?{'&'.join(params)}"
+
+
+def _proposal_safe_return_to(request, *, mode: str | None = None):
+    status_filter = _get_proposal_list_status_filter(request)
+    return _proposal_list_url(mode=mode, status_filter=status_filter)
+
+
+def _build_proposal_list_actions(active_filter: str | None, list_mode: str):
+    if list_mode == "reorder":
+        return [
+            {
+                "key": "save_list_order",
+                "label": "Guardar Orden",
+                "url": reverse("proposal_list_reorder"),
+                "method": "button",
+                "icon": "check",
+                "order": 10,
+                "desktop_position": "inline",
+                "mobile_position": "inline",
+                "extra_class": "js-list-reorder-save",
+            },
+        ]
+
+    if list_mode == "delete":
+        return [
+            {
+                "key": "exit_delete_mode",
+                "label": "Cerrar",
+                "url": _proposal_list_url(status_filter=active_filter),
+                "method": "get",
+                "icon": "check",
+                "order": 10,
+                "desktop_position": "inline",
+                "mobile_position": "inline",
+            },
+            {
+                "key": "bulk_delete",
+                "label": "Eliminar seleccionadas",
+                "url": (
+                    reverse("proposal_list_bulk_delete")
+                    + (f"?status={active_filter}" if active_filter else "")
+                ),
+                "method": "post",
+                "icon": "trash-2",
+                "order": 20,
+                "desktop_position": "inline",
+                "mobile_position": "inline",
+                "disabled": True,
+                "extra_class": "js-list-bulk-delete-submit",
+            },
+        ]
+
+    actions = [
+        {
+            "key": "enter_reorder_mode",
+            "label": "Reordenar Propuestas",
+            "url": _proposal_list_url(mode="reorder", status_filter=active_filter),
+            "method": "get",
+            "icon": "list-ordered",
+            "order": 10,
+            "desktop_position": "menu",
+            "mobile_position": "menu",
+        },
+        {
+            "key": "enter_delete_mode",
+            "label": "Eliminar Propuestas",
+            "url": _proposal_list_url(mode="delete", status_filter=active_filter),
+            "method": "get",
+            "icon": "trash-2",
+            "order": 20,
+            "desktop_position": "menu",
+            "mobile_position": "menu",
+        },
+    ]
+
+    actions.extend(_build_proposal_list_filter_actions(active_filter))
+    return actions
+
+
 def _build_proposal_list_filter_actions(active_filter: str | None):
     base_url = reverse("proposal_list")
 
@@ -91,18 +196,18 @@ def _build_proposal_list_filter_actions(active_filter: str | None):
         {
             "key": "filter_pending_review",
             "label": "Ver solo pendientes de revisión",
-            "url": f"{base_url}?status=pending_review",
+            "url": _proposal_list_url(status_filter="pending_review"),
             "icon": "clock",
             "status_filter": "pending_review",
-            "order": 10,
+            "order": 100,
         },
         {
             "key": "filter_approved",
             "label": "Ver solo aprobadas",
-            "url": f"{base_url}?status=approved",
+            "url": _proposal_list_url(status_filter="approved"),
             "icon": "check",
             "status_filter": "approved",
-            "order": 20,
+            "order": 110,
         },
         {
             "key": "filter_all",
@@ -110,7 +215,7 @@ def _build_proposal_list_filter_actions(active_filter: str | None):
             "url": base_url,
             "icon": "list",
             "status_filter": None,
-            "order": 30,
+            "order": 120,
         },
     ]
 
@@ -322,6 +427,7 @@ def _get_proposal_intent(proposal):
 @login_required
 def proposal_list(request):
     status_filter = _get_proposal_list_status_filter(request)
+    list_mode = _normalize_proposal_list_mode(request)
     proposals = [
         proposal.as_dict()
         for proposal in list_user_proposals(
@@ -333,9 +439,10 @@ def proposal_list(request):
     content_vm = ProposalListContentVM(
         header=build_page_header(
             title="Propuestas",
-            actions=_build_proposal_list_filter_actions(status_filter),
+            actions=_build_proposal_list_actions(status_filter, list_mode),
         ),
         proposals=proposals,
+        list_mode=list_mode,
     )
 
     ui_vm = build_ui_vm(PROPOSAL_VIEWMODE_LIST)
@@ -352,6 +459,89 @@ def proposal_list(request):
     )
 
 
+@login_required
+@require_POST
+def proposal_list_reorder(request):
+    ordered_ids = request.POST.getlist("order[]")
+
+    if not ordered_ids:
+        return HttpResponseBadRequest("No order received.")
+
+    proposals = {
+        proposal.id: proposal
+        for proposal in get_available_proposal_queryset(request.user).filter(
+            id__in=ordered_ids,
+        )
+    }
+
+    for index, raw_id in enumerate(ordered_ids):
+        try:
+            proposal_id = int(raw_id)
+        except (TypeError, ValueError):
+            continue
+
+        proposal = proposals.get(proposal_id)
+        if not proposal:
+            continue
+
+        if proposal.list_order != index:
+            proposal.list_order = index
+            proposal.save(update_fields=["list_order"])
+
+    return HttpResponse(status=204)
+
+
+@login_required
+@require_POST
+def proposal_list_bulk_delete(request):
+    selected_ids = request.POST.getlist("selected_ids[]")
+
+    if not selected_ids:
+        messages.info(request, "No seleccionaste propuestas para eliminar.")
+        return redirect(_proposal_safe_return_to(request, mode="delete"))
+
+    proposals = get_available_proposal_queryset(request.user).filter(
+        id__in=selected_ids,
+    )
+
+    deleted_count = 0
+
+    for proposal in proposals:
+        try:
+            delete_proposal(
+                user=request.user,
+                proposal=proposal,
+            )
+            deleted_count += 1
+        except ValueError:
+            continue
+
+    if deleted_count:
+        messages.success(request, f"{deleted_count} propuesta(s) eliminada(s).")
+    else:
+        messages.info(request, "No se eliminaron propuestas.")
+
+    return redirect(_proposal_safe_return_to(request, mode="delete"))
+
+
+@login_required
+@require_POST
+def proposal_delete(request, proposal_id):
+    proposal = _get_proposal_model_for_action(
+        request.user,
+        proposal_id,
+    )
+
+    delete_proposal(
+        user=request.user,
+        proposal=proposal,
+    )
+
+    messages.success(request, "Propuesta eliminada.")
+
+    return redirect(_proposal_safe_return_to(request))
+
+
 @login_required
 def proposal_detail(request, proposal_id):
     proposal = get_proposal_detail(
diff --git a/notas/migrations/0026_nutritionproposal_list_order.py b/notas/migrations/0026_nutritionproposal_list_order.py
new file mode 100644
index 0000000..e9c72b7
--- /dev/null
+++ b/notas/migrations/0026_nutritionproposal_list_order.py
@@ -0,0 +1,18 @@
+# Generated by ChatGPT patch for proposal list ordering.
+
+from django.db import migrations, models
+
+
+class Migration(migrations.Migration):
+
+    dependencies = [
+        ("notas", "0025_dailyplan_meal_list_order"),
+    ]
+
+    operations = [
+        migrations.AddField(
+            model_name="nutritionproposal",
+            name="list_order",
+            field=models.PositiveIntegerField(default=0),
+        ),
+    ]
diff --git a/notas/static/notas/css/components/proposals.css b/notas/static/notas/css/components/proposals.css
index bc69bc5..84da172 100644
--- a/notas/static/notas/css/components/proposals.css
+++ b/notas/static/notas/css/components/proposals.css
@@ -234,6 +234,11 @@
   stroke: currentColor;
 }
 
+
+.picker-summary-card__icon.proposal {
+  background-color: var(--entity-proposal, var(--entity-home));
+}
+
 /* =========================================================
    Shared badges
    ========================================================= */
diff --git a/notas/templates/components/list_panel_proposals_delete.html b/notas/templates/components/list_panel_proposals_delete.html
new file mode 100644
index 0000000..1191b8f
--- /dev/null
+++ b/notas/templates/components/list_panel_proposals_delete.html
@@ -0,0 +1,42 @@
+<div class="data-grid data-grid--list-panel data-grid--list-panel-delete js-list-delete-panel">
+    <h3 class="data-grid-list-panel-title">Eliminar Propuestas</h3>
+
+    {% for proposal in proposals %}
+        <div
+            class="data-grid-row data-grid-row--list-panel-delete js-list-delete-row"
+            data-item-id="{{ proposal.id }}"
+        >
+            <div class="data-grid-cell data-grid-cell--select">
+                <label class="list-panel-select" aria-label="Seleccionar propuesta">
+                    <input
+                        type="checkbox"
+                        class="js-list-delete-checkbox"
+                        value="{{ proposal.id }}"
+                    >
+                    <span class="list-panel-select__control"></span>
+                </label>
+            </div>
+
+            <div class="data-grid-cell data-grid-cell--name">
+                <div class="list-panel-title-row">
+                    <i data-lucide="sparkles" class="picker-summary-card__icon proposal list-panel-title-icon"></i>
+                    <div class="truncate">{{ proposal.title }}</div>
+                </div>
+            </div>
+
+            <div class="data-grid-cell data-grid-cell--action">
+                <form method="post" action="{% url 'proposal_delete' proposal.id %}">
+                    {% csrf_token %}
+                    <input type="hidden" name="status" value="{{ request.GET.status }}">
+                    <button type="submit" aria-label="Eliminar propuesta">
+                        <i data-lucide="trash" class="table-icon"></i>
+                    </button>
+                </form>
+            </div>
+        </div>
+    {% empty %}
+        <div class="data-grid-empty">
+            No tienes propuestas para eliminar.
+        </div>
+    {% endfor %}
+</div>
diff --git a/notas/templates/components/list_panel_proposals_reorder.html b/notas/templates/components/list_panel_proposals_reorder.html
new file mode 100644
index 0000000..47b42f4
--- /dev/null
+++ b/notas/templates/components/list_panel_proposals_reorder.html
@@ -0,0 +1,35 @@
+<div
+    class="data-grid data-grid--list-panel data-grid--list-panel-reorder js-list-reorder-panel"
+    data-reorder-url="{% url 'proposal_list_reorder' %}"
+    data-return-url="{% url 'proposal_list' %}{% if request.GET.status %}?status={{ request.GET.status }}{% endif %}"
+>
+    <h3 class="data-grid-list-panel-title">Reordenar Propuestas</h3>
+
+    {% for proposal in proposals %}
+        <div
+            class="data-grid-row data-grid-row--list-panel-reorder js-list-reorder-row"
+            data-item-id="{{ proposal.id }}"
+        >
+            <div class="data-grid-cell data-grid-cell--drag">
+                <button
+                    type="button"
+                    class="list-panel-drag-handle"
+                    aria-label="Reordenar propuesta"
+                >
+                    <i data-lucide="grip-vertical" class="table-icon"></i>
+                </button>
+            </div>
+
+            <div class="data-grid-cell data-grid-cell--name">
+                <div class="list-panel-title-row">
+                    <i data-lucide="sparkles" class="picker-summary-card__icon proposal list-panel-title-icon"></i>
+                    <div class="truncate">{{ proposal.title }}</div>
+                </div>
+            </div>
+        </div>
+    {% empty %}
+        <div class="data-grid-empty">
+            No tienes propuestas para reordenar.
+        </div>
+    {% endfor %}
+</div>
diff --git a/notas/templates/notas/proposals/list.html b/notas/templates/notas/proposals/list.html
index fa32c7b..31af572 100644
--- a/notas/templates/notas/proposals/list.html
+++ b/notas/templates/notas/proposals/list.html
@@ -9,58 +9,67 @@
     <h2>{{ vm.ui.title }}</h2>
   </div>
 
-  {% for proposal in vm.content.proposals %}
-    <article class="card proposal-list-card proposal-list-card--{{ proposal.status }}">
-      <div class="proposal-list-card__main">
-        <div class="proposal-list-card__title-block">
-          <div class="proposal-list-card__copy">
-            <h3 class="proposal-list-card__title">{{ proposal.title }}</h3>
+  {% if vm.content.list_mode == "reorder" %}
+    {% include "components/list_panel_proposals_reorder.html" with proposals=vm.content.proposals %}
+  {% elif vm.content.list_mode == "delete" %}
+    {% include "components/list_panel_proposals_delete.html" with proposals=vm.content.proposals %}
+  {% else %}
+    {% for proposal in vm.content.proposals %}
+      <article class="card proposal-list-card proposal-list-card--{{ proposal.status }}">
+        <div class="proposal-list-card__main">
+          <div class="proposal-list-card__title-block">
+            <div class="proposal-list-card__copy">
+              <h3 class="proposal-list-card__title">{{ proposal.title }}</h3>
+            </div>
           </div>
-        </div>
 
-        <div class="proposal-list-card__icons-title" aria-hidden="true">
-          <i data-lucide="sparkles" class="home-section-header-icon proposal"></i>
+          <div class="proposal-list-card__icons-title" aria-hidden="true">
+            <i data-lucide="sparkles" class="home-section-header-icon proposal"></i>
+          </div>
         </div>
-      </div>
 
-      <span class="proposal-status-badge proposal-status-badge--{{ proposal.status }}">
-        {{ proposal.status }}
-      </span>
+        <span class="proposal-status-badge proposal-status-badge--{{ proposal.status }}">
+          {{ proposal.status }}
+        </span>
 
 
-      {% if proposal.summary %}
-        <div class="proposal-list-card__summary-block">
-          <span class="proposal-list-card__summary-label">Requerimiento</span>
-          <p class="proposal-list-card__summary">{{ proposal.summary }}</p>
-        </div>
-      {% endif %}
+        {% if proposal.summary %}
+          <div class="proposal-list-card__summary-block">
+            <span class="proposal-list-card__summary-label">Requerimiento</span>
+            <p class="proposal-list-card__summary">{{ proposal.summary }}</p>
+          </div>
+        {% endif %}
 
-      <div class="card-bottom proposal-list-card__bottom">
-        <div class="proposal-attachment proposal-attachment--{{ proposal.attachment_kind }}">
-          <span class="proposal-attachment__icon" aria-hidden="true">
-            <i data-lucide="{{ proposal.attachment_icon }}" class="proposal-attachment__svg"></i>
-          </span>
-          <span class="proposal-attachment__copy">
-            <strong class="proposal-attachment__name">{{ proposal.attachment_name }}</strong>
-          </span>
-        </div>
+        <div class="card-bottom proposal-list-card__bottom">
+          <div class="proposal-attachment proposal-attachment--{{ proposal.attachment_kind }}">
+            <span class="proposal-attachment__icon" aria-hidden="true">
+              <i data-lucide="{{ proposal.attachment_icon }}" class="proposal-attachment__svg"></i>
+            </span>
+            <span class="proposal-attachment__copy">
+              <strong class="proposal-attachment__name">{{ proposal.attachment_name }}</strong>
+            </span>
+          </div>
 
-        <div class="card-actions proposal-list-card__actions">
-          {% include "components/card_actions.html" with actions=proposal.actions %}
+          <div class="card-actions proposal-list-card__actions">
+            {% include "components/card_actions.html" with actions=proposal.actions %}
+          </div>
         </div>
-      </div>
-    </article>
-  {% empty %}
-    <section class="card proposal-empty-card">
-      <div>
-        <h3>No tienes propuestas todavía.</h3>
-        <p>
-          Las propuestas creadas por IA/MCP aparecerán aquí para que puedas revisarlas antes de aprobarlas o aplicarlas.
-        </p>
-      </div>
-    </section>
-  {% endfor %}
+      </article>
+    {% empty %}
+      <section class="card proposal-empty-card">
+        <div>
+          <h3>No tienes propuestas todavía.</h3>
+          <p>
+            Las propuestas creadas por IA/MCP aparecerán aquí para que puedas revisarlas antes de aprobarlas o aplicarlas.
+          </p>
+        </div>
+      </section>
+    {% endfor %}
+  {% endif %}
 
 </div>
 
+{% load static %}
+<script src="{% static 'notas/js/list_panel_modes.js' %}"></script>
+
 {% endblock %}
diff --git a/notas/urls.py b/notas/urls.py
index 1bd9f9d..c235817 100644
--- a/notas/urls.py
+++ b/notas/urls.py
@@ -117,9 +117,12 @@ from notas.interface.views.admin_tools import (
 from notas.interface.views.proposals import (
     proposal_approve,
     proposal_cancel,
+    proposal_delete,
     proposal_detail,
     proposal_entity_detail,
     proposal_list,
+    proposal_list_bulk_delete,
+    proposal_list_reorder,
     proposal_reject,
     proposal_apply,
 )
@@ -325,11 +328,14 @@ urlpatterns = [
 
     # PROPOSALS
     path("proposals/", proposal_list, name="proposal_list"),
+    path("proposals/reorder/", proposal_list_reorder, name="proposal_list_reorder"),
+    path("proposals/bulk-delete/", proposal_list_bulk_delete, name="proposal_list_bulk_delete"),
     path("proposals/<int:proposal_id>/", proposal_detail, name="proposal_detail"),
     path("proposals/<int:proposal_id>/entity/", proposal_entity_detail, name="proposal_entity_detail"),
     path("proposals/<int:proposal_id>/approve/", proposal_approve, name="proposal_approve"),
     path("proposals/<int:proposal_id>/reject/", proposal_reject, name="proposal_reject"),
     path("proposals/<int:proposal_id>/cancel/", proposal_cancel, name="proposal_cancel"),
+    path("proposals/<int:proposal_id>/delete/", proposal_delete, name="proposal_delete"),
 
     # AI TOOLS API ADAPTER
     path("ai-tools/health/", ai_tools_health, name="ai_tools_health"),
