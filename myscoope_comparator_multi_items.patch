--- a/notas/interface/views/comparators.py
+++ b/notas/interface/views/comparators.py
@@ -16,6 +16,9 @@
 from notas.presentation.viewmodels.base_vm import BaseVM
 
 
+MIN_COMPARATOR_SLOTS = 2
+
+
 @dataclass
 class ComparatorChoice:
     id: int
@@ -27,6 +30,11 @@
     id: int | None = None
     name: str = ""
     quantity: float | None = None
+    position: int = 1
+
+    @property
+    def label(self) -> str:
+        return f"Selector {self.position}"
 
 
 @dataclass
@@ -42,8 +50,7 @@
     key: str
     label: str
     unit: str
-    a: ComparatorMetricBar
-    b: ComparatorMetricBar
+    bars: list[ComparatorMetricBar] = field(default_factory=list)
 
 
 @dataclass
@@ -59,19 +66,22 @@
     entity_label: str
     entity_plural_label: str
     entity_icon: str
+    entity_scope: str
     selector_label: str
-    selector_a_label: str
-    selector_b_label: str
+    add_action_label: str
     show_quantity_inputs: bool = False
     quantity_unit: str = "g"
     choices: list[ComparatorChoice] = field(default_factory=list)
-    selection_a: ComparatorSelection = field(default_factory=ComparatorSelection)
-    selection_b: ComparatorSelection = field(default_factory=ComparatorSelection)
+    selections: list[ComparatorSelection] = field(default_factory=list)
     metrics: list[ComparatorMetric] = field(default_factory=list)
     tabs: list[ComparatorTab] = field(default_factory=list)
     item_count: int = 0
     is_ready: bool = False
-    empty_message: str = "Selecciona dos elementos para compararlos."
+    empty_message: str = "Selecciona al menos dos elementos para compararlos."
+
+    @property
+    def can_remove_selection(self) -> bool:
+        return len(self.selections) > MIN_COMPARATOR_SLOTS
 
 
 def _parse_int(value: Any) -> int | None:
@@ -81,6 +91,15 @@
         return None
 
     return parsed if parsed > 0 else None
+
+
+def _parse_zero_based_index(value: Any) -> int | None:
+    try:
+        parsed = int(value)
+    except (TypeError, ValueError):
+        return None
+
+    return parsed if parsed >= 0 else None
 
 
 def _parse_quantity(value: Any, fallback: float = 100.0) -> float:
@@ -129,15 +148,22 @@
     )
 
 
-def _build_metric(key: str, label: str, unit: str, value_a: float, value_b: float) -> ComparatorMetric:
-    max_value = max(float(value_a or 0), float(value_b or 0), 0)
+def _build_metric(
+    key: str,
+    label: str,
+    unit: str,
+    comparable_rows: list[tuple[ComparatorSelection, dict[str, float]]],
+) -> ComparatorMetric:
+    max_value = max((float(values.get(key, 0) or 0) for _, values in comparable_rows), default=0)
 
     return ComparatorMetric(
         key=key,
         label=label,
         unit=unit,
-        a=_metric_bar("Selector 1", value_a, unit, max_value),
-        b=_metric_bar("Selector 2", value_b, unit, max_value),
+        bars=[
+            _metric_bar(selection.name, values.get(key, 0), unit, max_value)
+            for selection, values in comparable_rows
+        ],
     )
 
 
@@ -191,8 +217,7 @@
 
 
 def _build_metrics(
-    values_a: dict[str, float],
-    values_b: dict[str, float],
+    comparable_rows: list[tuple[ComparatorSelection, dict[str, float]]],
     *,
     include_ppk: bool = False,
 ) -> list[ComparatorMetric]:
@@ -217,8 +242,7 @@
             key=key,
             label=label,
             unit=unit,
-            value_a=values_a.get(key, 0),
-            value_b=values_b.get(key, 0),
+            comparable_rows=comparable_rows,
         )
         for key, label, unit in metric_specs
     ]
@@ -246,15 +270,105 @@
     return [ComparatorChoice(id=item.id, name=item.name) for item in queryset]
 
 
-def _find_selected(queryset, selected_id: int | None):
-    if not selected_id:
-        return None
-
-    for item in queryset:
-        if item.id == selected_id:
-            return item
-
-    return None
+def _items_by_id(queryset) -> dict[int, Any]:
+    return {item.id: item for item in queryset}
+
+
+def _selection_rows_from_request(request, *, include_quantities: bool = False) -> list[dict[str, Any]]:
+    indexed_positions: list[int] = []
+
+    for key in request.GET.keys():
+        if key.startswith("item_"):
+            parsed = _parse_int(key.removeprefix("item_"))
+            if parsed:
+                indexed_positions.append(parsed)
+
+    if indexed_positions:
+        max_position = max(max(indexed_positions), MIN_COMPARATOR_SLOTS)
+        return [
+            {
+                "id": _parse_int(request.GET.get(f"item_{position}")),
+                "quantity": _quantity_from_request(request, position) if include_quantities else None,
+            }
+            for position in range(1, max_position + 1)
+        ]
+
+    return [
+        {
+            "id": _parse_int(request.GET.get("a")),
+            "quantity": _quantity_from_request(request, 1) if include_quantities else None,
+        },
+        {
+            "id": _parse_int(request.GET.get("b")),
+            "quantity": _quantity_from_request(request, 2) if include_quantities else None,
+        },
+    ]
+
+
+def _quantity_from_request(request, position: int, fallback: float = 100.0) -> float:
+    value = request.GET.get(f"qty_{position}")
+
+    if value is None and position == 1:
+        value = request.GET.get("qty_a")
+    elif value is None and position == 2:
+        value = request.GET.get("qty_b")
+
+    return _parse_quantity(value, fallback=fallback)
+
+
+def _build_selections_from_request(
+    request,
+    *,
+    items_by_id: dict[int, Any],
+    include_quantities: bool = False,
+) -> list[ComparatorSelection]:
+    rows = _selection_rows_from_request(request, include_quantities=include_quantities)
+    remove_index = _parse_zero_based_index(request.GET.get("remove_index"))
+
+    if remove_index is not None and len(rows) > MIN_COMPARATOR_SLOTS:
+        if remove_index < len(rows):
+            rows.pop(remove_index)
+
+    if request.GET.get("comparator_action") == "add":
+        rows.append({"id": None, "quantity": 100.0 if include_quantities else None})
+
+    while len(rows) < MIN_COMPARATOR_SLOTS:
+        rows.append({"id": None, "quantity": 100.0 if include_quantities else None})
+
+    selections: list[ComparatorSelection] = []
+    for index, row in enumerate(rows, start=1):
+        selected_id = row.get("id")
+        selected_item = items_by_id.get(selected_id) if selected_id else None
+        selections.append(
+            ComparatorSelection(
+                id=selected_item.id if selected_item else selected_id,
+                name=selected_item.name if selected_item else "",
+                quantity=row.get("quantity") if include_quantities else None,
+                position=index,
+            )
+        )
+
+    return selections
+
+
+def _comparable_rows(
+    selections: list[ComparatorSelection],
+    items_by_id: dict[int, Any],
+    value_builder,
+) -> list[tuple[ComparatorSelection, dict[str, float]]]:
+    rows: list[tuple[ComparatorSelection, dict[str, float]]] = []
+
+    for selection in selections:
+        if not selection.id:
+            continue
+
+        item = items_by_id.get(selection.id)
+        if not item:
+            continue
+
+        rows.append((selection, value_builder(item, selection)))
+
+    return rows
 
 
 @login_required
@@ -272,46 +386,34 @@
         )
         .order_by("list_order", "name", "id")
     )
-
-    selected_a_id = _parse_int(request.GET.get("a"))
-    selected_b_id = _parse_int(request.GET.get("b"))
-    quantity_a = _parse_quantity(request.GET.get("qty_a"), fallback=100.0)
-    quantity_b = _parse_quantity(request.GET.get("qty_b"), fallback=100.0)
-
-    selected_a = _find_selected(foods, selected_a_id)
-    selected_b = _find_selected(foods, selected_b_id)
-
-    metrics = []
-    if selected_a and selected_b:
-        metrics = _build_metrics(
-            _food_values(selected_a, quantity_a),
-            _food_values(selected_b, quantity_b),
-        )
+    foods_by_id = _items_by_id(foods)
+    selections = _build_selections_from_request(
+        request,
+        items_by_id=foods_by_id,
+        include_quantities=True,
+    )
+    comparable_rows = _comparable_rows(
+        selections,
+        foods_by_id,
+        lambda food, selection: _food_values(food, selection.quantity or 100.0),
+    )
+    metrics = _build_metrics(comparable_rows) if len(comparable_rows) >= 2 else []
 
     content_vm = ComparatorContentVM(
         entity_label="alimento",
         entity_plural_label="alimentos",
         entity_icon="carrot",
+        entity_scope="food",
         selector_label="Alimento",
-        selector_a_label="Alimento 1",
-        selector_b_label="Alimento 2",
+        add_action_label="Agregar Alimento a la comparación",
         show_quantity_inputs=True,
         choices=_choices_from_queryset(foods),
-        selection_a=ComparatorSelection(
-            id=selected_a_id,
-            name=selected_a.name if selected_a else "",
-            quantity=quantity_a,
-        ),
-        selection_b=ComparatorSelection(
-            id=selected_b_id,
-            name=selected_b.name if selected_b else "",
-            quantity=quantity_b,
-        ),
+        selections=selections,
         metrics=metrics,
         tabs=_build_tabs("foods"),
         item_count=len(foods),
         is_ready=bool(metrics),
-        empty_message="Selecciona dos alimentos y define sus porciones para ver la comparación.",
+        empty_message="Selecciona al menos dos alimentos y define sus porciones para ver la comparación.",
     )
 
     return _render_comparator(
@@ -333,43 +435,30 @@
         .order_by("list_order", "name", "id")
         .distinct()
     )
-
-    selected_a_id = _parse_int(request.GET.get("a"))
-    selected_b_id = _parse_int(request.GET.get("b"))
-    selected_a = _find_selected(meals, selected_a_id)
-    selected_b = _find_selected(meals, selected_b_id)
-
-    metrics = []
+    meals_by_id = _items_by_id(meals)
+    selections = _build_selections_from_request(request, items_by_id=meals_by_id)
     current_weight = get_current_weight(request.user)
-
-    if selected_a and selected_b:
-        metrics = _build_metrics(
-            _entity_values(selected_a, current_weight=current_weight),
-            _entity_values(selected_b, current_weight=current_weight),
-            include_ppk=True,
-        )
+    comparable_rows = _comparable_rows(
+        selections,
+        meals_by_id,
+        lambda meal, selection: _entity_values(meal, current_weight=current_weight),
+    )
+    metrics = _build_metrics(comparable_rows, include_ppk=True) if len(comparable_rows) >= 2 else []
 
     content_vm = ComparatorContentVM(
         entity_label="comida",
         entity_plural_label="comidas",
         entity_icon="utensils",
+        entity_scope="meal",
         selector_label="Comida",
-        selector_a_label="Comida 1",
-        selector_b_label="Comida 2",
+        add_action_label="Agregar Comida a la comparación",
         choices=_choices_from_queryset(meals),
-        selection_a=ComparatorSelection(
-            id=selected_a_id,
-            name=selected_a.name if selected_a else "",
-        ),
-        selection_b=ComparatorSelection(
-            id=selected_b_id,
-            name=selected_b.name if selected_b else "",
-        ),
+        selections=selections,
         metrics=metrics,
         tabs=_build_tabs("meals"),
         item_count=len(meals),
         is_ready=bool(metrics),
-        empty_message="Selecciona dos comidas para ver la comparación.",
+        empty_message="Selecciona al menos dos comidas para ver la comparación.",
     )
 
     return _render_comparator(
@@ -390,43 +479,30 @@
         .exclude(source=DailyPlan.SOURCE_PROGRAM)
         .order_by("list_order", "name", "id")
     )
-
-    selected_a_id = _parse_int(request.GET.get("a"))
-    selected_b_id = _parse_int(request.GET.get("b"))
-    selected_a = _find_selected(dailyplans, selected_a_id)
-    selected_b = _find_selected(dailyplans, selected_b_id)
-
-    metrics = []
+    dailyplans_by_id = _items_by_id(dailyplans)
+    selections = _build_selections_from_request(request, items_by_id=dailyplans_by_id)
     current_weight = get_current_weight(request.user)
-
-    if selected_a and selected_b:
-        metrics = _build_metrics(
-            _entity_values(selected_a, current_weight=current_weight),
-            _entity_values(selected_b, current_weight=current_weight),
-            include_ppk=True,
-        )
+    comparable_rows = _comparable_rows(
+        selections,
+        dailyplans_by_id,
+        lambda dailyplan, selection: _entity_values(dailyplan, current_weight=current_weight),
+    )
+    metrics = _build_metrics(comparable_rows, include_ppk=True) if len(comparable_rows) >= 2 else []
 
     content_vm = ComparatorContentVM(
         entity_label="plan diario",
         entity_plural_label="planes diarios",
         entity_icon="clipboard-list",
+        entity_scope="dailyplan",
         selector_label="Plan",
-        selector_a_label="Plan 1",
-        selector_b_label="Plan 2",
+        add_action_label="Agregar Plan a la comparación",
         choices=_choices_from_queryset(dailyplans),
-        selection_a=ComparatorSelection(
-            id=selected_a_id,
-            name=selected_a.name if selected_a else "",
-        ),
-        selection_b=ComparatorSelection(
-            id=selected_b_id,
-            name=selected_b.name if selected_b else "",
-        ),
+        selections=selections,
         metrics=metrics,
         tabs=_build_tabs("dailyplans"),
         item_count=len(dailyplans),
         is_ready=bool(metrics),
-        empty_message="Selecciona dos planes diarios para ver la comparación.",
+        empty_message="Selecciona al menos dos planes diarios para ver la comparación.",
     )
 
     return _render_comparator(
--- a/notas/static/notas/css/components/comparators.css
+++ b/notas/static/notas/css/components/comparators.css
@@ -171,7 +171,9 @@
 
 .comparator-form__actions {
   display: flex;
-  justify-content: flex-end;
+  align-items: center;
+  justify-content: space-between;
+  gap: 10px;
   margin-top: 14px;
 }
 
@@ -183,10 +185,25 @@
   min-height: 36px;
 }
 
-.comparator-submit__icon {
+.comparator-submit__icon,
+.comparator-add__icon,
+.comparator-remove__icon {
   width: 15px;
   height: 15px;
   stroke-width: 2;
+}
+
+.comparator-add,
+.comparator-remove {
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+  gap: 7px;
+}
+
+.comparator-remove {
+  align-self: flex-start;
+  margin-left: 0;
 }
 
 .comparator-results {
@@ -250,10 +267,26 @@
 }
 
 .comparator-bar-row__entity-icon {
-  width: 16px;
-  height: 16px;
-  color: var(--text-muted);
-  stroke-width: 1.8;
+  width: 22px;
+  height: 22px;
+  box-sizing: border-box;
+  padding: 4px;
+  border-radius: 7px;
+  color: #ffffff;
+  background: var(--entity-comparator, var(--entity-home));
+  stroke-width: 1.9;
+}
+
+.comparator-bar-row__entity-icon--food {
+  background: var(--entity-food);
+}
+
+.comparator-bar-row__entity-icon--meal {
+  background: var(--entity-meal);
+}
+
+.comparator-bar-row__entity-icon--dailyplan {
+  background: var(--entity-dailyplan);
 }
 
 .comparator-bar-row__meta strong {
@@ -312,9 +345,6 @@
   background: var(--nutrition-fat);
 }
 
-.comparator-bar--b {
-  opacity: 0.55;
-}
 
 .comparator-empty {
   display: flex;
@@ -405,7 +435,13 @@
     font-size: 16px;
   }
 
-  .comparator-submit {
+  .comparator-form__actions {
+    flex-direction: column;
+    align-items: stretch;
+  }
+
+  .comparator-submit,
+  .comparator-add {
     width: 100%;
     min-height: 42px;
   }
--- a/notas/templates/notas/comparators/detail.html
+++ b/notas/templates/notas/comparators/detail.html
@@ -24,64 +24,50 @@
 
   <form method="get" class="comparator-form card-detail-block" data-comparator-form>
     <div class="comparator-selectors">
-      <section class="comparator-selector" data-comparator-selector>
-        <div class="comparator-selector__heading">
-          <span class="comparator-selector__badge">1</span>
-          <div>
-            <p>{{ vm.content.selector_a_label }}</p>
-            {% if vm.content.selection_a.name %}<strong>{{ vm.content.selection_a.name }}</strong>{% endif %}
+      {% for selection in vm.content.selections %}
+        <section class="comparator-selector" data-comparator-selector>
+          <div class="comparator-selector__heading">
+            <span class="comparator-selector__badge">{{ forloop.counter }}</span>
+            <div>
+              <p>{{ vm.content.selector_label }} {{ forloop.counter }}</p>
+              {% if selection.name %}<strong>{{ selection.name }}</strong>{% endif %}
+            </div>
           </div>
-        </div>
 
-        <label class="comparator-field">
-          <span>{{ vm.content.selector_label }}</span>
-          <select name="a" data-comparator-select>
-            <option value="">Seleccionar {{ vm.content.entity_label }}</option>
-            {% for choice in vm.content.choices %}
-              <option value="{{ choice.id }}" {% if choice.id == vm.content.selection_a.id %}selected{% endif %}>{{ choice.name }}</option>
-            {% endfor %}
-          </select>
-        </label>
+          <label class="comparator-field">
+            <span>{{ vm.content.selector_label }}</span>
+            <select name="item_{{ forloop.counter }}" data-comparator-select>
+              <option value="">Seleccionar {{ vm.content.entity_label }}</option>
+              {% for choice in vm.content.choices %}
+                <option value="{{ choice.id }}" {% if choice.id == selection.id %}selected{% endif %}>{{ choice.name }}</option>
+              {% endfor %}
+            </select>
+          </label>
 
-        {% if vm.content.show_quantity_inputs %}
-          <label class="comparator-field comparator-field--quantity {% if not vm.content.selection_a.id %}is-hidden{% endif %}" data-comparator-quantity>
-            <span>Cantidad</span>
-            <input type="number" name="qty_a" min="1" step="1" value="{{ vm.content.selection_a.quantity|floatformat:0 }}">
-            <em>{{ vm.content.quantity_unit }}</em>
-          </label>
-        {% endif %}
-      </section>
+          {% if vm.content.show_quantity_inputs %}
+            <label class="comparator-field comparator-field--quantity {% if not selection.id %}is-hidden{% endif %}" data-comparator-quantity>
+              <span>Cantidad</span>
+              <input type="number" name="qty_{{ forloop.counter }}" min="1" step="1" value="{{ selection.quantity|floatformat:0 }}">
+              <em>{{ vm.content.quantity_unit }}</em>
+            </label>
+          {% endif %}
 
-      <section class="comparator-selector" data-comparator-selector>
-        <div class="comparator-selector__heading">
-          <span class="comparator-selector__badge">2</span>
-          <div>
-            <p>{{ vm.content.selector_b_label }}</p>
-            {% if vm.content.selection_b.name %}<strong>{{ vm.content.selection_b.name }}</strong>{% endif %}
-          </div>
-        </div>
-
-        <label class="comparator-field">
-          <span>{{ vm.content.selector_label }}</span>
-          <select name="b" data-comparator-select>
-            <option value="">Seleccionar {{ vm.content.entity_label }}</option>
-            {% for choice in vm.content.choices %}
-              <option value="{{ choice.id }}" {% if choice.id == vm.content.selection_b.id %}selected{% endif %}>{{ choice.name }}</option>
-            {% endfor %}
-          </select>
-        </label>
-
-        {% if vm.content.show_quantity_inputs %}
-          <label class="comparator-field comparator-field--quantity {% if not vm.content.selection_b.id %}is-hidden{% endif %}" data-comparator-quantity>
-            <span>Cantidad</span>
-            <input type="number" name="qty_b" min="1" step="1" value="{{ vm.content.selection_b.quantity|floatformat:0 }}">
-            <em>{{ vm.content.quantity_unit }}</em>
-          </label>
-        {% endif %}
-      </section>
+          {% if vm.content.can_remove_selection %}
+            <button type="submit" name="remove_index" value="{{ forloop.counter0 }}" class="data-grid-edit-actions__button data-grid-edit-actions__button--secondary comparator-remove">
+              <i data-lucide="x" class="comparator-remove__icon" aria-hidden="true"></i>
+              <span>Eliminar de la comparación</span>
+            </button>
+          {% endif %}
+        </section>
+      {% endfor %}
     </div>
 
     <div class="comparator-form__actions">
+      <button type="submit" name="comparator_action" value="add" class="data-grid-edit-actions__button data-grid-edit-actions__button--secondary comparator-add">
+        <i data-lucide="plus" class="comparator-add__icon" aria-hidden="true"></i>
+        <span>{{ vm.content.add_action_label }}</span>
+      </button>
+
       <button type="submit" class="data-grid-edit-actions__button data-grid-edit-actions__button--primary comparator-submit">
         <i data-lucide="columns-3" class="comparator-submit__icon" aria-hidden="true"></i>
         <span>Comparar</span>
@@ -99,27 +85,18 @@
           </header>
 
           <div class="comparator-bars">
-            <div class="comparator-bar-row comparator-bar-row--a">
-              <div class="comparator-bar-row__meta">
-                <i data-lucide="{{ vm.content.entity_icon }}" class="comparator-bar-row__entity-icon" aria-hidden="true"></i>
-                <strong>{{ vm.content.selection_a.name }}</strong>
-                <span>{{ metric.a.formatted_value }}</span>
+            {% for bar in metric.bars %}
+              <div class="comparator-bar-row">
+                <div class="comparator-bar-row__meta">
+                  <i data-lucide="{{ vm.content.entity_icon }}" class="comparator-bar-row__entity-icon comparator-bar-row__entity-icon--{{ vm.content.entity_scope }}" aria-hidden="true"></i>
+                  <strong>{{ bar.label }}</strong>
+                  <span>{{ bar.formatted_value }}</span>
+                </div>
+                <div class="comparator-bar-track">
+                  <span class="comparator-bar" style="width: {{ bar.width|unlocalize }}%;"></span>
+                </div>
               </div>
-              <div class="comparator-bar-track">
-                <span class="comparator-bar comparator-bar--a" style="width: {{ metric.a.width|unlocalize }}%;"></span>
-              </div>
-            </div>
-
-            <div class="comparator-bar-row comparator-bar-row--b">
-              <div class="comparator-bar-row__meta">
-                <i data-lucide="{{ vm.content.entity_icon }}" class="comparator-bar-row__entity-icon" aria-hidden="true"></i>
-                <strong>{{ vm.content.selection_b.name }}</strong>
-                <span>{{ metric.b.formatted_value }}</span>
-              </div>
-              <div class="comparator-bar-track">
-                <span class="comparator-bar comparator-bar--b" style="width: {{ metric.b.width|unlocalize }}%;"></span>
-              </div>
-            </div>
+            {% endfor %}
           </div>
         </article>
       {% endfor %}
