diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' proyecto_django_export_original/notas/application/queries/food_picker_queries.py proyecto_django_export/notas/application/queries/food_picker_queries.py
--- proyecto_django_export_original/notas/application/queries/food_picker_queries.py	2026-05-19 03:58:52.000000000 +0000
+++ proyecto_django_export/notas/application/queries/food_picker_queries.py	2026-06-08 17:23:25.642071159 +0000
@@ -1,5 +1,6 @@
 from dataclasses import asdict, dataclass
 
+from django.core.exceptions import ObjectDoesNotExist
 from django.db.models import Case, IntegerField, Q, QuerySet, Value, When
 
 from notas.application.services.food_imports.localized_names import (
@@ -96,6 +97,7 @@
                 visibility__in=visible_global_values,
             )
         )
+        .select_related("source_metadata")
         .prefetch_related(
             "aliases",
             "localized_names",
@@ -184,6 +186,26 @@
     return result.foods
 
 
+def get_food_picker_item_by_id(
+    *,
+    user,
+    food_id: int,
+) -> FoodPickerItemDTO | None:
+    food = (
+        get_food_picker_queryset(user)
+        .filter(id=food_id)
+        .first()
+    )
+
+    if food is None:
+        return None
+
+    return build_food_picker_item_dto(
+        food=food,
+        user=user,
+    )
+
+
 def build_food_picker_item_dto(
     *,
     food: Food,
@@ -312,8 +334,13 @@
 
 
 def _resolve_source(food: Food) -> str:
-    if hasattr(food, "source_metadata"):
-        return food.source_metadata.source
+    try:
+        metadata = food.source_metadata
+    except ObjectDoesNotExist:
+        metadata = None
+
+    if metadata is not None:
+        return metadata.source
 
     if food.is_global:
         return PICKER_SOURCE_GLOBAL
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' proyecto_django_export_original/notas/application/services/nutrition/nutrition_kpis.py proyecto_django_export/notas/application/services/nutrition/nutrition_kpis.py
--- proyecto_django_export_original/notas/application/services/nutrition/nutrition_kpis.py	2026-04-05 04:00:48.000000000 +0000
+++ proyecto_django_export/notas/application/services/nutrition/nutrition_kpis.py	2026-06-08 17:27:24.630056613 +0000
@@ -19,44 +19,157 @@
 
 
 
+def _meal_macro_snapshot(meal):
+    protein = meal.protein_cached if meal.protein_cached is not None else meal.protein
+    carbs = meal.carbs_cached if meal.carbs_cached is not None else meal.carbs
+    fat = meal.fat_cached if meal.fat_cached is not None else meal.fat
+
+    kcal_protein = (
+        meal.kcal_protein_cached
+        if meal.kcal_protein_cached is not None
+        else meal.kcal_protein
+    )
+    kcal_carbs = (
+        meal.kcal_carbs_cached
+        if meal.kcal_carbs_cached is not None
+        else meal.kcal_carbs
+    )
+    kcal_fat = (
+        meal.kcal_fat_cached
+        if meal.kcal_fat_cached is not None
+        else meal.kcal_fat
+    )
+
+    return {
+        "protein": protein,
+        "carbs": carbs,
+        "fat": fat,
+        "kcal_protein": kcal_protein,
+        "kcal_carbs": kcal_carbs,
+        "kcal_fat": kcal_fat,
+    }
+
+
+def build_dailyplan_nutrition_snapshot(dailyplan):
+    totals = {
+        "protein": 0,
+        "carbs": 0,
+        "fat": 0,
+        "kcal_protein": 0,
+        "kcal_carbs": 0,
+        "kcal_fat": 0,
+    }
+
+    for dpm in dailyplan.dailyplan_meals.all():
+        meal_totals = _meal_macro_snapshot(dpm.meal)
+
+        for key in totals:
+            totals[key] += meal_totals[key]
+
+    total_kcal = (
+        totals["kcal_protein"]
+        + totals["kcal_carbs"]
+        + totals["kcal_fat"]
+    )
+
+    if total_kcal > 0:
+        alloc = {
+            "protein": totals["kcal_protein"] / total_kcal * 100,
+            "carbs": totals["kcal_carbs"] / total_kcal * 100,
+            "fat": totals["kcal_fat"] / total_kcal * 100,
+        }
+    else:
+        alloc = {
+            "protein": 0,
+            "carbs": 0,
+            "fat": 0,
+        }
+
+    return {
+        **totals,
+        "total_kcal": total_kcal,
+        "alloc": alloc,
+    }
+
+
 #=====TRAIDOS DESDE NUTRITION=========
 
 def build_nutrition_kpis_from_meal(meal, user):
+    snapshot = _meal_macro_snapshot(meal)
+    total_kcal = (
+        snapshot["kcal_protein"]
+        + snapshot["kcal_carbs"]
+        + snapshot["kcal_fat"]
+    )
+
+    if total_kcal > 0:
+        alloc = {
+            "protein": snapshot["kcal_protein"] / total_kcal * 100,
+            "carbs": snapshot["kcal_carbs"] / total_kcal * 100,
+            "fat": snapshot["kcal_fat"] / total_kcal * 100,
+        }
+    else:
+        alloc = {
+            "protein": 0,
+            "carbs": 0,
+            "fat": 0,
+        }
+
+    weight = get_current_weight(user)
+    protein = float(snapshot["protein"])
+    ppk = (protein / weight) if (weight and protein) else None
+
     return {
-        "total_kcal": float(meal.total_kcal),
+        "total_kcal": float(total_kcal),
 
-        "protein": float(meal.protein),
-        "carbs": float(meal.carbs),
-        "fat": float(meal.fat),
+        "protein": protein,
+        "carbs": float(snapshot["carbs"]),
+        "fat": float(snapshot["fat"]),
 
         "alloc": {
-            "protein": float(meal.alloc["protein"]),
-            "carbs": float(meal.alloc["carbs"]),
-            "fat": float(meal.alloc["fat"]),
+            "protein": float(alloc["protein"]),
+            "carbs": float(alloc["carbs"]),
+            "fat": float(alloc["fat"]),
         },
 
-        "ppk": get_ppk_meal(meal, user),
-        "weight": get_current_weight (user)
+        "ppk": {
+            "ppk": ppk,
+        },
+        "weight": weight,
+        "kcal_protein": float(snapshot["kcal_protein"]),
+        "kcal_carbs": float(snapshot["kcal_carbs"]),
+        "kcal_fat": float(snapshot["kcal_fat"]),
     }
 
 
 
 def build_nutrition_kpis_from_dailyplan(dailyplan, user):
+    snapshot = build_dailyplan_nutrition_snapshot(dailyplan)
+    weight = get_current_weight(user)
+    protein = float(snapshot["protein"])
+
+    ppk = (protein / weight) if (weight and protein) else None
+
     return {
-        "total_kcal": float(dailyplan.total_kcal),
-        
-        "protein": float(dailyplan.protein),
-        "carbs": float(dailyplan.carbs),
-        "fat": float(dailyplan.fat),
+        "total_kcal": float(snapshot["total_kcal"]),
+
+        "protein": protein,
+        "carbs": float(snapshot["carbs"]),
+        "fat": float(snapshot["fat"]),
 
         "alloc": {
-            "protein": float(dailyplan.alloc["protein"]),
-            "carbs": float(dailyplan.alloc["carbs"]),
-            "fat": float(dailyplan.alloc["fat"]),
+            "protein": float(snapshot["alloc"]["protein"]),
+            "carbs": float(snapshot["alloc"]["carbs"]),
+            "fat": float(snapshot["alloc"]["fat"]),
         },
 
-        "ppk": get_ppk_dailyplan(dailyplan, user),
-        "weight": get_current_weight (user)
+        "ppk": {
+            "ppk": ppk,
+        },
+        "weight": weight,
+        "kcal_protein": float(snapshot["kcal_protein"]),
+        "kcal_carbs": float(snapshot["kcal_carbs"]),
+        "kcal_fat": float(snapshot["kcal_fat"]),
     }
 
 
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' proyecto_django_export_original/notas/application/use_cases/dpm_pages.py proyecto_django_export/notas/application/use_cases/dpm_pages.py
--- proyecto_django_export_original/notas/application/use_cases/dpm_pages.py	2026-05-14 18:36:35.000000000 +0000
+++ proyecto_django_export/notas/application/use_cases/dpm_pages.py	2026-06-08 17:25:20.667360862 +0000
@@ -3,9 +3,9 @@
 import json
 
 from django.core.serializers.json import DjangoJSONEncoder
+from django.db.models import Prefetch
 from django.shortcuts import get_object_or_404
 
-from notas.application.queries.food_picker_queries import list_food_picker_items
 from notas.application.services.nutrition.nutrition_kpis import (
     build_nutrition_kpis_from_dailyplan,
     build_nutrition_kpis_from_meal,
@@ -29,6 +29,17 @@
         .prefetch_related(
             "meal__meal_food_set",
             "meal__meal_food_set__food",
+            Prefetch(
+                "dailyplan__dailyplan_meals",
+                queryset=(
+                    DailyPlanMeal.objects
+                    .select_related("meal")
+                    .prefetch_related(
+                        "meal__meal_food_set",
+                        "meal__meal_food_set__food",
+                    )
+                ),
+            ),
         ),
         id=dpm_id,
         dailyplan_id=dailyplan_id,
@@ -50,15 +61,6 @@
     return edit_mf_id, mealfood
 
 
-def _build_food_picker_items_payload(user) -> list[dict]:
-    picker_items = list_food_picker_items(user=user)
-
-    return [
-        food.as_dict()
-        for food in picker_items.foods
-    ]
-
-
 @dataclass
 class DpmDetailPageData:
     dailyplan: Any
@@ -70,6 +72,7 @@
     editing_mealfood_id: Optional[int] = None
     foods_json: str = "[]"
     food_picker_context_json: str = "{}"
+    can_edit_foods: bool = False
     viewmode: Any = None
 
 
@@ -96,15 +99,15 @@
     editing_mealfood_id = None
     foods_json = "[]"
     food_picker_context_json = "{}"
+    can_edit_foods = False
 
     if viewmode == DAILYPLAN_MEAL_VIEWMODE_DETAIL:
+        can_edit_foods = True
         edit_mf_id, mealfood = _get_optional_editing_mealfood(
             request_get=request_get,
             meal=meal,
         )
 
-        foods_payload = _build_food_picker_items_payload(user)
-
         meal_kpis = build_nutrition_kpis_from_meal(
             meal,
             user,
@@ -126,11 +129,6 @@
         selected_food_id = request_get.get("select_food")
         editing_mealfood_id = int(edit_mf_id) if edit_mf_id else None
 
-        foods_json = json.dumps(
-            foods_payload,
-            cls=DjangoJSONEncoder,
-        )
-
         food_picker_context_json = json.dumps(
             food_picker_ctx.as_dict(),
             cls=DjangoJSONEncoder,
@@ -143,6 +141,8 @@
         meal_foods=meal_foods,
         user=user,
         viewmode=viewmode,
+        dailyplan_kpis=dailyplan_kpis if viewmode == DAILYPLAN_MEAL_VIEWMODE_DETAIL else None,
+        meal_kpis=meal_kpis if viewmode == DAILYPLAN_MEAL_VIEWMODE_DETAIL else None,
     )
 
     return DpmDetailPageData(
@@ -155,6 +155,7 @@
         editing_mealfood_id=editing_mealfood_id,
         foods_json=foods_json,
         food_picker_context_json=food_picker_context_json,
+        can_edit_foods=can_edit_foods,
         viewmode=viewmode,
     )
 
@@ -192,6 +193,8 @@
         meal_foods=meal_foods,
         user=user,
         viewmode=viewmode,
+        dailyplan_kpis=dailyplan_kpis if viewmode == DAILYPLAN_MEAL_VIEWMODE_DETAIL else None,
+        meal_kpis=meal_kpis if viewmode == DAILYPLAN_MEAL_VIEWMODE_DETAIL else None,
     )
 
     return DpmEditPageData(
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' proyecto_django_export_original/notas/application/use_cases/meal_pages.py proyecto_django_export/notas/application/use_cases/meal_pages.py
--- proyecto_django_export_original/notas/application/use_cases/meal_pages.py	2026-05-14 18:36:21.000000000 +0000
+++ proyecto_django_export/notas/application/use_cases/meal_pages.py	2026-06-08 17:22:47.880747715 +0000
@@ -5,7 +5,6 @@
 from django.core.serializers.json import DjangoJSONEncoder
 from django.shortcuts import get_object_or_404
 
-from notas.application.queries.food_picker_queries import list_food_picker_items
 from notas.application.resolvers.meal_resolvers import (
     resolve_meal_page_actions,
 )
@@ -40,6 +39,7 @@
     editing_mealfood_id: Optional[int] = None
     foods_json: str = "[]"
     food_picker_context_json: str = "{}"
+    can_edit_foods: bool = False
     show_return_to_dailyplan: bool = False
     viewmode: Any = None
 
@@ -52,15 +52,6 @@
     viewmode: Any
 
 
-def _build_food_picker_items_payload(user) -> list[dict]:
-    picker_items = list_food_picker_items(user=user)
-
-    return [
-        food.as_dict()
-        for food in picker_items.foods
-    ]
-
-
 def get_meal_detail_page_data(
     user,
     meal_id: int,
@@ -82,10 +73,12 @@
     foods_json = "[]"
     food_picker_context_json = "{}"
     show_return_to_dailyplan = False
+    can_edit_foods = False
 
     effective_viewmode = viewmode
 
     if viewmode == MEAL_VIEWMODE_PERSONAL_DETAIL and meal.created_by == user:
+        can_edit_foods = True
         edit_mf_id = request_get.get("edit_food")
         mealfood = None
 
@@ -96,8 +89,6 @@
                 meal=meal,
             )
 
-        foods_payload = _build_food_picker_items_payload(user)
-
         nutrition_kpis = build_nutrition_kpis_from_meal(
             meal,
             user,
@@ -109,11 +100,6 @@
             mealfood=mealfood,
         )
 
-        foods_json = json.dumps(
-            foods_payload,
-            cls=DjangoJSONEncoder,
-        )
-
         food_picker_context_json = json.dumps(
             food_picker_ctx.as_dict(),
             cls=DjangoJSONEncoder,
@@ -148,6 +134,7 @@
         editing_mealfood_id=editing_mealfood_id,
         foods_json=foods_json,
         food_picker_context_json=food_picker_context_json,
+        can_edit_foods=can_edit_foods,
         show_return_to_dailyplan=show_return_to_dailyplan,
         viewmode=effective_viewmode,
     )
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' proyecto_django_export_original/notas/interface/views/dailyplan_meals.py proyecto_django_export/notas/interface/views/dailyplan_meals.py
--- proyecto_django_export_original/notas/interface/views/dailyplan_meals.py	2026-05-26 04:52:38.000000000 +0000
+++ proyecto_django_export/notas/interface/views/dailyplan_meals.py	2026-06-08 17:22:59.573421304 +0000
@@ -129,6 +129,7 @@
     context = base_vm.as_context()
     context["foods_json"] = page.foods_json
     context["food_picker_json"] = page.food_picker_context_json
+    context["can_edit_foods"] = page.can_edit_foods
     context["selected_food_id"] = page.selected_food_id
     context["editing_mealfood_id"] = page.editing_mealfood_id
 
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' proyecto_django_export_original/notas/interface/views/foods.py proyecto_django_export/notas/interface/views/foods.py
--- proyecto_django_export_original/notas/interface/views/foods.py	2026-05-19 03:29:28.000000000 +0000
+++ proyecto_django_export/notas/interface/views/foods.py	2026-06-08 17:23:38.851165103 +0000
@@ -6,7 +6,10 @@
 from django.contrib import messages
 from notas.application.services.access.capabilities import get_capabilities
 from notas.domain.models import Food
-from notas.application.queries.food_picker_queries import list_food_picker_items
+from notas.application.queries.food_picker_queries import (
+    get_food_picker_item_by_id,
+    list_food_picker_items,
+)
 from notas.presentation.config.viewmodel_config import (
     FOOD_VIEWMODE_PERSONAL_LIST, 
     FOOD_VIEWMODE_PERSONAL_DETAIL,
@@ -268,21 +271,39 @@
 def foods_json(request):
     search = request.GET.get("search")
     raw_limit = request.GET.get("limit")
+    raw_food_id = request.GET.get("food_id")
 
     try:
         limit = int(raw_limit) if raw_limit else 80
     except (TypeError, ValueError):
         limit = 80
 
-    picker_items = list_food_picker_items(
-        user=request.user,
-        search=search,
-        limit=limit,
-    )
+    if raw_food_id:
+        try:
+            food_id = int(raw_food_id)
+        except (TypeError, ValueError):
+            return JsonResponse([], safe=False)
+
+        item = get_food_picker_item_by_id(
+            user=request.user,
+            food_id=food_id,
+        )
+
+        if item is None:
+            return JsonResponse([], safe=False)
+
+        picker_foods = [item]
+    else:
+        picker_items = list_food_picker_items(
+            user=request.user,
+            search=search,
+            limit=limit,
+        )
+        picker_foods = picker_items.foods
 
     foods = []
 
-    for item in picker_items.foods:
+    for item in picker_foods:
         foods.append({
             "id": item.id,
             "name": item.name,
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' proyecto_django_export_original/notas/interface/views/meals.py proyecto_django_export/notas/interface/views/meals.py
--- proyecto_django_export_original/notas/interface/views/meals.py	2026-05-12 04:29:47.000000000 +0000
+++ proyecto_django_export/notas/interface/views/meals.py	2026-06-08 17:22:59.572909697 +0000
@@ -340,6 +340,7 @@
     context["show_return_to_dailyplan"] = page.show_return_to_dailyplan
     context["foods_json"] = page.foods_json
     context["food_picker_context"] = page.food_picker_context_json
+    context["can_edit_foods"] = page.can_edit_foods
     context["editing_mealfood_id"] = page.editing_mealfood_id
     context["selected_food_id"] = page.selected_food_id
 
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' proyecto_django_export_original/notas/presentation/composition/viewmodel/dpm/dpm_content.py proyecto_django_export/notas/presentation/composition/viewmodel/dpm/dpm_content.py
--- proyecto_django_export_original/notas/presentation/composition/viewmodel/dpm/dpm_content.py	2026-05-19 04:03:47.000000000 +0000
+++ proyecto_django_export/notas/presentation/composition/viewmodel/dpm/dpm_content.py	2026-06-08 17:27:36.246988681 +0000
@@ -8,8 +8,8 @@
     build_meal_foods_aggregation,
 )
 from notas.application.services.nutrition.nutrition_kpis import (
-    get_ppk_dailyplan,
-    get_ppk_meal,
+    build_nutrition_kpis_from_dailyplan,
+    build_nutrition_kpis_from_meal,
 )
 from notas.presentation.composition.viewmodel.components.builder_headers import (
     build_dailyplan_meal_header,
@@ -39,6 +39,8 @@
     meal_foods,
     user,
     viewmode,
+    dailyplan_kpis=None,
+    meal_kpis=None,
 ):
     header = build_dailyplan_meal_header(
         dpm=dpm,
@@ -46,35 +48,38 @@
         viewmode=viewmode,
     )
 
-    dp_total_kcal = dailyplan.total_kcal
-    dp_protein = dailyplan.protein
-    dp_carbs = dailyplan.carbs
-    dp_fat = dailyplan.fat
-
-    dp_kcal_protein = dailyplan.kcal_protein
-    dp_kcal_carbs = dailyplan.kcal_carbs
-    dp_kcal_fat = dailyplan.kcal_fat
-
-    dp_alloc = dailyplan.alloc
-
-    father_ppk = get_ppk_dailyplan(dailyplan, user)
-
-    meal_total_kcal = meal.total_kcal_cached or meal.total_kcal
-    meal_protein = meal.protein_cached or meal.protein
-    meal_carbs = meal.carbs_cached or meal.carbs
-    meal_fat = meal.fat_cached or meal.fat
-
-    meal_kcal_protein = meal.kcal_protein_cached or meal.kcal_protein
-    meal_kcal_carbs = meal.kcal_carbs_cached or meal.kcal_carbs
-    meal_kcal_fat = meal.kcal_fat_cached or meal.kcal_fat
-
-    meal_alloc = {
-        "protein": meal.alloc_protein_cached or meal.alloc["protein"],
-        "carbs": meal.alloc_carbs_cached or meal.alloc["carbs"],
-        "fat": meal.alloc_fat_cached or meal.alloc["fat"],
-    }
+    dailyplan_kpis = dailyplan_kpis or build_nutrition_kpis_from_dailyplan(
+        dailyplan,
+        user,
+    )
+    meal_kpis = meal_kpis or build_nutrition_kpis_from_meal(
+        meal,
+        user,
+    )
+
+    dp_total_kcal = dailyplan_kpis["total_kcal"]
+    dp_protein = dailyplan_kpis["protein"]
+    dp_carbs = dailyplan_kpis["carbs"]
+    dp_fat = dailyplan_kpis["fat"]
+
+    dp_kcal_protein = dailyplan_kpis.get("kcal_protein", dp_protein * 4)
+    dp_kcal_carbs = dailyplan_kpis.get("kcal_carbs", dp_carbs * 4)
+    dp_kcal_fat = dailyplan_kpis.get("kcal_fat", dp_fat * 9)
+
+    dp_alloc = dailyplan_kpis["alloc"]
+    father_ppk = dailyplan_kpis["ppk"]
+
+    meal_total_kcal = meal_kpis["total_kcal"]
+    meal_protein = meal_kpis["protein"]
+    meal_carbs = meal_kpis["carbs"]
+    meal_fat = meal_kpis["fat"]
+
+    meal_kcal_protein = meal_kpis.get("kcal_protein", meal_protein * 4)
+    meal_kcal_carbs = meal_kpis.get("kcal_carbs", meal_carbs * 4)
+    meal_kcal_fat = meal_kpis.get("kcal_fat", meal_fat * 9)
 
-    meal_ppk = get_ppk_meal(meal, user)
+    meal_alloc = meal_kpis["alloc"]
+    meal_ppk = meal_kpis["ppk"]
 
     meal_foods_table_items = [
         build_mealfood_table_item(mf)
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' proyecto_django_export_original/notas/static/notas/js/dpm_food_picker.js proyecto_django_export/notas/static/notas/js/dpm_food_picker.js
--- proyecto_django_export_original/notas/static/notas/js/dpm_food_picker.js	2026-06-02 05:31:35.000000000 +0000
+++ proyecto_django_export/notas/static/notas/js/dpm_food_picker.js	2026-06-08 17:24:21.343938803 +0000
@@ -24,13 +24,14 @@
 document.addEventListener("DOMContentLoaded", () => {
 
   const ctx = window.FOOD_PICKER_CONTEXT;
-  const foods = Array.isArray(window.FOOD_PICKER_FOODS)
+  let foods = Array.isArray(window.FOOD_PICKER_FOODS)
     ? window.FOOD_PICKER_FOODS
     : [];
 
   const foodSearchCache = new Map();
   let activeSearchController = null;
   let searchDebounceTimer = null;
+  let initialFoodsLoaded = foods.length > 0;
 
   foods.forEach(food => {
     if (food?.id) {
@@ -98,6 +99,20 @@
     return foodSearchCache.get(Number(foodId)) || null;
   }
 
+  function cacheFoodResults(results) {
+    if (!Array.isArray(results)) return;
+
+    results.forEach(food => {
+      if (food?.id) {
+        foodSearchCache.set(Number(food.id), food);
+      }
+    });
+  }
+
+  function getCachedFoodResults() {
+    return Array.from(foodSearchCache.values());
+  }
+
   function getFoodDisplayName(food) {
     return food?.display_name || food?.name || "";
   }
@@ -164,67 +179,101 @@
 
   function filterFoodsBySearch(value) {
     const normalizedValue = normalizeSearchValue(value);
+    const cachedFoods = getCachedFoodResults();
 
     if (!normalizedValue) {
-      return foods;
+      return cachedFoods;
     }
 
-    return foods.filter(food => {
+    return cachedFoods.filter(food => {
       return getFoodSearchText(food).includes(normalizedValue);
     });
   }
 
-  function getInitialFoodResults() {
-    return foods;
-  }
-
-  function getFoodSearchUrl(value) {
+  function getFoodSearchUrl(value, limit = 50) {
     const params = new URLSearchParams();
     params.set("search", value);
-    params.set("limit", "50");
+    params.set("limit", String(limit));
 
     return `/app/api/foods/?${params.toString()}`;
   }
 
-  async function searchFoodsFromServer(value) {
-    const normalizedValue = normalizeSearchValue(value);
-
-    if (!normalizedValue) {
-      return getInitialFoodResults();
-    }
-
-    if (activeSearchController) {
-      activeSearchController.abort();
-    }
+  function getFoodByIdUrl(foodId) {
+    const params = new URLSearchParams();
+    params.set("food_id", String(foodId));
+    params.set("limit", "1");
 
-    activeSearchController = new AbortController();
+    return `/app/api/foods/?${params.toString()}`;
+  }
 
-    const response = await fetch(getFoodSearchUrl(value), {
+  async function fetchFoods(url, signal = undefined) {
+    const response = await fetch(url, {
       headers: {
         "Accept": "application/json"
       },
-      signal: activeSearchController.signal
+      signal
     });
 
     if (!response.ok) {
-      return filterFoodsBySearch(value);
+      return [];
     }
 
     const results = await response.json();
 
     if (!Array.isArray(results)) {
-      return filterFoodsBySearch(value);
+      return [];
     }
 
-    results.forEach(food => {
-      if (food?.id) {
-        foodSearchCache.set(Number(food.id), food);
-      }
-    });
+    cacheFoodResults(results);
 
     return results;
   }
 
+  async function getInitialFoodResults() {
+    if (initialFoodsLoaded) {
+      return foods;
+    }
+
+    const results = await fetchFoods(getFoodSearchUrl("", 50));
+    foods = results;
+    initialFoodsLoaded = true;
+
+    return foods;
+  }
+
+  async function fetchFoodById(foodId) {
+    const cachedFood = findFoodById(foodId);
+
+    if (cachedFood) {
+      return cachedFood;
+    }
+
+    const results = await fetchFoods(getFoodByIdUrl(foodId));
+
+    return results[0] || null;
+  }
+
+  async function searchFoodsFromServer(value) {
+    const normalizedValue = normalizeSearchValue(value);
+
+    if (!normalizedValue) {
+      return getInitialFoodResults();
+    }
+
+    if (activeSearchController) {
+      activeSearchController.abort();
+    }
+
+    activeSearchController = new AbortController();
+
+    const results = await fetchFoods(
+      getFoodSearchUrl(value, 50),
+      activeSearchController.signal
+    );
+
+    return results.length ? results : filterFoodsBySearch(value);
+  }
+
   function scheduleServerSearch(value) {
     window.clearTimeout(searchDebounceTimer);
 
@@ -443,11 +492,10 @@
     let baseMeal = ctx.meal.kpis;
 
     if (isEdit()) {
-      const oldPortion = portionFromFoodById(
-        foods,
-        ctx.editing.food_id,
-        ctx.editing.original_quantity
-      );
+      const originalFood = findFoodById(ctx.editing.food_id);
+      const oldPortion = originalFood
+        ? portionFromFood(originalFood, ctx.editing.original_quantity)
+        : null;
 
       baseMeal = removePortionTotals(ctx.meal.kpis, oldPortion);
     }
@@ -463,11 +511,10 @@
     let baseDailyPlan = ctx.dailyplan.kpis;
 
     if (isEdit()) {
-      const oldPortion = portionFromFoodById(
-        foods,
-        ctx.editing.food_id,
-        ctx.editing.original_quantity
-      );
+      const originalFood = findFoodById(ctx.editing.food_id);
+      const oldPortion = originalFood
+        ? portionFromFood(originalFood, ctx.editing.original_quantity)
+        : null;
 
       baseDailyPlan = removePortionTotals(
         ctx.dailyplan.kpis,
@@ -487,8 +534,8 @@
   // ---------------------------
   // Input events
   // ---------------------------
-  input.addEventListener("focus", () => {
-    renderFoodList(foods);
+  input.addEventListener("focus", async () => {
+    renderFoodList(await getInitialFoodResults());
     openList();
     schedulePickerScrollIntoMobileView();
   });
@@ -497,8 +544,10 @@
     const value = input.value;
 
     if (!normalizeSearchValue(value)) {
-      renderFoodList(getInitialFoodResults());
-      openList();
+      getInitialFoodResults().then(results => {
+        renderFoodList(results);
+        openList();
+      });
       return;
     }
 
@@ -532,12 +581,14 @@
         detail: { sectionId: "dpm-picker-section" }
       }));
 
-      selectedFood = findFoodById(ctx.editing.food_id);
-      if (!selectedFood) return;
+      fetchFoodById(ctx.editing.food_id).then(food => {
+        selectedFood = food;
+        if (!selectedFood) return;
 
-      input.value = getFoodDisplayName(selectedFood);
-      showPreview();
-      schedulePickerScrollIntoMobileView();
+        input.value = getFoodDisplayName(selectedFood);
+        showPreview();
+        schedulePickerScrollIntoMobileView();
+      });
     });
   });
 
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' proyecto_django_export_original/notas/static/notas/js/food_picker.js proyecto_django_export/notas/static/notas/js/food_picker.js
--- proyecto_django_export_original/notas/static/notas/js/food_picker.js	2026-06-02 05:31:35.000000000 +0000
+++ proyecto_django_export/notas/static/notas/js/food_picker.js	2026-06-08 17:24:21.342990290 +0000
@@ -22,13 +22,14 @@
 document.addEventListener("DOMContentLoaded", () => {
 
   const ctx = window.FOOD_PICKER_CONTEXT;
-  const foods = Array.isArray(window.FOOD_PICKER_FOODS)
+  let foods = Array.isArray(window.FOOD_PICKER_FOODS)
     ? window.FOOD_PICKER_FOODS
     : [];
 
   const foodSearchCache = new Map();
   let activeSearchController = null;
   let searchDebounceTimer = null;
+  let initialFoodsLoaded = foods.length > 0;
 
   foods.forEach(food => {
     if (food?.id) {
@@ -96,6 +97,20 @@
     return foodSearchCache.get(Number(foodId)) || null;
   }
 
+  function cacheFoodResults(results) {
+    if (!Array.isArray(results)) return;
+
+    results.forEach(food => {
+      if (food?.id) {
+        foodSearchCache.set(Number(food.id), food);
+      }
+    });
+  }
+
+  function getCachedFoodResults() {
+    return Array.from(foodSearchCache.values());
+  }
+
   function getFoodDisplayName(food) {
     return food?.display_name || food?.name || "";
   }
@@ -162,67 +177,101 @@
 
   function filterFoodsBySearch(value) {
     const normalizedValue = normalizeSearchValue(value);
+    const cachedFoods = getCachedFoodResults();
 
     if (!normalizedValue) {
-      return foods;
+      return cachedFoods;
     }
 
-    return foods.filter(food => {
+    return cachedFoods.filter(food => {
       return getFoodSearchText(food).includes(normalizedValue);
     });
   }
 
-  function getInitialFoodResults() {
-    return foods;
-  }
-
-  function getFoodSearchUrl(value) {
+  function getFoodSearchUrl(value, limit = 50) {
     const params = new URLSearchParams();
     params.set("search", value);
-    params.set("limit", "50");
+    params.set("limit", String(limit));
 
     return `/app/api/foods/?${params.toString()}`;
   }
 
-  async function searchFoodsFromServer(value) {
-    const normalizedValue = normalizeSearchValue(value);
-
-    if (!normalizedValue) {
-      return getInitialFoodResults();
-    }
-
-    if (activeSearchController) {
-      activeSearchController.abort();
-    }
+  function getFoodByIdUrl(foodId) {
+    const params = new URLSearchParams();
+    params.set("food_id", String(foodId));
+    params.set("limit", "1");
 
-    activeSearchController = new AbortController();
+    return `/app/api/foods/?${params.toString()}`;
+  }
 
-    const response = await fetch(getFoodSearchUrl(value), {
+  async function fetchFoods(url, signal = undefined) {
+    const response = await fetch(url, {
       headers: {
         "Accept": "application/json"
       },
-      signal: activeSearchController.signal
+      signal
     });
 
     if (!response.ok) {
-      return filterFoodsBySearch(value);
+      return [];
     }
 
     const results = await response.json();
 
     if (!Array.isArray(results)) {
-      return filterFoodsBySearch(value);
+      return [];
     }
 
-    results.forEach(food => {
-      if (food?.id) {
-        foodSearchCache.set(Number(food.id), food);
-      }
-    });
+    cacheFoodResults(results);
 
     return results;
   }
 
+  async function getInitialFoodResults() {
+    if (initialFoodsLoaded) {
+      return foods;
+    }
+
+    const results = await fetchFoods(getFoodSearchUrl("", 50));
+    foods = results;
+    initialFoodsLoaded = true;
+
+    return foods;
+  }
+
+  async function fetchFoodById(foodId) {
+    const cachedFood = findFoodById(foodId);
+
+    if (cachedFood) {
+      return cachedFood;
+    }
+
+    const results = await fetchFoods(getFoodByIdUrl(foodId));
+
+    return results[0] || null;
+  }
+
+  async function searchFoodsFromServer(value) {
+    const normalizedValue = normalizeSearchValue(value);
+
+    if (!normalizedValue) {
+      return getInitialFoodResults();
+    }
+
+    if (activeSearchController) {
+      activeSearchController.abort();
+    }
+
+    activeSearchController = new AbortController();
+
+    const results = await fetchFoods(
+      getFoodSearchUrl(value, 50),
+      activeSearchController.signal
+    );
+
+    return results.length ? results : filterFoodsBySearch(value);
+  }
+
   function scheduleServerSearch(value) {
     window.clearTimeout(searchDebounceTimer);
 
@@ -440,11 +489,10 @@
     let baseMeal = ctx.meal.kpis;
 
     if (isEdit()) {
-      const oldPortion = portionFromFoodById(
-        foods,
-        ctx.editing.food_id,
-        ctx.editing.original_quantity
-      );
+      const originalFood = findFoodById(ctx.editing.food_id);
+      const oldPortion = originalFood
+        ? portionFromFood(originalFood, ctx.editing.original_quantity)
+        : null;
 
       baseMeal = removePortionTotals(ctx.meal.kpis, oldPortion);
     }
@@ -460,8 +508,8 @@
   // ---------------------------
   // Input events
   // ---------------------------
-  input.addEventListener("focus", () => {
-    renderFoodList(foods);
+  input.addEventListener("focus", async () => {
+    renderFoodList(await getInitialFoodResults());
     openList();
     schedulePickerScrollIntoMobileView();
   });
@@ -470,8 +518,10 @@
     const value = input.value;
 
     if (!normalizeSearchValue(value)) {
-      renderFoodList(getInitialFoodResults());
-      openList();
+      getInitialFoodResults().then(results => {
+        renderFoodList(results);
+        openList();
+      });
       return;
     }
 
@@ -505,12 +555,14 @@
         detail: { sectionId: "meal-picker-section" }
       }));
 
-      selectedFood = findFoodById(ctx.editing.food_id);
-      if (!selectedFood) return;
+      fetchFoodById(ctx.editing.food_id).then(food => {
+        selectedFood = food;
+        if (!selectedFood) return;
 
-      input.value = getFoodDisplayName(selectedFood);
-      showPreview();
-      schedulePickerScrollIntoMobileView();
+        input.value = getFoodDisplayName(selectedFood);
+        showPreview();
+        schedulePickerScrollIntoMobileView();
+      });
     });
   });
 
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' proyecto_django_export_original/notas/templates/notas/dailyplan_meals/detail.html proyecto_django_export/notas/templates/notas/dailyplan_meals/detail.html
--- proyecto_django_export_original/notas/templates/notas/dailyplan_meals/detail.html	2026-05-26 23:02:10.000000000 +0000
+++ proyecto_django_export/notas/templates/notas/dailyplan_meals/detail.html	2026-06-08 17:23:11.197727048 +0000
@@ -8,9 +8,9 @@
 {% block content %}
 
 
-<div class="page page--detail has-collapsible-panels dpm dpm_meal {% if foods_json and foods_json != '[]' %}edit{% endif %}" data-page="meal-detail">
+<div class="page page--detail has-collapsible-panels dpm dpm_meal {% if can_edit_foods %}edit{% endif %}" data-page="meal-detail">
 
-  {% if foods_json and foods_json != "[]" %}
+  {% if can_edit_foods %}
     {% include "components/card_main_dpm_edit.html" with main_card=vm.content.main_card %}
   {% else %}
     {% include "components/card_main_dpm.html" with main_card=vm.content.main_card %}
diff -ruN '--exclude=__pycache__' '--exclude=.pytest_cache' proyecto_django_export_original/notas/templates/notas/meals/detail.html proyecto_django_export/notas/templates/notas/meals/detail.html
--- proyecto_django_export_original/notas/templates/notas/meals/detail.html	2026-05-26 05:14:22.000000000 +0000
+++ proyecto_django_export/notas/templates/notas/meals/detail.html	2026-06-08 17:23:11.197135831 +0000
@@ -7,9 +7,9 @@
 
 {% block content %}
 
-<div class="page page--detail has-collapsible-panels {% if foods_json and foods_json != '[]' %}edit{% endif %}" data-page="meal-detail">
+<div class="page page--detail has-collapsible-panels {% if can_edit_foods %}edit{% endif %}" data-page="meal-detail">
 
-    {% if foods_json and foods_json != "[]" %}
+    {% if can_edit_foods %}
       {% include "components/card_main_meal_edit.html" with main_card=vm.content.main_card %}
     {% else %}
       {% include "components/card_main_meal.html" with main_card=vm.content.main_card %}
