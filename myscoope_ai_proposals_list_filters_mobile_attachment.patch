diff --git a/notas/application/queries/proposal_queries.py b/notas/application/queries/proposal_queries.py
index ca08bc3..9d372bf 100644
--- a/notas/application/queries/proposal_queries.py
+++ b/notas/application/queries/proposal_queries.py
@@ -210,9 +210,22 @@ def build_proposal_dto(
     )
 
 
-def list_user_proposals(user) -> list[NutritionProposalListItemDTO]:
+def list_user_proposals(
+    user,
+    *,
+    status_filter: str | None = None,
+) -> list[NutritionProposalListItemDTO]:
     proposals = get_available_proposal_queryset(user)
 
+    if status_filter == NutritionProposal.STATUS_PENDING_REVIEW:
+        proposals = proposals.filter(
+            status=NutritionProposal.STATUS_PENDING_REVIEW,
+        )
+    elif status_filter == NutritionProposal.STATUS_APPROVED:
+        proposals = proposals.filter(
+            status=NutritionProposal.STATUS_APPROVED,
+        )
+
     return [
         build_proposal_list_item_dto(proposal)
         for proposal in proposals
diff --git a/notas/interface/views/proposals.py b/notas/interface/views/proposals.py
index 6fd3e09..634c426 100644
--- a/notas/interface/views/proposals.py
+++ b/notas/interface/views/proposals.py
@@ -61,6 +61,65 @@ class BreadcrumbParent:
         return self.url
 
 
+
+def _get_proposal_list_status_filter(request):
+    status = (request.GET.get("status") or "all").strip()
+
+    if status in {"pending_review", "approved"}:
+        return status
+
+    return None
+
+
+def _build_proposal_list_filter_actions(active_filter: str | None):
+    base_url = reverse("proposal_list")
+
+    filters = [
+        {
+            "key": "filter_pending_review",
+            "label": "Ver solo pendientes de revisión",
+            "url": f"{base_url}?status=pending_review",
+            "icon": "clock",
+            "status_filter": "pending_review",
+            "order": 10,
+        },
+        {
+            "key": "filter_approved",
+            "label": "Ver solo aprobadas",
+            "url": f"{base_url}?status=approved",
+            "icon": "check",
+            "status_filter": "approved",
+            "order": 20,
+        },
+        {
+            "key": "filter_all",
+            "label": "Ver todas",
+            "url": base_url,
+            "icon": "list",
+            "status_filter": None,
+            "order": 30,
+        },
+    ]
+
+    return [
+        {
+            "key": item["key"],
+            "label": item["label"],
+            "url": item["url"],
+            "method": "get",
+            "icon": item["icon"],
+            "order": item["order"],
+            "desktop_position": "menu",
+            "mobile_position": "menu",
+            "extra_class": (
+                "is-active"
+                if item["status_filter"] == active_filter
+                else ""
+            ),
+        }
+        for item in filters
+    ]
+
 def _proposal_list_parent():
     return BreadcrumbParent(
         label="Propuestas",
@@ -129,14 +188,19 @@ def _get_proposal_intent(proposal):
 
 @login_required
 def proposal_list(request):
+    status_filter = _get_proposal_list_status_filter(request)
     proposals = [
         proposal.as_dict()
-        for proposal in list_user_proposals(request.user)
+        for proposal in list_user_proposals(
+            request.user,
+            status_filter=status_filter,
+        )
     ]
 
     content_vm = ProposalListContentVM(
         header=build_page_header(
             title="Propuestas",
+            actions=_build_proposal_list_filter_actions(status_filter),
         ),
         proposals=proposals,
     )
diff --git a/notas/static/notas/css/components/proposals.css b/notas/static/notas/css/components/proposals.css
index 604a900..5434c75 100644
--- a/notas/static/notas/css/components/proposals.css
+++ b/notas/static/notas/css/components/proposals.css
@@ -219,6 +219,16 @@
   line-height: 1.45;
 }
 
+.proposal-list-page .overflow-action.is-active {
+  color: var(--interactive-primary);
+  font-weight: 800;
+}
+
+.proposal-list-page .overflow-action.is-active .overflow-dropdown-icons {
+  color: currentColor;
+  stroke: currentColor;
+}
+
 /* =========================================================
    Shared badges
    ========================================================= */
@@ -748,7 +758,6 @@ html[data-theme="dark"] .proposal-review-action--reject {
     right: 14px;
   }
 
-  .proposal-list-card__bottom,
   .proposal-review-actions,
   .proposal-review-apply,
   .proposal-review-applied {
@@ -756,18 +765,35 @@ html[data-theme="dark"] .proposal-review-action--reject {
     flex-direction: column;
   }
 
-  .proposal-list-card__actions,
-  .proposal-attachment,
+  .proposal-list-card__bottom {
+    align-items: center;
+    flex-direction: row;
+    gap: 10px;
+  }
+
+  .proposal-list-card__bottom .proposal-attachment {
+    flex: 1 1 auto;
+    width: auto;
+    max-width: calc(100% - 48px);
+    justify-content: flex-start;
+  }
+
+  .proposal-list-card__bottom .proposal-list-card__actions {
+    flex: 0 0 auto;
+    width: auto;
+    margin-left: auto;
+  }
+
+  .proposal-list-card__bottom .proposal-list-card__actions .actions {
+    margin-right: 0;
+  }
+
   .proposal-review-apply form,
   .proposal-review-action--apply,
   .proposal-review-result-link {
     width: 100%;
   }
 
-  .proposal-attachment {
-    justify-content: flex-start;
-  }
-
   .proposal-review-attachment {
     width: fit-content;
     max-width: 100%;
diff --git a/notas/templates/components/header_actions.html b/notas/templates/components/header_actions.html
index ee12c14..484aec2 100644
--- a/notas/templates/components/header_actions.html
+++ b/notas/templates/components/header_actions.html
@@ -52,7 +52,7 @@
               <div class="overflow-dropdown">
                   {% for action in header.desktop_menu_actions %}
                       {% if action.method == "get" %}
-                          <a href="{{ action.url }}" class="overflow-action">
+                          <a href="{{ action.url }}" class="overflow-action {{ action.extra_class }}">
                               <i data-lucide="{{ action.icon }}" class="overflow-dropdown-icons"></i>
                               <span>{{ action.label }}</span>
                           </a>
@@ -140,7 +140,7 @@
               <div class="overflow-dropdown">
                   {% for action in header.mobile_menu_actions %}
                       {% if action.method == "get" %}
-                          <a href="{{ action.url }}" class="overflow-action">
+                          <a href="{{ action.url }}" class="overflow-action {{ action.extra_class }}">
                               <i data-lucide="{{ action.icon }}"  class="overflow-dropdown-icons"></i>
                               <span>{{ action.label }}</span>
                           </a>
