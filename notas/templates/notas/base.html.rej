***************
*** 216,221 ****
                  toggle.addEventListener("click", function () {
                      const isCurrent = group.classList.contains("is-current");
                      const isOpen = group.classList.contains("is-open");
      
                      if (isCurrent) {
                          ensureCurrentGroupOpen();
--- 216,234 ----
                  toggle.addEventListener("click", function () {
                      const isCurrent = group.classList.contains("is-current");
                      const isOpen = group.classList.contains("is-open");
+ 
+                     if (isDesktopSidebar() && document.body.classList.contains("sidebar-collapsed")) {
+                         setDesktopSidebarCollapsed(false);
+ 
+                         const extraOpenGroup = getExtraOpenGroup();
+                         if (extraOpenGroup && extraOpenGroup !== group) {
+                             closeGroup(extraOpenGroup);
+                         }
+ 
+                         openGroup(group);
+                         ensureCurrentGroupOpen();
+                         return;
+                     }
      
                      if (isCurrent) {
                          ensureCurrentGroupOpen();
***************
*** 290,301 ****
  
      window.addEventListener("resize", restoreDesktopSidebarState);
  
-     /* cerrar al hacer click en links del sidebar en mobile */
-     document.querySelectorAll(".sidebar-nav a").forEach(link => {
          link.addEventListener("click", () => {
-             if (!isDesktopSidebar()) {
-                 document.body.classList.remove("sidebar-open");
              }
          });
      });
  
--- 303,317 ----
  
      window.addEventListener("resize", restoreDesktopSidebarState);
  
+     /* cerrar al hacer click en links del sidebar en mobile y desplegar sidebar desktop al navegar */
+     document.querySelectorAll(".sidebar a").forEach(link => {
          link.addEventListener("click", () => {
+             if (isDesktopSidebar()) {
+                 setDesktopSidebarCollapsed(false);
+                 return;
              }
+ 
+             document.body.classList.remove("sidebar-open");
          });
      });
  
