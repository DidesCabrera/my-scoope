{
  "name": "my-scoope-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild frontend/src/ai_async_jobs.js --bundle --minify --format=iife --global-name=MyScoopeAsyncJobs --outfile=notas/static/notas/dist/ai_async_jobs.js",
    "generate:ui": "node scripts/generate_ui_tokens.mjs",
    "check:ui": "node scripts/generate_ui_tokens.mjs --check",
    "test": "node --test frontend/tests/*.test.js"
  },
  "devDependencies": {
    "esbuild": "0.25.12"
  }
}
