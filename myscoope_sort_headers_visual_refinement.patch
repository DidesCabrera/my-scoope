--- a/notas/static/notas/css/components/data_grid.css
+++ b/notas/static/notas/css/components/data_grid.css
@@ -967,7 +967,10 @@
     min-width: 0;
     cursor: pointer;
     user-select: none;
+    border-radius: 999px;
     transition:
+        background-color 0.15s ease,
+        box-shadow 0.15s ease,
         color 0.15s ease,
         opacity 0.15s ease;
 }
@@ -979,28 +982,26 @@
     outline: none;
 }
 
-.data-grid-col.is-grid-sortable::after {
-    content: "";
-    display: inline-flex;
-    width: 12px;
-    flex: 0 0 12px;
-    justify-content: center;
-    font-size: 11px;
-    line-height: 1;
-    color: currentColor;
-    opacity: 0;
-}
-
-.data-grid-col.is-grid-sorted::after {
-    opacity: 1;
+.data-grid-col.is-grid-sorted {
+    color: var(--text-main);
+    background: var(--surface-card-muted);
+    box-shadow: inset 0 0 0 1px var(--color-border-1);
+    padding: 4px 8px;
+    margin: -4px 0 4px;
+    font-weight: 750;
 }
 
-.data-grid-col.is-grid-sorted-desc::after {
-    content: "▾";
+.data-grid-sort-icon {
+    width: 14px;
+    height: 14px;
+    flex: 0 0 14px;
+    stroke-width: 2.5;
+    color: currentColor;
 }
 
-.data-grid-col.is-grid-sorted-asc::after {
-    content: "▴";
+.data-grid-col.is-grid-sorted-desc .data-grid-sort-icon,
+.data-grid-col.is-grid-sorted-asc .data-grid-sort-icon {
+    opacity: 1;
 }
 
 .data-grid-col--name.is-grid-sortable,
--- a/notas/static/notas/js/sortable_data_grids.js
+++ b/notas/static/notas/js/sortable_data_grids.js
@@ -10,6 +10,7 @@
   const HEADER_CELL_SELECTOR = ".data-grid-col";
   const ROW_SELECTOR = ".data-grid-row";
   const EMPTY_SELECTOR = ".data-grid-empty";
+  const SORT_ICON_CLASS = "data-grid-sort-icon";
 
   const EXCLUDED_GRID_SELECTORS = [
     ".data-grid--menu",
@@ -87,6 +88,20 @@
     });
   }
 
+  function refreshLucideIcons() {
+    if (window.lucide && typeof window.lucide.createIcons === "function") {
+      window.lucide.createIcons();
+    }
+  }
+
+  function removeSortIcon(headerCell) {
+    const existingIcon = headerCell.querySelector(`.${SORT_ICON_CLASS}`);
+
+    if (existingIcon) {
+      existingIcon.remove();
+    }
+  }
+
   function resetHeaderCells(grid) {
     getHeaderCells(grid).forEach((cell) => {
       cell.classList.remove(
@@ -95,15 +110,25 @@
         "is-grid-sorted-asc"
       );
       cell.removeAttribute("aria-sort");
+      removeSortIcon(cell);
     });
   }
 
   function markHeaderCell(headerCell, direction) {
+    const iconName = direction === "desc" ? "chevron-down" : "chevron-up";
+    const icon = document.createElement("i");
+
+    icon.setAttribute("data-lucide", iconName);
+    icon.setAttribute("aria-hidden", "true");
+    icon.className = SORT_ICON_CLASS;
+
     headerCell.classList.add("is-grid-sorted", `is-grid-sorted-${direction}`);
     headerCell.setAttribute(
       "aria-sort",
       direction === "desc" ? "descending" : "ascending"
     );
+    headerCell.appendChild(icon);
+    refreshLucideIcons();
   }
 
   function restoreOriginalOrder(grid) {
