--- a/notas/application/services/commands/program_commands.py	2026-06-19 01:30:52.000000000 +0000
+++ b/notas/application/services/commands/program_commands.py	2026-06-19 17:42:23.190427020 +0000
@@ -98,9 +98,9 @@
 
 
 @transaction.atomic
-def create_weekly_program(*, user, name: str, duration_weeks) -> ProgramCreateResult:
+def create_weekly_program(*, user, name: str, duration_weeks=None) -> ProgramCreateResult:
     clean_name = (name or "").strip()
-    weeks = normalize_duration_weeks(duration_weeks)
+    weeks = normalize_duration_weeks(duration_weeks or Program.DEFAULT_DURATION_WEEKS)
 
     if not clean_name:
         raise ValueError("program_name_required")
@@ -116,6 +116,17 @@
 
 
 @transaction.atomic
+def add_week_to_program(*, program: Program) -> Program:
+    program.duration_weeks = program.normalized_duration_weeks + 1
+
+    if program.is_draft and program.program_dailyplan.exists():
+        program.is_draft = False
+
+    program.save(update_fields=["duration_weeks", "is_draft"])
+    return program
+
+
+@transaction.atomic
 def assign_dailyplan_to_program_slot(
     *,
     program: Program,
--- a/notas/interface/views/programs.py	2026-06-19 17:27:37.000000000 +0000
+++ b/notas/interface/views/programs.py	2026-06-19 17:42:24.400988684 +0000
@@ -17,6 +17,7 @@
 
 from notas.application.services.access.capabilities import get_capabilities
 from notas.application.services.commands.program_commands import (
+    add_week_to_program,
     assign_dailyplan_to_program_slot,
     copy_program as copy_program_command,
     create_weekly_program,
@@ -916,19 +917,14 @@
 
     if request.method == "POST":
         name = request.POST.get("name")
-        duration_weeks = request.POST.get("duration_weeks")
 
         try:
             result = create_weekly_program(
                 user=request.user,
                 name=name,
-                duration_weeks=duration_weeks,
             )
-        except ValueError as exc:
-            if str(exc) == "program_name_required":
-                messages.error(request, "El nombre es obligatorio.")
-            else:
-                messages.error(request, "El número de semanas debe ser 1 o superior.")
+        except ValueError:
+            messages.error(request, "El nombre es obligatorio.")
             return redirect("program_create")
 
         return redirect("program_detail", pk=result.program.pk)
@@ -988,6 +984,7 @@
         "available_dailyplans": dailyplan_options,
         "available_dailyplans_json": json.dumps(dailyplan_options, cls=DjangoJSONEncoder),
         "day_labels": DAY_LABELS,
+        "can_edit": program.created_by_id == request.user.id,
     }
 
     context = _vm_context(
@@ -1073,6 +1070,15 @@
 
 
 @login_required
+@require_POST
+def program_add_week(request, pk):
+    program = get_object_or_404(Program, pk=pk, created_by=request.user)
+    add_week_to_program(program=program)
+    messages.success(request, f"Semana {program.normalized_duration_weeks} agregada al programa.")
+    return redirect(f"{reverse('program_detail', args=[program.pk])}#week-{program.normalized_duration_weeks}")
+
+
+@login_required
 def configure_program(request, pk):
     program = get_object_or_404(
         Program.objects.prefetch_related("program_dailyplan"),
--- a/notas/urls.py	2026-06-19 17:03:27.000000000 +0000
+++ b/notas/urls.py	2026-06-19 17:42:41.181001939 +0000
@@ -99,6 +99,7 @@
     program_list_bulk_delete,
     program_remove,
     program_share,
+    program_add_week,
     add_dailyplan_to_program,
     remove_dailyplan_from_program,
     configure_program,
@@ -362,6 +363,7 @@
     path("programs/<int:pk>/configure/", configure_program, name="configure_program"),
     path("programs/<int:pk>/share/", program_share, name="program_share"),
     path("programs/<int:pk>/remove/", program_remove, name="program_remove"),
+    path("programs/<int:pk>/add-week/", program_add_week, name="program_add_week"),
     path("programs/<int:pk>/add-dailyplan/", add_dailyplan_to_program, name="add_dailyplan_to_program"),
     path("programs/<int:pk>/days/<int:program_day_id>/remove/", remove_dailyplan_from_program, name="remove_dailyplan_from_program"),
     path("programs/<int:pk>/weeks/<int:week_number>/", program_week_detail, name="program_week_detail"),
--- a/notas/templates/notas/programs/create.html	2026-06-19 04:58:32.000000000 +0000
+++ b/notas/templates/notas/programs/create.html	2026-06-19 17:42:42.339957181 +0000
@@ -27,34 +27,13 @@
             class="input-create entity-form__input"
             type="text"
             name="name"
-            placeholder="Ej: Volumen controlado · 8 semanas"
+            placeholder="Ej: Volumen controlado"
             autocomplete="off"
             required
           >
         </div>
       </section>
 
-      <section class="entity-form__section entity-form__section--duration" aria-labelledby="program-create-duration-title">
-        <div class="entity-form__section-header">
-          <h3 id="program-create-duration-title">2. Define la duración</h3>
-        </div>
-
-        <div class="entity-form__field">
-          <label for="program-duration-weeks" class="entity-form__label entity-form__label--hidden">Número de semanas</label>
-          <input
-            id="program-duration-weeks"
-            class="input-create entity-form__input"
-            type="number"
-            name="duration_weeks"
-            min="1"
-            step="1"
-            value="4"
-            inputmode="numeric"
-            required
-          >
-          <p class="entity-form__help">1 semana equivale a 7 días. Luego podrás asignar planes diarios en la grilla semanal.</p>
-        </div>
-      </section>
 
       <div class="entity-form__actions">
         <a href="{% url 'program_list' %}" class="btn-cancel-create entity-form__cancel">Cancelar</a>
--- a/notas/templates/notas/programs/detail.html	2026-06-19 17:28:44.000000000 +0000
+++ b/notas/templates/notas/programs/detail.html	2026-06-19 17:42:42.342988591 +0000
@@ -36,8 +36,19 @@
   <h2>Gráfico diario por Indicador</h2>
   {% include "components/program_metric_chart.html" with chart=vm.content.program_chart modifier="program-chart-panel--detail" %}
   
-  <h2>Semanas en este Programa</h2>
-  {% include "components/program_week_panels.html" with panel_id=vm.content.program.id weeks=vm.content.weeks %}
+  <div class="title-section-panels program-weeks-title-section">
+    <h2>Semanas en este Programa</h2>
+    {% if vm.content.can_edit %}
+      <form method="post" action="{% url 'program_add_week' vm.content.program.id %}" class="program-add-week-form">
+        {% csrf_token %}
+        <button type="submit" class="add-title program-add-week-button">
+          <i data-lucide="plus" class="table-icon"></i>
+          <h4>Agregar Semana</h4>
+        </button>
+      </form>
+    {% endif %}
+  </div>
+  {% include "components/program_week_panels.html" with panel_id=vm.content.program.id weeks=vm.content.weeks default_desktop=True %}
 
   <hr>
   <section class="program-board-shell">
--- a/notas/templates/components/program_week_panels.html	2026-06-19 17:27:37.000000000 +0000
+++ b/notas/templates/components/program_week_panels.html	2026-06-19 17:42:42.344140185 +0000
@@ -2,6 +2,7 @@
 <div
   class="card-detail-block program-card-panels"
   data-default-mobile="#card-grid-program-macros-mobile-{{ panel_id }}"
+  {% if default_desktop %}data-default-desktop="#card-grid-program-weeks-desktop-{{ panel_id }}"{% endif %}
 >
   <div class="card-detail-tabs card-detail-tabs--desktop block-desplegar-tabla block-card-desplegar program-card-panels__desktop-tabs">
     <div
--- a/notas/static/notas/css/components/programs.css	2026-06-19 17:27:37.000000000 +0000
+++ b/notas/static/notas/css/components/programs.css	2026-06-19 17:43:06.618036833 +0000
@@ -1940,3 +1940,48 @@
 .program-slot-picker.section_picker[hidden] {
   display: none !important;
 }
+
+/* Program weeks incremental controls ============================== */
+.program-weeks-title-section {
+  margin: 1.35rem 0 0.75rem;
+}
+
+.program-weeks-title-section h2 {
+  margin: 0;
+  color: var(--text-main);
+  white-space: nowrap;
+}
+
+.program-add-week-form {
+  display: flex;
+  align-items: center;
+  margin: 0;
+}
+
+.program-add-week-button.add-title {
+  font: inherit;
+}
+
+.program-add-week-button .table-icon {
+  width: 16px;
+  height: 16px;
+  stroke-width: 2.4;
+}
+
+@media (max-width: 780px) {
+  .program-weeks-title-section {
+    align-items: flex-start;
+    gap: 10px;
+    flex-direction: column;
+  }
+
+  .program-add-week-form,
+  .program-add-week-button.add-title {
+    width: 100%;
+  }
+
+  .program-add-week-button.add-title {
+    justify-content: center;
+    margin-left: 0;
+  }
+}
