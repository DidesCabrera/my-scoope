diff -ruN a/notas/interface/views/comparators.py b/notas/interface/views/comparators.py
--- a/notas/interface/views/comparators.py	2026-06-23 18:40:53.000000000 +0000
+++ b/notas/interface/views/comparators.py	2026-06-23 18:58:28.021466189 +0000
@@ -5,6 +5,7 @@
 from django.shortcuts import redirect, render
 from django.urls import reverse
 
+from notas.application.services.nutrition.weight import get_current_weight
 from notas.domain.models import DailyPlan, Food, Meal
 from notas.presentation.composition.viewmodel.ui_builder import build_ui_vm
 from notas.presentation.config.viewmodel_config import (
@@ -55,6 +56,7 @@
 
 @dataclass
 class ComparatorContentVM:
+    entity_key: str
     entity_label: str
     entity_plural_label: str
     entity_icon: str
@@ -103,7 +105,7 @@
 
 
 def _format_metric_value(value: float, unit: str) -> str:
-    decimals = 1 if unit in {"g", "%"} else 0
+    decimals = 2 if unit == "g/kg" else 1 if unit in {"g", "%"} else 0
     return f"{_format_number(value, decimals)} {unit}"
 
 
@@ -167,12 +169,15 @@
     }
 
 
-def _entity_values(entity) -> dict[str, float]:
+def _entity_values(entity, current_weight: float | None = None) -> dict[str, float]:
     alloc = entity.alloc
+    protein = entity.protein
+    ppk = (protein / current_weight) if current_weight and protein else 0
 
     return {
         "total_kcal": entity.total_kcal,
-        "protein": entity.protein,
+        "ppk": ppk,
+        "protein": protein,
         "carbs": entity.carbs,
         "fat": entity.fat,
         "alloc_protein": alloc.get("protein", 0),
@@ -181,16 +186,25 @@
     }
 
 
-def _build_metrics(values_a: dict[str, float], values_b: dict[str, float]) -> list[ComparatorMetric]:
-    metric_specs = [
-        ("total_kcal", "Calorías", "kcal"),
+def _build_metrics(
+    values_a: dict[str, float],
+    values_b: dict[str, float],
+    *,
+    include_ppk: bool = False,
+) -> list[ComparatorMetric]:
+    metric_specs = [("total_kcal", "Calorías", "kcal")]
+
+    if include_ppk:
+        metric_specs.append(("ppk", "PPK", "g/kg"))
+
+    metric_specs.extend([
         ("protein", "Proteínas", "g"),
         ("carbs", "Carbohidratos", "g"),
         ("fat", "Grasas", "g"),
         ("alloc_protein", "P%", "%"),
         ("alloc_carbs", "C%", "%"),
         ("alloc_fat", "F%", "%"),
-    ]
+    ])
 
     return [
         _build_metric(
@@ -269,6 +283,7 @@
         )
 
     content_vm = ComparatorContentVM(
+        entity_key="food",
         entity_label="alimento",
         entity_plural_label="alimentos",
         entity_icon="carrot",
@@ -319,11 +334,18 @@
     selected_a = _find_selected(meals, selected_a_id)
     selected_b = _find_selected(meals, selected_b_id)
 
+    current_weight = get_current_weight(request.user)
+
     metrics = []
     if selected_a and selected_b:
-        metrics = _build_metrics(_entity_values(selected_a), _entity_values(selected_b))
+        metrics = _build_metrics(
+            _entity_values(selected_a, current_weight),
+            _entity_values(selected_b, current_weight),
+            include_ppk=True,
+        )
 
     content_vm = ComparatorContentVM(
+        entity_key="meal",
         entity_label="comida",
         entity_plural_label="comidas",
         entity_icon="utensils",
@@ -370,11 +392,18 @@
     selected_a = _find_selected(dailyplans, selected_a_id)
     selected_b = _find_selected(dailyplans, selected_b_id)
 
+    current_weight = get_current_weight(request.user)
+
     metrics = []
     if selected_a and selected_b:
-        metrics = _build_metrics(_entity_values(selected_a), _entity_values(selected_b))
+        metrics = _build_metrics(
+            _entity_values(selected_a, current_weight),
+            _entity_values(selected_b, current_weight),
+            include_ppk=True,
+        )
 
     content_vm = ComparatorContentVM(
+        entity_key="dailyplan",
         entity_label="plan diario",
         entity_plural_label="planes diarios",
         entity_icon="clipboard-list",
diff -ruN a/notas/static/notas/css/components/comparators.css b/notas/static/notas/css/components/comparators.css
--- a/notas/static/notas/css/components/comparators.css	2026-06-23 18:40:53.000000000 +0000
+++ b/notas/static/notas/css/components/comparators.css	2026-06-23 18:59:10.378395147 +0000
@@ -46,6 +46,9 @@
 }
 
 .comparator-form {
+  display: flex;
+  flex-direction: column;
+  gap: 14px;
   margin: 0 0 18px 0;
   padding: 14px;
 }
@@ -125,13 +128,20 @@
 .comparator-field select,
 .comparator-field input {
   width: 100%;
-  min-height: 38px;
+  min-height: 40px;
   border-radius: 10px;
-  padding: 8px 10px;
+  padding: 9px 10px;
   background: var(--input-bg);
   color: var(--input-text);
   border: 1px solid var(--input-border);
-  font-size: 14px;
+  font-size: 15px;
+  line-height: 1.25;
+}
+
+.comparator-field select option {
+  color: var(--input-text);
+  background: var(--input-bg);
+  font-size: 15px;
 }
 
 .comparator-field select:focus,
@@ -166,7 +176,55 @@
 .comparator-form__actions {
   display: flex;
   justify-content: flex-end;
-  margin-top: 14px;
+}
+
+.comparator-submit {
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+  gap: 8px;
+  min-height: 40px;
+  padding: 0 17px;
+  border: 0;
+  border-radius: 999px;
+  background: var(--entity-comparator, var(--text-main));
+  color: #ffffff;
+  box-shadow: 0 10px 24px color-mix(in srgb, var(--entity-comparator, #111111) 24%, transparent);
+  cursor: pointer;
+  font-size: 13px;
+  font-weight: 800;
+  letter-spacing: 0.01em;
+  transition: transform 0.16s ease, filter 0.16s ease, box-shadow 0.16s ease;
+}
+
+.comparator-submit:hover {
+  filter: brightness(1.05);
+  transform: translateY(-1px);
+}
+
+.comparator-submit:active {
+  transform: translateY(0);
+}
+
+.comparator-submit__icon {
+  width: 16px;
+  height: 16px;
+  stroke-width: 2;
+}
+
+.comparator-page--food .comparator-submit {
+  background: var(--entity-food);
+  box-shadow: 0 10px 24px color-mix(in srgb, var(--entity-food) 28%, transparent);
+}
+
+.comparator-page--meal .comparator-submit {
+  background: var(--entity-meal);
+  box-shadow: 0 10px 24px color-mix(in srgb, var(--entity-meal) 28%, transparent);
+}
+
+.comparator-page--dailyplan .comparator-submit {
+  background: var(--entity-dailyplan);
+  box-shadow: 0 10px 24px color-mix(in srgb, var(--entity-dailyplan) 28%, transparent);
 }
 
 .comparator-results {
@@ -229,6 +287,35 @@
   min-width: 0;
 }
 
+.comparator-bar-row__label {
+  display: inline-flex;
+  align-items: center;
+  gap: 7px;
+  min-width: 0;
+}
+
+.comparator-bar-row__icon {
+  width: 18px;
+  height: 18px;
+  flex: 0 0 auto;
+  padding: 3px;
+  border-radius: 5px;
+  color: #ffffff;
+  stroke-width: 2;
+}
+
+.comparator-bar-row__icon--food {
+  background: var(--entity-food);
+}
+
+.comparator-bar-row__icon--meal {
+  background: var(--entity-meal);
+}
+
+.comparator-bar-row__icon--dailyplan {
+  background: var(--entity-dailyplan);
+}
+
 .comparator-bar-row__meta strong {
   min-width: 0;
   overflow: hidden;
@@ -263,7 +350,30 @@
 }
 
 .comparator-bar--b {
-  opacity: 0.55;
+  opacity: 1;
+}
+
+.comparator-metric--total_kcal .comparator-bar {
+  background: var(--kpi-total-border);
+}
+
+.comparator-metric--ppk .comparator-bar {
+  background: var(--nutrition-ppk);
+}
+
+.comparator-metric--protein .comparator-bar,
+.comparator-metric--alloc_protein .comparator-bar {
+  background: var(--nutrition-protein);
+}
+
+.comparator-metric--carbs .comparator-bar,
+.comparator-metric--alloc_carbs .comparator-bar {
+  background: var(--nutrition-carbs);
+}
+
+.comparator-metric--fat .comparator-bar,
+.comparator-metric--alloc_fat .comparator-bar {
+  background: var(--nutrition-fat);
 }
 
 .comparator-empty {
@@ -272,7 +382,7 @@
   justify-content: center;
   gap: 10px;
   margin: 0;
-  padding: 22px;
+  padding: 18px;
   color: var(--text-muted);
   text-align: center;
 }
@@ -295,6 +405,36 @@
   background: var(--entity-comparator, var(--entity-home));
 }
 
+.comparator-page--food .list-page-header__icon--comparator,
+.comparator-page--food .label-entity--comparator {
+  background: var(--entity-food);
+}
+
+.comparator-page--meal .list-page-header__icon--comparator,
+.comparator-page--meal .label-entity--comparator {
+  background: var(--entity-meal);
+}
+
+.comparator-page--dailyplan .list-page-header__icon--comparator,
+.comparator-page--dailyplan .label-entity--comparator {
+  background: var(--entity-dailyplan);
+}
+
+.comparator-page--food .comparator-tab.is-active {
+  background: var(--entity-food);
+  border-color: var(--entity-food);
+}
+
+.comparator-page--meal .comparator-tab.is-active {
+  background: var(--entity-meal);
+  border-color: var(--entity-meal);
+}
+
+.comparator-page--dailyplan .comparator-tab.is-active {
+  background: var(--entity-dailyplan);
+  border-color: var(--entity-dailyplan);
+}
+
 .sidebar-main-icon--comparator {
   color: var(--entity-comparator, var(--text-main));
 }
@@ -322,6 +462,52 @@
     margin-left: 0;
     margin-right: 0;
     padding: 12px;
+    font-size: 14px;
+  }
+
+  .comparator-selector {
+    padding: 12px;
+  }
+
+  .comparator-selector__heading p,
+  .comparator-field,
+  .comparator-field > span,
+  .comparator-field--quantity em,
+  .comparator-submit {
+    font-size: 14px;
+  }
+
+  .comparator-selector__badge {
+    width: 30px;
+    height: 30px;
+    font-size: 14px;
+  }
+
+  .comparator-selector__heading strong {
+    font-size: 15px;
+  }
+
+  .comparator-field select,
+  .comparator-field input {
+    min-height: 48px;
+    padding: 12px 11px;
+    font-size: 16px;
+    line-height: 1.35;
+    -webkit-text-size-adjust: 100%;
+    text-size-adjust: 100%;
+  }
+
+  .comparator-field select option {
+    font-size: 16px;
+  }
+
+  .comparator-form__actions {
+    justify-content: stretch;
+  }
+
+  .comparator-submit {
+    width: 100%;
+    min-height: 46px;
   }
 
   .comparator-selectors {
diff -ruN a/notas/templates/notas/comparators/detail.html b/notas/templates/notas/comparators/detail.html
--- a/notas/templates/notas/comparators/detail.html	2026-06-23 18:40:53.000000000 +0000
+++ b/notas/templates/notas/comparators/detail.html	2026-06-23 18:58:28.022339001 +0000
@@ -3,7 +3,7 @@
 {% block title %}Comparadores{% endblock %}
 {% block content %}
 
-<div class="page list comparator-page">
+<div class="page list comparator-page comparator-page--{{ vm.content.entity_key }}">
   {% include "components/list_page_header.html" %}
 
   <nav class="comparator-tabs" aria-label="Tipo de comparador">
@@ -74,8 +74,18 @@
       </section>
     </div>
 
+    {% if not vm.content.is_ready %}
+      <section class="comparator-empty card-detail-block">
+        <i data-lucide="columns-3" class="comparator-empty__icon"></i>
+        <p>{{ vm.content.empty_message }}</p>
+      </section>
+    {% endif %}
+
     <div class="comparator-form__actions">
-      <button type="submit" class="btn-primary">Comparar</button>
+      <button type="submit" class="comparator-submit">
+        <i data-lucide="columns-3" class="comparator-submit__icon" aria-hidden="true"></i>
+        <span>Comparar</span>
+      </button>
     </div>
   </form>
 
@@ -91,7 +101,10 @@
           <div class="comparator-bars">
             <div class="comparator-bar-row comparator-bar-row--a">
               <div class="comparator-bar-row__meta">
-                <strong>{{ vm.content.selection_a.name }}</strong>
+                <span class="comparator-bar-row__label">
+                  <i data-lucide="{{ vm.content.entity_icon }}" class="comparator-bar-row__icon comparator-bar-row__icon--{{ vm.content.entity_key }}" aria-hidden="true"></i>
+                  <strong>{{ vm.content.selection_a.name }}</strong>
+                </span>
                 <span>{{ metric.a.formatted_value }}</span>
               </div>
               <div class="comparator-bar-track">
@@ -101,7 +114,10 @@
 
             <div class="comparator-bar-row comparator-bar-row--b">
               <div class="comparator-bar-row__meta">
-                <strong>{{ vm.content.selection_b.name }}</strong>
+                <span class="comparator-bar-row__label">
+                  <i data-lucide="{{ vm.content.entity_icon }}" class="comparator-bar-row__icon comparator-bar-row__icon--{{ vm.content.entity_key }}" aria-hidden="true"></i>
+                  <strong>{{ vm.content.selection_b.name }}</strong>
+                </span>
                 <span>{{ metric.b.formatted_value }}</span>
               </div>
               <div class="comparator-bar-track">
@@ -112,11 +128,6 @@
         </article>
       {% endfor %}
     </section>
-  {% else %}
-    <section class="comparator-empty card-detail-block">
-      <i data-lucide="columns-3" class="comparator-empty__icon"></i>
-      <p>{{ vm.content.empty_message }}</p>
-    </section>
   {% endif %}
 </div>
 
