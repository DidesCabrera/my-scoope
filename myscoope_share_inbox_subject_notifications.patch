diff -ruN -x __pycache__ -x '*.pyc' a/notas/application/resolvers/dailyplan_resolvers.py b/notas/application/resolvers/dailyplan_resolvers.py
--- a/notas/application/resolvers/dailyplan_resolvers.py	2026-06-17 17:15:40.000000000 +0000
+++ b/notas/application/resolvers/dailyplan_resolvers.py	2026-06-17 19:10:52.129450419 +0000
@@ -307,7 +307,6 @@
     DAILYPLAN_VIEWMODE_PERSONAL_LIST: [
         "list_fork",
         "list_remove",
-        "share",
         "detail",
     ],
 
diff -ruN -x __pycache__ -x '*.pyc' a/notas/application/resolvers/meal_resolvers.py b/notas/application/resolvers/meal_resolvers.py
--- a/notas/application/resolvers/meal_resolvers.py	2026-06-17 17:15:40.000000000 +0000
+++ b/notas/application/resolvers/meal_resolvers.py	2026-06-17 19:10:52.130198956 +0000
@@ -270,7 +270,6 @@
     MEAL_VIEWMODE_PERSONAL_LIST: [
         "list_fork",
         "list_remove",
-        "share",
         "detail",
     ],
 
diff -ruN -x __pycache__ -x '*.pyc' a/notas/application/services/commands/share_commands.py b/notas/application/services/commands/share_commands.py
--- a/notas/application/services/commands/share_commands.py	2026-06-17 18:19:20.000000000 +0000
+++ b/notas/application/services/commands/share_commands.py	2026-06-17 19:10:08.692276155 +0000
@@ -77,16 +77,23 @@
     return (message or "").strip()
 
 
+def _clean_share_subject(subject: str | None, fallback: str) -> str:
+    clean_subject = (subject or "").strip()
+    return clean_subject or fallback
+
+
 @transaction.atomic
 def create_dailyplan_share(
     *,
     sender,
     dailyplan: DailyPlan,
     recipient_email: str,
+    subject: str | None = None,
     message: str | None = None,
 ) -> DailyPlanShareCreateResult:
     clean_email = (recipient_email or "").strip().lower()
     clean_message = _clean_share_message(message)
+    clean_subject = _clean_share_subject(subject, dailyplan.name)
 
     if not clean_email:
         raise ValueError("recipient_email_required")
@@ -95,6 +102,7 @@
     defaults = {
         **delivery_defaults,
         "message": clean_message,
+        "subject": clean_subject,
         "is_read": False,
     }
 
@@ -116,6 +124,10 @@
         share.message = clean_message
         update_fields.append("message")
 
+    if share.subject != clean_subject:
+        share.subject = clean_subject
+        update_fields.append("subject")
+
     if share.is_read:
         share.is_read = False
         update_fields.append("is_read")
@@ -193,10 +205,12 @@
     sender,
     meal: Meal,
     recipient_email: str,
+    subject: str | None = None,
     message: str | None = None,
 ) -> MealShareCreateResult:
     clean_email = (recipient_email or "").strip().lower()
     clean_message = _clean_share_message(message)
+    clean_subject = _clean_share_subject(subject, meal.name)
 
     if not clean_email:
         raise ValueError("recipient_email_required")
@@ -205,6 +219,7 @@
     defaults = {
         **delivery_defaults,
         "message": clean_message,
+        "subject": clean_subject,
         "is_read": False,
     }
 
@@ -226,6 +241,10 @@
         share.message = clean_message
         update_fields.append("message")
 
+    if share.subject != clean_subject:
+        share.subject = clean_subject
+        update_fields.append("subject")
+
     if share.is_read:
         share.is_read = False
         update_fields.append("is_read")
diff -ruN -x __pycache__ -x '*.pyc' a/notas/application/services/notifications/share_emails.py b/notas/application/services/notifications/share_emails.py
--- a/notas/application/services/notifications/share_emails.py	2026-06-17 18:58:10.000000000 +0000
+++ b/notas/application/services/notifications/share_emails.py	2026-06-17 19:10:29.171903395 +0000
@@ -15,6 +15,7 @@
     share,
     kind: str,
     item_name: str,
+    custom_subject: str | None = None,
     custom_message: str | None = None,
 ):
     sender_name = share.sender.username
@@ -25,6 +26,7 @@
         else "meal_share_accept"
     )
 
+    clean_subject = (custom_subject or getattr(share, "subject", "") or item_name).strip()
     clean_message = (custom_message or getattr(share, "message", "") or "").strip()
 
     accept_path = reverse(accept_url_name, args=[share.token])
@@ -36,7 +38,7 @@
         reverse("account_login") + "?" + urlencode({"next": accept_path})
     )
 
-    subject = f"{sender_name} compartió un {kind_label} contigo en My Scoope"
+    subject = clean_subject
 
     message_lines = [
         "Hola,",
@@ -44,6 +46,9 @@
         f"{sender_name} compartió este {kind_label} contigo en My Scoope:",
         item_name,
         "",
+        "Asunto:",
+        clean_subject,
+        "",
     ]
 
     if clean_message:
diff -ruN -x __pycache__ -x '*.pyc' a/notas/application/use_cases/inbox_pages.py b/notas/application/use_cases/inbox_pages.py
--- a/notas/application/use_cases/inbox_pages.py	2026-06-17 18:58:10.000000000 +0000
+++ b/notas/application/use_cases/inbox_pages.py	2026-06-17 19:10:40.909987915 +0000
@@ -107,7 +107,7 @@
         {
             "key": "save_attachment",
             "label": "Guardar en Mi librería",
-            "icon": "save",
+            "icon": "bookmark",
             "url": save_url,
             "method": "post",
             "desktop_position": "inline",
@@ -195,7 +195,7 @@
         created_at=share.created_at,
         received_at_label=_format_received_at(share.created_at),
         sender=share.sender.username,
-        title=share.dailyplan.name,
+        title=(share.subject or share.dailyplan.name),
         summary=summary,
         message=summary,
         is_favorite=share.is_favorite,
@@ -244,7 +244,7 @@
         created_at=share.created_at,
         received_at_label=_format_received_at(share.created_at),
         sender=share.sender.username,
-        title=share.meal.name,
+        title=(share.subject or share.meal.name),
         summary=summary,
         message=summary,
         is_favorite=share.is_favorite,
diff -ruN -x __pycache__ -x '*.pyc' a/notas/domain/models.py b/notas/domain/models.py
--- a/notas/domain/models.py	2026-06-17 18:19:20.000000000 +0000
+++ b/notas/domain/models.py	2026-06-17 19:10:08.690932225 +0000
@@ -1499,6 +1499,7 @@
     is_favorite = models.BooleanField(default=False)    # inbox
     is_read = models.BooleanField(default=False)        # inbox
     message = models.TextField(blank=True)              # inbox / email
+    subject = models.CharField(max_length=160, blank=True)  # inbox title / email subject
 
     class Meta:
         unique_together = ("recipient_email", "dailyplan")
@@ -1539,6 +1540,7 @@
     is_favorite = models.BooleanField(default=False)    # inbox
     is_read = models.BooleanField(default=False)        # inbox
     message = models.TextField(blank=True)              # inbox / email
+    subject = models.CharField(max_length=160, blank=True)  # inbox title / email subject
 
     class Meta:
         unique_together = ("recipient_email", "meal")
diff -ruN -x __pycache__ -x '*.pyc' a/notas/interface/forms/forms.py b/notas/interface/forms/forms.py
--- a/notas/interface/forms/forms.py	2026-06-17 18:19:20.000000000 +0000
+++ b/notas/interface/forms/forms.py	2026-06-17 19:11:33.067570798 +0000
@@ -9,6 +9,15 @@
             "autocomplete": "email",
         }),
     )
+    subject = forms.CharField(
+        label="Asunto",
+        max_length=160,
+        widget=forms.TextInput(attrs={
+            "class": "input-create entity-form__input",
+            "placeholder": "Asunto del mensaje",
+            "autocomplete": "off",
+        }),
+    )
     message = forms.CharField(
         label="Mensaje",
         required=False,
@@ -29,6 +38,15 @@
             "autocomplete": "email",
         }),
     )
+    subject = forms.CharField(
+        label="Asunto",
+        max_length=160,
+        widget=forms.TextInput(attrs={
+            "class": "input-create entity-form__input",
+            "placeholder": "Asunto del mensaje",
+            "autocomplete": "off",
+        }),
+    )
     message = forms.CharField(
         label="Mensaje",
         required=False,
diff -ruN -x __pycache__ -x '*.pyc' a/notas/interface/views/dailyplans.py b/notas/interface/views/dailyplans.py
--- a/notas/interface/views/dailyplans.py	2026-06-17 18:19:20.000000000 +0000
+++ b/notas/interface/views/dailyplans.py	2026-06-17 19:10:29.171309074 +0000
@@ -84,16 +84,18 @@
     if dailyplan.created_by != request.user:
         return HttpResponseForbidden()
 
-    form = DailyPlanShareForm(request.POST or None)
+    form = DailyPlanShareForm(request.POST or None, initial={"subject": dailyplan.name})
 
     if request.method == "POST" and form.is_valid():
         email = form.cleaned_data["recipient_email"]
+        share_subject = form.cleaned_data.get("subject", dailyplan.name)
         message = form.cleaned_data.get("message", "")
 
         result = create_dailyplan_share(
             sender=request.user,
             recipient_email=email,
             dailyplan=dailyplan,
+            subject=share_subject,
             message=message,
         )
 
@@ -104,6 +106,7 @@
             share=share,
             kind="dailyplan",
             item_name=dailyplan.name,
+            custom_subject=share_subject,
             custom_message=message,
         )
 
diff -ruN -x __pycache__ -x '*.pyc' a/notas/interface/views/meals.py b/notas/interface/views/meals.py
--- a/notas/interface/views/meals.py	2026-06-17 18:19:20.000000000 +0000
+++ b/notas/interface/views/meals.py	2026-06-17 19:10:29.170222657 +0000
@@ -88,16 +88,18 @@
     if meal.created_by != request.user:
         return HttpResponseForbidden()
 
-    form = MealShareForm(request.POST or None)
+    form = MealShareForm(request.POST or None, initial={"subject": meal.name})
 
     if request.method == "POST" and form.is_valid():
         email = form.cleaned_data["recipient_email"]
+        share_subject = form.cleaned_data.get("subject", meal.name)
         message = form.cleaned_data.get("message", "")
 
         result = create_meal_share(
             sender=request.user,
             recipient_email=email,
             meal=meal,
+            subject=share_subject,
             message=message,
         )
 
@@ -108,6 +110,7 @@
             share=share,
             kind="meal",
             item_name=meal.name,
+            custom_subject=share_subject,
             custom_message=message,
         )
 
diff -ruN -x __pycache__ -x '*.pyc' a/notas/migrations/0030_share_subject.py b/notas/migrations/0030_share_subject.py
--- a/notas/migrations/0030_share_subject.py	1970-01-01 00:00:00.000000000 +0000
+++ b/notas/migrations/0030_share_subject.py	2026-06-17 19:11:13.835483057 +0000
@@ -0,0 +1,39 @@
+# Generated manually for My Scoope share inbox subject.
+
+from django.db import migrations, models
+
+
+def backfill_share_subjects(apps, schema_editor):
+    DailyPlanShare = apps.get_model("notas", "DailyPlanShare")
+    MealShare = apps.get_model("notas", "MealShare")
+
+    for share in DailyPlanShare.objects.select_related("dailyplan").filter(subject=""):
+        if share.dailyplan_id and share.dailyplan:
+            share.subject = share.dailyplan.name
+            share.save(update_fields=["subject"])
+
+    for share in MealShare.objects.select_related("meal").filter(subject=""):
+        if share.meal_id and share.meal:
+            share.subject = share.meal.name
+            share.save(update_fields=["subject"])
+
+
+class Migration(migrations.Migration):
+
+    dependencies = [
+        ("notas", "0029_share_messages_read_state"),
+    ]
+
+    operations = [
+        migrations.AddField(
+            model_name="dailyplanshare",
+            name="subject",
+            field=models.CharField(blank=True, max_length=160),
+        ),
+        migrations.AddField(
+            model_name="mealshare",
+            name="subject",
+            field=models.CharField(blank=True, max_length=160),
+        ),
+        migrations.RunPython(backfill_share_subjects, migrations.RunPython.noop),
+    ]
diff -ruN -x __pycache__ -x '*.pyc' a/notas/static/notas/css/components/sidebar.css b/notas/static/notas/css/components/sidebar.css
--- a/notas/static/notas/css/components/sidebar.css	2026-06-17 18:32:09.000000000 +0000
+++ b/notas/static/notas/css/components/sidebar.css	2026-06-17 19:12:40.850761691 +0000
@@ -545,19 +545,36 @@
     position: relative;
 }
 
+.sidebar-main .sidebar-left {
+    flex: 1 1 auto;
+    min-width: 0;
+}
+
 .sidebar-notification-dot {
-    position: absolute;
-    top: 8px;
-    right: 8px;
-    width: 7px;
-    height: 7px;
-    flex: 0 0 7px;
+    position: static;
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    min-width: 16px;
+    height: 16px;
+    padding: 0 5px;
+    margin-left: auto;
     border-radius: 999px;
     background: var(--color-inbox, #ff4343);
+    color: #ffffff;
+    font-size: 10px;
+    font-weight: 700;
+    line-height: 1;
     pointer-events: none;
 }
 
 :where(body.sidebar-collapsed, html.sidebar-collapsed body) .sidebar-notification-dot {
+    position: absolute;
     top: 6px;
-    right: 10px;
+    right: 8px;
+    min-width: 14px;
+    height: 14px;
+    padding: 0 4px;
+    margin-left: 0;
+    font-size: 9px;
 }
diff -ruN -x __pycache__ -x '*.pyc' a/notas/templates/components/sidebar.html b/notas/templates/components/sidebar.html
--- a/notas/templates/components/sidebar.html	2026-06-17 18:19:20.000000000 +0000
+++ b/notas/templates/components/sidebar.html	2026-06-17 19:11:05.044997187 +0000
@@ -51,10 +51,10 @@
                                                 <i data-lucide="{{ group.icon }}" class="sidebar-main-icon sidebar-main-icon--{{ group.nav_root }}{% if section.key == 'workspace' %} sidebar-main-icon--library{% endif %}"></i>
                                                 <span class="sidebar-label">{{ group.label }}</span>
                                                 {% if group.key == "proposal" and proposal_unread_count %}
-                                                    <span class="sidebar-notification-dot" aria-label="{{ proposal_unread_count }} propuesta(s) sin leer"></span>
+                                                    <span class="sidebar-notification-dot" aria-label="{{ proposal_unread_count }} propuesta(s) sin leer">{{ proposal_unread_count }}</span>
                                                 {% endif %}
                                                 {% if group.key == "inbox" and inbox_unread_count %}
-                                                    <span class="sidebar-notification-dot" aria-label="{{ inbox_unread_count }} inbox sin leer"></span>
+                                                    <span class="sidebar-notification-dot" aria-label="{{ inbox_unread_count }} inbox sin leer">{{ inbox_unread_count }}</span>
                                                 {% endif %}
                                             </span>
                                         </a>
@@ -73,10 +73,10 @@
                                             <i data-lucide="{{ group.icon }}" class="sidebar-main-icon sidebar-main-icon--{{ group.nav_root }}{% if section.key == 'workspace' %} sidebar-main-icon--library{% endif %}"></i>
                                             <span class="sidebar-label">{{ group.label }}</span>
                                             {% if group.key == "proposal" and proposal_unread_count %}
-                                                <span class="sidebar-notification-dot" aria-label="{{ proposal_unread_count }} propuesta(s) sin leer"></span>
+                                                <span class="sidebar-notification-dot" aria-label="{{ proposal_unread_count }} propuesta(s) sin leer">{{ proposal_unread_count }}</span>
                                             {% endif %}
                                             {% if group.key == "inbox" and inbox_unread_count %}
-                                                <span class="sidebar-notification-dot" aria-label="{{ inbox_unread_count }} inbox sin leer"></span>
+                                                <span class="sidebar-notification-dot" aria-label="{{ inbox_unread_count }} inbox sin leer">{{ inbox_unread_count }}</span>
                                             {% endif %}
                                         </span>
                                     </a>
@@ -88,10 +88,10 @@
                                             <i data-lucide="{{ group.icon }}" class="sidebar-main-icon sidebar-main-icon--{{ group.nav_root|default:group.key }}{% if section.key == 'workspace' %} sidebar-main-icon--library{% endif %}"></i>
                                             <span class="sidebar-label">{{ group.label }}</span>
                                             {% if group.key == "proposal" and proposal_unread_count %}
-                                                <span class="sidebar-notification-dot" aria-label="{{ proposal_unread_count }} propuesta(s) sin leer"></span>
+                                                <span class="sidebar-notification-dot" aria-label="{{ proposal_unread_count }} propuesta(s) sin leer">{{ proposal_unread_count }}</span>
                                             {% endif %}
                                             {% if group.key == "inbox" and inbox_unread_count %}
-                                                <span class="sidebar-notification-dot" aria-label="{{ inbox_unread_count }} inbox sin leer"></span>
+                                                <span class="sidebar-notification-dot" aria-label="{{ inbox_unread_count }} inbox sin leer">{{ inbox_unread_count }}</span>
                                             {% endif %}
                                         </span>
                                     </button>
diff -ruN -x __pycache__ -x '*.pyc' a/notas/templates/notas/dailyplans/share.html b/notas/templates/notas/dailyplans/share.html
--- a/notas/templates/notas/dailyplans/share.html	2026-06-17 18:19:20.000000000 +0000
+++ b/notas/templates/notas/dailyplans/share.html	2026-06-17 19:10:42.947922470 +0000
@@ -28,6 +28,20 @@
         </div>
       </section>
 
+      <section class="entity-form__section share-form__section" aria-labelledby="dailyplan-share-subject-title">
+        <div class="entity-form__section-header">
+          <h3 id="dailyplan-share-subject-title">Asunto</h3>
+        </div>
+
+        <div class="entity-form__field">
+          <label for="{{ form.subject.id_for_label }}" class="entity-form__label entity-form__label--hidden">Asunto</label>
+          {{ form.subject }}
+          {% if form.subject.errors %}
+            <p class="entity-form__error">{{ form.subject.errors|striptags }}</p>
+          {% endif %}
+        </div>
+      </section>
+
       <section class="entity-form__section share-form__section" aria-labelledby="dailyplan-share-message-title">
         <div class="entity-form__section-header">
           <h3 id="dailyplan-share-message-title">Mensaje</h3>
diff -ruN -x __pycache__ -x '*.pyc' a/notas/templates/notas/meals/share.html b/notas/templates/notas/meals/share.html
--- a/notas/templates/notas/meals/share.html	2026-06-17 18:19:20.000000000 +0000
+++ b/notas/templates/notas/meals/share.html	2026-06-17 19:10:42.947359074 +0000
@@ -28,6 +28,20 @@
         </div>
       </section>
 
+      <section class="entity-form__section share-form__section" aria-labelledby="meal-share-subject-title">
+        <div class="entity-form__section-header">
+          <h3 id="meal-share-subject-title">Asunto</h3>
+        </div>
+
+        <div class="entity-form__field">
+          <label for="{{ form.subject.id_for_label }}" class="entity-form__label entity-form__label--hidden">Asunto</label>
+          {{ form.subject }}
+          {% if form.subject.errors %}
+            <p class="entity-form__error">{{ form.subject.errors|striptags }}</p>
+          {% endif %}
+        </div>
+      </section>
+
       <section class="entity-form__section share-form__section" aria-labelledby="meal-share-message-title">
         <div class="entity-form__section-header">
           <h3 id="meal-share-message-title">Mensaje</h3>
