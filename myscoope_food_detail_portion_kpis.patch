diff -ruN a/notas/presentation/composition/viewmodel/food/detail_food_builder.py b/notas/presentation/composition/viewmodel/food/detail_food_builder.py
--- a/notas/presentation/composition/viewmodel/food/detail_food_builder.py	2026-06-11 21:50:00.000000000 +0000
+++ b/notas/presentation/composition/viewmodel/food/detail_food_builder.py	2026-06-11 21:58:47.077726329 +0000
@@ -5,6 +5,7 @@
 from notas.presentation.resolvers.title_resolvers import resolve_category_badge
 from notas.application.services.food_imports.localized_names import resolve_food_display_name
 from notas.presentation.composition.viewmodel.food.list_foods_builder import _build_food_badges
+from notas.application.services.nutrition.weight import get_current_weight
 
 
 def build_food_detail_vm(food, user, viewmode):
@@ -41,6 +42,13 @@
         "fat": food.alloc["fat"],
     }
 
+    current_weight = get_current_weight(user)
+    food_ppk = (
+        float(food_protein) / float(current_weight)
+        if current_weight and food_protein
+        else 0
+    )
+
     # ==================================================
     # MAIN CARD
     # ==================================================
@@ -55,10 +63,12 @@
             icon=main_entity_icon,
             category=getattr(food, "category", None),
             category_badge=resolve_category_badge(getattr(food, "category", None)),
-            badges=_build_food_badges(food, user),
+            badges=_build_food_badges(food, user, include_unit=False),
         ),
 
         kpis=KPIUI(
+            ppk=food_ppk,
+            body_weight=float(current_weight or 0),
             tot_kcal=food_total_kcal,
 
             g_protein=food_protein,
diff -ruN a/notas/presentation/composition/viewmodel/food/list_foods_builder.py b/notas/presentation/composition/viewmodel/food/list_foods_builder.py
--- a/notas/presentation/composition/viewmodel/food/list_foods_builder.py	2026-06-11 21:50:00.000000000 +0000
+++ b/notas/presentation/composition/viewmodel/food/list_foods_builder.py	2026-06-11 21:58:25.074002800 +0000
@@ -31,8 +31,11 @@
     return str(source).replace("_", " ").title()
 
 
-def _build_food_badges(food, user):
-    badges = [FoodBadgeUI(label="100g", modifier="unit")]
+def _build_food_badges(food, user, include_unit=True):
+    badges = []
+
+    if include_unit:
+        badges.append(FoodBadgeUI(label="100g", modifier="unit"))
 
     if food.created_by_id == getattr(user, "id", None):
         badges.append(FoodBadgeUI(label="Tu alimento", modifier="user"))
diff -ruN a/notas/presentation/viewmodels/content/food/detail_food_vm.py b/notas/presentation/viewmodels/content/food/detail_food_vm.py
--- a/notas/presentation/viewmodels/content/food/detail_food_vm.py	2026-06-11 21:50:00.000000000 +0000
+++ b/notas/presentation/viewmodels/content/food/detail_food_vm.py	2026-06-11 21:58:47.077180157 +0000
@@ -31,6 +31,8 @@
 
 @dataclass
 class KPIUI:
+    ppk: float
+    body_weight: float
     tot_kcal: float
     g_protein: float
     g_carbs: float
diff -ruN a/notas/static/notas/css/components/food_picker.css b/notas/static/notas/css/components/food_picker.css
--- a/notas/static/notas/css/components/food_picker.css	2026-06-02 06:31:18.000000000 +0000
+++ b/notas/static/notas/css/components/food_picker.css	2026-06-11 21:58:25.250353837 +0000
@@ -1213,3 +1213,75 @@
     stroke: currentColor;
   }
 }
+
+/* =========================================================
+   Food detail portion control
+   ========================================================= */
+
+.food-detail-portion-control {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 12px;
+  margin-bottom: 10px;
+}
+
+.food-detail-portion-control h3 {
+  margin: 0;
+}
+
+.food-detail-portion-control__label {
+  display: inline-flex;
+  align-items: center;
+  gap: 8px;
+  min-width: 0;
+  padding: 8px 10px;
+  border-radius: 12px;
+  background-color: var(--alloc-bar-bg);
+  color: var(--text-main);
+  font-size: 14px;
+  font-weight: 500;
+}
+
+.food-detail-portion-control__icon {
+  width: 16px;
+  height: 16px;
+  color: var(--text-muted);
+  flex-shrink: 0;
+}
+
+.food-detail-portion-control__input {
+  background-color: var(--input-bg);
+  color: var(--input-text);
+  border: 1px solid var(--input-border);
+  border-radius: 6px;
+  text-align: center;
+  width: 58px;
+  line-height: 20px;
+  padding: 2px 8px;
+  font-weight: 600;
+}
+
+.food-detail-portion-control__input:focus {
+  outline: none;
+  border-color: var(--input-focus-border);
+  box-shadow: 0 0 0 3px var(--input-focus-shadow);
+}
+
+.food-detail-portion-control__unit {
+  color: var(--text-muted);
+  white-space: nowrap;
+}
+
+@media (max-width: 768px) {
+  .food-detail-portion-control {
+    align-items: flex-start;
+    flex-direction: column;
+  }
+
+  .food-detail-portion-control__label {
+    width: 100%;
+    justify-content: flex-start;
+    box-sizing: border-box;
+  }
+}
diff -ruN a/notas/static/notas/js/food_detail_portion.js b/notas/static/notas/js/food_detail_portion.js
--- a/notas/static/notas/js/food_detail_portion.js	1970-01-01 00:00:00.000000000 +0000
+++ b/notas/static/notas/js/food_detail_portion.js	2026-06-11 21:58:25.238741288 +0000
@@ -0,0 +1,90 @@
+// ======================================================
+// food_detail_portion.js
+// Recalcula los KPI del detalle de Food según porción.
+// ======================================================
+
+import { computeAlloc, computePPK, portionFromFood } from "./food_math.js";
+
+function toNumber(value, fallback = 0) {
+  const number = Number(value);
+  return Number.isFinite(number) ? number : fallback;
+}
+
+function formatNumber(value, decimals = 0) {
+  return toNumber(value).toFixed(decimals);
+}
+
+function setText(root, selector, value) {
+  const element = root.querySelector(selector);
+  if (!element) return;
+  element.textContent = value;
+}
+
+function setAlloc(row, value) {
+  if (!row) return;
+
+  const safeValue = Math.max(0, toNumber(value));
+  const pctText = row.querySelector(".alloc-pct p");
+  const bar = row.querySelector(".alloc-bar-comp");
+
+  if (pctText) {
+    pctText.textContent = `${formatNumber(safeValue, 0)}%`;
+  }
+
+  if (bar) {
+    bar.style.setProperty("--alloc", formatNumber(safeValue, 3));
+  }
+}
+
+function getRows(root) {
+  const rows = root.querySelectorAll(".kpi-row");
+
+  return {
+    protein: rows[0] || null,
+    carbs: rows[1] || null,
+    fat: rows[2] || null,
+  };
+}
+
+function renderFoodDetailPortion(root, grams) {
+  const baseFood = {
+    protein: toNumber(root.dataset.baseProtein),
+    carbs: toNumber(root.dataset.baseCarbs),
+    fat: toNumber(root.dataset.baseFat),
+    total_kcal: toNumber(root.dataset.baseKcal),
+  };
+
+  const currentWeight = toNumber(root.dataset.currentWeight);
+  const portion = portionFromFood(baseFood, grams);
+  const alloc = computeAlloc(portion);
+  const rows = getRows(root);
+
+  setText(root, ".tot h2", formatNumber(portion.total_kcal, 0));
+  setText(rows.protein, ".kpi-grams", `${formatNumber(portion.protein, 0)}g`);
+  setText(rows.carbs, ".kpi-grams", `${formatNumber(portion.carbs, 0)}g`);
+  setText(rows.fat, ".kpi-grams", `${formatNumber(portion.fat, 0)}g`);
+
+  const ppk = computePPK(portion.protein, currentWeight);
+  setText(rows.protein, ".proteins_per_kilo", `${formatNumber(ppk, 1)}g/kg`);
+
+  setAlloc(rows.protein, alloc.protein);
+  setAlloc(rows.carbs, alloc.carbs);
+  setAlloc(rows.fat, alloc.fat);
+}
+
+document.addEventListener("DOMContentLoaded", () => {
+  const root = document.querySelector("[data-food-detail-portion]");
+  if (!root) return;
+
+  const input = document.getElementById("food-detail-quantity");
+  if (!input) return;
+
+  function sync() {
+    const grams = Math.max(0, toNumber(input.value));
+    renderFoodDetailPortion(root, grams);
+  }
+
+  input.addEventListener("input", sync);
+  input.addEventListener("change", sync);
+  sync();
+});
diff -ruN a/notas/templates/components/card_main_food.html b/notas/templates/components/card_main_food.html
--- a/notas/templates/components/card_main_food.html	2026-03-05 22:17:24.000000000 +0000
+++ b/notas/templates/components/card_main_food.html	2026-06-11 21:58:47.078253893 +0000
@@ -1,4 +1,12 @@
-<div data-page="food-detail">
+<div
+  data-page="food-detail"
+  data-food-detail-portion
+  data-base-kcal="{{ main_card.kpis.tot_kcal }}"
+  data-base-protein="{{ main_card.kpis.g_protein }}"
+  data-base-carbs="{{ main_card.kpis.g_carbs }}"
+  data-base-fat="{{ main_card.kpis.g_fat }}"
+  data-current-weight="{{ main_card.kpis.body_weight|default:0 }}"
+>
  
     <div class="card-title main">
         {% include "components/card_main_title.html" with titulo=main_card.titulo %}
@@ -7,7 +15,24 @@
     <!-- KPI ========================================= -->
     <div class="page-kpi">
         
-        <h3>Información Nutricional (100g)</h3>
+        <div class="food-detail-portion-control">
+          <h3>Información Nutricional</h3>
+
+          <label class="food-detail-portion-control__label" for="food-detail-quantity">
+            <i data-lucide="scale" class="food-detail-portion-control__icon"></i>
+            Ajusta la Porción:
+            <input
+              type="number"
+              id="food-detail-quantity"
+              class="food-detail-portion-control__input"
+              value="100"
+              min="0"
+              step="1"
+              inputmode="decimal"
+            >
+            <span class="food-detail-portion-control__unit">g|ml</span>
+          </label>
+        </div>
     
         <!-- KPI ======================== -->
         <div class="card-kpi main">
diff -ruN a/notas/templates/notas/foods/detail.html b/notas/templates/notas/foods/detail.html
--- a/notas/templates/notas/foods/detail.html	2026-03-11 15:28:39.000000000 +0000
+++ b/notas/templates/notas/foods/detail.html	2026-06-11 21:58:47.078491191 +0000
@@ -1,4 +1,5 @@
 {% extends "notas/base.html" %}
+{% load static %}
 {% block title %}
 Food – {{ food.name }}
 {% endblock %}
@@ -6,10 +7,12 @@
 
 
 <!-- ======== PAGE BODY ===================================== -->           
-<div class="page page--detail" data-page="meal-detail">
+<div class="page page--detail" data-page="food-detail">
 
   {% include "components/card_main_food.html" with main_card=vm.content.main_card %}
 
 </div>
 
+<script type="module" src="{% static 'notas/js/food_detail_portion.js' %}"></script>
+
 {% endblock %}
