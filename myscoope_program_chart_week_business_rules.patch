--- a/notas/presentation/viewmodels/programs.py	2026-06-23 03:13:38.000000000 +0000
+++ b/notas/presentation/viewmodels/programs.py	2026-06-23 05:25:58.087914845 +0000
@@ -329,7 +329,7 @@
         axis_count = len(axis_labels)
     else:
         axis_labels = [
-            {"label": f"w{week_number}", "mobileLabel": f"w{week_number}"}
+            {"label": f"S{week_number}", "mobileLabel": f"S{week_number}"}
             for week_number in week_numbers
         ]
         axis_count = len(axis_labels)
--- a/notas/static/notas/js/program_metric_chart.js	2026-06-23 05:11:09.000000000 +0000
+++ b/notas/static/notas/js/program_metric_chart.js	2026-06-23 05:25:24.035475504 +0000
@@ -81,8 +81,9 @@
   }
 
   function renderAxis(data) {
-    const axis = makeElement("div", "program-chart-week-axis", {
-      style: `--program-chart-weeks: ${data.axisCount || data.weeksCount || 1};`,
+    const axisCount = data.axisCount || data.weeksCount || 1;
+    const axis = makeElement("div", `program-chart-week-axis program-chart-week-axis--${data.scope || "program"}`, {
+      style: `--program-chart-axis-count: ${axisCount}; --program-chart-weeks: ${data.weeksCount || 1};`,
       "aria-label": "Etiquetas del gráfico",
     });
 
@@ -96,6 +97,32 @@
     return axis;
   }
 
+  function groupBarsByWeek(bars, weeksCount) {
+    const groups = Array.from({ length: Math.max(weeksCount, 1) }, (_, index) => ({
+      weekNumber: index + 1,
+      bars: [],
+    }));
+
+    (bars || []).forEach((bar) => {
+      const weekIndex = Math.max(toNumber(bar.weekNumber) - 1, 0);
+      if (!groups[weekIndex]) {
+        groups[weekIndex] = { weekNumber: weekIndex + 1, bars: [] };
+      }
+      groups[weekIndex].bars.push(bar);
+    });
+
+    return groups;
+  }
+
+  function getChartMinWidth(data) {
+    const weeks = Math.min(Math.max(toNumber(data.weeksCount || 1), 1), 12);
+    // Business rule: programs are capped at 12 weeks and the viewport should
+    // show up to 4 complete weeks before horizontal scroll is needed.
+    if ((data.scope || "program") !== "program") return "100%";
+    if (weeks <= 4) return "100%";
+    return `${weeks * 25}%`;
+  }
+
   function renderSegment(segment, stackTotal, barIsEmpty, showSegmentValues) {
     const percent = segmentPercent(segment.value, stackTotal);
     const element = makeElement("span", `program-chart-stacked-bar__segment program-chart-stacked-bar__segment--${segment.key}`, {
@@ -180,12 +207,15 @@
       "data-metric": metric.key,
     });
 
+    const scope = data.scope || "program";
+    const weeksCount = Math.max(toNumber(data.weeksCount || 1), 1);
     const layout = makeElement("div", "program-chart-layout");
-    const axis = makeElement("div", "program-chart-main-axis", {
-      style: `--program-chart-days: ${data.daysCount || 1}; --program-chart-weeks: ${data.weeksCount || 1}; --program-chart-min-width: ${Math.max(100, (data.weeksCount || 1) * 25)}%;`,
+    const axis = makeElement("div", `program-chart-main-axis program-chart-main-axis--${scope}`, {
+      style: `--program-chart-days: ${data.daysCount || 1}; --program-chart-weeks: ${weeksCount}; --program-chart-min-width: ${getChartMinWidth(data)};`,
+      "data-weeks-count": Math.min(weeksCount, 12),
     });
-    const plot = makeElement("div", `program-chart-plot program-chart-plot--${metric.kind} program-chart-plot--${metric.key}`, {
-      style: `--program-chart-days: ${data.daysCount || 1};`,
+    const plot = makeElement("div", `program-chart-plot program-chart-plot--${scope} program-chart-plot--${metric.kind} program-chart-plot--${metric.key}`, {
+      style: `--program-chart-days: ${data.daysCount || 1}; --program-chart-weeks: ${weeksCount};`,
       "aria-label": `Gráfico de ${metric.label}`,
     });
 
@@ -193,9 +223,22 @@
     plot.appendChild(makeElement("span", "program-chart-guide program-chart-guide--min", { "aria-hidden": "true" }));
 
     const metricMax = getMetricMax(metric);
-    (metric.bars || []).forEach((bar) => {
-      plot.appendChild(renderBar(metric, bar, metricMax, showBarValues));
-    });
+    if (scope === "program") {
+      groupBarsByWeek(metric.bars || [], weeksCount).forEach((week) => {
+        const weekColumn = makeElement("div", "program-chart-week-column", {
+          "data-week-number": week.weekNumber,
+          "aria-label": `Semana ${week.weekNumber}`,
+        });
+        week.bars.forEach((bar) => {
+          weekColumn.appendChild(renderBar(metric, bar, metricMax, showBarValues));
+        });
+        plot.appendChild(weekColumn);
+      });
+    } else {
+      (metric.bars || []).forEach((bar) => {
+        plot.appendChild(renderBar(metric, bar, metricMax, showBarValues));
+      });
+    }
 
     axis.appendChild(plot);
     axis.appendChild(renderAxis(data));
--- a/notas/static/notas/css/components/programs.css	2026-06-23 05:17:16.000000000 +0000
+++ b/notas/static/notas/css/components/programs.css	2026-06-23 05:25:40.178444145 +0000
@@ -2556,3 +2556,126 @@
   color: var(--text-main);
   font-weight: 450;
 }
+
+/* Program charts: week-oriented rendering rules =========================== */
+.program-chart-panel {
+  --program-chart-max-visible-weeks: 4;
+}
+
+.program-chart-plot,
+.program-chart-week-axis {
+  width: var(--program-chart-min-width, 100%);
+  min-width: var(--program-chart-min-width, 100%);
+}
+
+.program-chart-plot {
+  background:
+    linear-gradient(to top, color-mix(in srgb, var(--border-default) 42%, transparent) 1px, transparent 1px) 0 0 / 100% 25%,
+    var(--surface-app);
+}
+
+.program-chart-plot--program {
+  grid-template-columns: repeat(var(--program-chart-weeks), minmax(0, 1fr));
+  align-items: stretch;
+  padding-inline: 0.35rem;
+}
+
+.program-chart-week-column {
+  display: grid;
+  grid-template-columns: repeat(7, minmax(3px, 1fr));
+  align-items: end;
+  height: 100%;
+  min-width: 0;
+  border-left: 2px solid color-mix(in srgb, var(--entity-program) 34%, transparent);
+}
+
+.program-chart-week-column:first-of-type {
+  border-left-color: transparent;
+}
+
+.program-chart-plot--program .program-chart-bar-slot.is-week-start {
+  border-left: 0;
+}
+
+.program-chart-plot--program .program-chart-bar-slot,
+.program-chart-plot--week .program-chart-bar-slot {
+  padding-inline: clamp(0.03rem, 0.3vw, 0.14rem);
+}
+
+.program-chart-week-axis {
+  grid-template-columns: repeat(var(--program-chart-axis-count, var(--program-chart-weeks)), minmax(0, 1fr));
+  width: var(--program-chart-min-width, 100%);
+  min-width: var(--program-chart-min-width, 100%);
+  padding-inline: 0.35rem;
+}
+
+.program-chart-week-axis--program span {
+  border-left: 2px solid color-mix(in srgb, var(--entity-program) 34%, transparent);
+}
+
+.program-chart-week-axis--program span:first-child {
+  border-left-color: transparent;
+}
+
+.program-chart-week-axis--week {
+  padding-inline: 0.35rem;
+}
+
+.program-chart-week-axis--week span {
+  border-left-color: transparent;
+}
+
+.program-chart-main-axis--program[data-weeks-count="1"] .program-chart-bar,
+.program-chart-main-axis--program[data-weeks-count="1"] .program-chart-stacked-bar,
+.program-chart-main-axis--program[data-weeks-count="2"] .program-chart-bar,
+.program-chart-main-axis--program[data-weeks-count="2"] .program-chart-stacked-bar,
+.program-chart-main-axis--program[data-weeks-count="3"] .program-chart-bar,
+.program-chart-main-axis--program[data-weeks-count="3"] .program-chart-stacked-bar,
+.program-chart-main-axis--program[data-weeks-count="4"] .program-chart-bar,
+.program-chart-main-axis--program[data-weeks-count="4"] .program-chart-stacked-bar {
+  width: min(80%, 22px);
+}
+
+.program-chart-main-axis--program[data-weeks-count="1"] .program-chart-week-column {
+  padding-inline: clamp(0.45rem, 4vw, 2rem);
+}
+
+.program-chart-main-axis--program[data-weeks-count="2"] .program-chart-week-column {
+  padding-inline: clamp(0.22rem, 2vw, 1rem);
+}
+
+.program-chart-main-axis--program[data-weeks-count="3"] .program-chart-week-column,
+.program-chart-main-axis--program[data-weeks-count="4"] .program-chart-week-column {
+  padding-inline: clamp(0.06rem, 0.9vw, 0.45rem);
+}
+
+.program-chart-main-axis--program[data-weeks-count="5"] .program-chart-week-column,
+.program-chart-main-axis--program[data-weeks-count="6"] .program-chart-week-column,
+.program-chart-main-axis--program[data-weeks-count="7"] .program-chart-week-column,
+.program-chart-main-axis--program[data-weeks-count="8"] .program-chart-week-column,
+.program-chart-main-axis--program[data-weeks-count="9"] .program-chart-week-column,
+.program-chart-main-axis--program[data-weeks-count="10"] .program-chart-week-column,
+.program-chart-main-axis--program[data-weeks-count="11"] .program-chart-week-column,
+.program-chart-main-axis--program[data-weeks-count="12"] .program-chart-week-column {
+  padding-inline: 0.03rem;
+}
+
+@media (max-width: 780px) {
+  .program-chart-week-axis--program span {
+    font-size: 0.55rem;
+  }
+
+  .program-chart-week-axis--program span::after {
+    content: none;
+  }
+
+  .program-chart-main-axis--program[data-weeks-count="1"] .program-chart-week-column {
+    padding-inline: 0.35rem;
+  }
+
+  .program-chart-main-axis--program[data-weeks-count="2"] .program-chart-week-column,
+  .program-chart-main-axis--program[data-weeks-count="3"] .program-chart-week-column,
+  .program-chart-main-axis--program[data-weeks-count="4"] .program-chart-week-column {
+    padding-inline: 0.04rem;
+  }
+}
