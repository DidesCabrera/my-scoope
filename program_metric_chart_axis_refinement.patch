--- myscoope_chart98_orig/notas/interface/views/programs.py	2026-06-19 03:23:36.000000000 +0000
+++ myscoope_chart98_mod/notas/interface/views/programs.py	2026-06-19 03:34:30.469463699 +0000
@@ -550,6 +550,15 @@
     return f"{value:.0f}"
 
 
+def _program_chart_axis_ticks(max_value, unit, decimals=0):
+    max_value = max(float(max_value or 0), 1)
+    ticks = (max_value, max_value / 2, 0)
+    return [
+        {"label": f"{_format_chart_number(tick, decimals)} {unit}".strip()}
+        for tick in ticks
+    ]
+
+
 def _program_chart_bar_height(value, max_value):
     value = max(float(value or 0), 0)
     max_value = max(float(max_value or 0), 1)
@@ -574,7 +583,7 @@
 
             day_points.append({
                 "day_index": day_index,
-                "x_label": f"D{day_index}",
+                "x_label": f"w{week['week_number']}",
                 "week_number": week["week_number"],
                 "day_number": day["day_number"],
                 "day_label": day["day_label"],
@@ -625,6 +634,11 @@
             "kind": "bar",
             "is_active": index == 0,
             "bars": bars,
+            "y_axis_ticks": _program_chart_axis_ticks(
+                max_value,
+                spec["unit"],
+                spec["decimals"],
+            ),
         })
 
     alloc_bars = []
@@ -659,12 +673,21 @@
         "kind": "stacked",
         "is_active": False,
         "bars": alloc_bars,
+        "y_axis_ticks": [
+            {"label": "100%"},
+            {"label": "50%"},
+            {"label": "0%"},
+        ],
     })
 
+    week_numbers = [week["week_number"] for week in weeks]
+
     return {
         "title": "Variación diaria del programa",
-        "subtitle": "Eje X por días, con cortes visuales entre semanas.",
+        "subtitle": "Eje X agrupado por semanas, con cortes visuales entre bloques de 7 días.",
         "days_count": len(day_points),
+        "weeks_count": len(week_numbers),
+        "week_labels": [f"w{week_number}" for week_number in week_numbers],
         "metrics": metrics,
     }
 
--- myscoope_chart98_orig/notas/templates/components/program_metric_chart.html	2026-06-19 03:23:36.000000000 +0000
+++ myscoope_chart98_mod/notas/templates/components/program_metric_chart.html	2026-06-19 03:34:12.874458766 +0000
@@ -30,36 +30,52 @@
         data-metric="{{ metric.key }}"
         {% if not metric.is_active %}hidden{% endif %}
       >
-        <div
-          class="program-chart-plot program-chart-plot--{{ metric.kind }}"
-          style="--program-chart-days: {{ chart.days_count|default:1 }};"
-          aria-label="Gráfico de {{ metric.label }}"
-        >
-          {% for bar in metric.bars %}
+        <div class="program-chart-layout">
+          <div class="program-chart-y-axis" aria-hidden="true">
+            {% for tick in metric.y_axis_ticks %}
+              <span>{{ tick.label }}</span>
+            {% endfor %}
+          </div>
+
+          <div class="program-chart-main-axis">
             <div
-              class="program-chart-bar-slot {% if bar.is_week_start %}is-week-start{% endif %} {% if bar.is_empty %}is-empty{% endif %}"
-              title="{{ bar.title }}"
-              aria-label="{{ bar.title }}"
+              class="program-chart-plot program-chart-plot--{{ metric.kind }} program-chart-plot--{{ metric.key }}"
+              style="--program-chart-days: {{ chart.days_count|default:1 }};"
+              aria-label="Gráfico de {{ metric.label }}"
             >
-              <span class="program-chart-bar-value">{{ bar.value_label }}</span>
-
-              {% if metric.kind == "stacked" %}
-                <div class="program-chart-stacked-bar" style="height: {{ bar.height_percent|floatformat:2 }}%;">
-                  {% for segment in bar.segments %}
-                    <span
-                      class="program-chart-stacked-bar__segment program-chart-stacked-bar__segment--{{ segment.key }}"
-                      style="height: {{ segment.height_percent|floatformat:2 }}%;"
-                      aria-label="{{ segment.label }} {{ segment.height_percent|floatformat:0 }}%"
-                    ></span>
-                  {% endfor %}
+              {% for bar in metric.bars %}
+                <div
+                  class="program-chart-bar-slot {% if bar.is_week_start %}is-week-start{% endif %} {% if bar.is_empty %}is-empty{% endif %}"
+                  title="{{ bar.title }}"
+                  aria-label="{{ bar.title }}"
+                >
+                  {% if metric.kind == "stacked" %}
+                    <div class="program-chart-stacked-bar" style="height: {{ bar.height_percent|floatformat:2 }}%;">
+                      {% for segment in bar.segments %}
+                        <span
+                          class="program-chart-stacked-bar__segment program-chart-stacked-bar__segment--{{ segment.key }}"
+                          style="height: {{ segment.height_percent|floatformat:2 }}%;"
+                          aria-label="{{ segment.label }} {{ segment.height_percent|floatformat:0 }}%"
+                        ></span>
+                      {% endfor %}
+                    </div>
+                  {% else %}
+                    <div class="program-chart-bar" style="height: {{ bar.height_percent|floatformat:2 }}%;"></div>
+                  {% endif %}
                 </div>
-              {% else %}
-                <div class="program-chart-bar" style="height: {{ bar.height_percent|floatformat:2 }}%;"></div>
-              {% endif %}
+              {% endfor %}
+            </div>
 
-              <span class="program-chart-bar-day">{{ bar.x_label }}</span>
+            <div
+              class="program-chart-week-axis"
+              style="--program-chart-weeks: {{ chart.weeks_count|default:1 }};"
+              aria-label="Semanas del programa"
+            >
+              {% for week_label in chart.week_labels %}
+                <span>{{ week_label }}</span>
+              {% endfor %}
             </div>
-          {% endfor %}
+          </div>
         </div>
 
         {% if metric.kind == "stacked" %}
--- myscoope_chart98_orig/notas/static/notas/css/components/programs.css	2026-06-19 03:23:36.000000000 +0000
+++ myscoope_chart98_mod/notas/static/notas/css/components/programs.css	2026-06-19 03:35:13.764622577 +0000
@@ -659,7 +659,7 @@
   gap: 0.85rem;
   padding: 0.9rem;
   border: 1px solid var(--border-default);
-  border-radius: 18px;
+  border-radius: 12px;
   background: var(--surface-card);
   overflow: hidden;
 }
@@ -691,8 +691,8 @@
 
 .program-chart-panel__header span,
 .program-chart-panel__eyebrow,
-.program-chart-bar-value,
-.program-chart-bar-day {
+.program-chart-y-axis,
+.program-chart-week-axis {
   color: var(--text-muted);
 }
 
@@ -726,7 +726,7 @@
 
 .program-chart-tab {
   border: 1px solid var(--border-default);
-  border-radius: 999px;
+  border-radius: 8px;
   background: var(--surface-card-muted);
   color: var(--text-muted);
   padding: 0.38rem 0.6rem;
@@ -742,7 +742,8 @@
 }
 
 .program-chart-panel__body,
-.program-chart-pane {
+.program-chart-pane,
+.program-chart-main-axis {
   min-width: 0;
 }
 
@@ -750,6 +751,32 @@
   display: none;
 }
 
+.program-chart-layout {
+  display: grid;
+  grid-template-columns: auto minmax(0, 1fr);
+  gap: 0.45rem;
+  align-items: stretch;
+}
+
+.program-chart-y-axis {
+  display: flex;
+  flex-direction: column;
+  justify-content: space-between;
+  width: 3.5rem;
+  height: 156px;
+  padding: 0.65rem 0 0.2rem;
+  font-size: 0.58rem;
+  font-weight: 800;
+  line-height: 1;
+  text-align: right;
+  white-space: nowrap;
+}
+
+.program-chart-main-axis {
+  display: grid;
+  gap: 0.3rem;
+}
+
 .program-chart-plot {
   --program-chart-days: 7;
   display: grid;
@@ -760,7 +787,7 @@
   height: 156px;
   padding: 0.65rem 0.2rem 0.2rem;
   border: 1px solid color-mix(in srgb, var(--entity-program) 12%, var(--border-default));
-  border-radius: 14px;
+  border-radius: 8px;
   background:
     linear-gradient(to top, color-mix(in srgb, var(--border-default) 48%, transparent) 1px, transparent 1px) 0 0 / 100% 25%,
     color-mix(in srgb, var(--surface-card-muted) 72%, transparent);
@@ -771,7 +798,6 @@
   display: grid;
   align-items: end;
   justify-items: center;
-  gap: 0.2rem;
   height: 100%;
   min-width: 0;
   padding: 0 0.12rem;
@@ -789,16 +815,32 @@
 .program-chart-stacked-bar {
   width: min(100%, 18px);
   min-height: 2px;
-  border-radius: 8px 8px 2px 2px;
+  border-radius: 4px 4px 1px 1px;
   overflow: hidden;
 }
 
 .program-chart-bar {
-  background: linear-gradient(
-    to top,
-    color-mix(in srgb, var(--entity-program) 68%, var(--surface-card)),
-    color-mix(in srgb, var(--entity-program) 34%, var(--surface-card))
-  );
+  background: var(--entity-program);
+}
+
+.program-chart-plot--calories .program-chart-bar {
+  background: var(--entity-program);
+}
+
+.program-chart-plot--protein .program-chart-bar {
+  background: var(--nutrition-protein);
+}
+
+.program-chart-plot--carbs .program-chart-bar {
+  background: var(--nutrition-carbs);
+}
+
+.program-chart-plot--fat .program-chart-bar {
+  background: var(--nutrition-fat);
+}
+
+.program-chart-plot--ppk .program-chart-bar {
+  background: var(--nutrition-ppk);
 }
 
 .program-chart-stacked-bar {
@@ -816,36 +858,38 @@
 
 .program-chart-stacked-bar__segment--protein,
 .program-chart-legend__swatch--protein {
-  background: var(--entity-dailyplan);
+  background: var(--nutrition-protein);
 }
 
 .program-chart-stacked-bar__segment--carbs,
 .program-chart-legend__swatch--carbs {
-  background: var(--entity-meal);
+  background: var(--nutrition-carbs);
 }
 
 .program-chart-stacked-bar__segment--fat,
 .program-chart-legend__swatch--fat {
-  background: var(--entity-food);
+  background: var(--nutrition-fat);
 }
 
-.program-chart-bar-value {
-  max-width: 100%;
-  min-height: 1.05rem;
-  font-size: 0.58rem;
-  font-weight: 800;
+.program-chart-week-axis {
+  --program-chart-weeks: 1;
+  display: grid;
+  grid-template-columns: repeat(var(--program-chart-weeks), minmax(0, 1fr));
+  gap: 0;
+  font-size: 0.56rem;
+  font-weight: 900;
   line-height: 1;
-  overflow: hidden;
   text-align: center;
-  text-overflow: ellipsis;
-  white-space: nowrap;
+  text-transform: lowercase;
 }
 
-.program-chart-bar-day {
-  min-height: 0.85rem;
-  font-size: 0.52rem;
-  font-weight: 800;
-  line-height: 1;
+.program-chart-week-axis span {
+  padding-top: 0.15rem;
+  border-left: 2px solid color-mix(in srgb, var(--entity-program) 32%, transparent);
+}
+
+.program-chart-week-axis span:first-child {
+  border-left-color: transparent;
 }
 
 .program-chart-legend {
@@ -867,16 +911,14 @@
 .program-chart-legend__swatch {
   width: 0.6rem;
   height: 0.6rem;
-  border-radius: 999px;
+  border-radius: 3px;
 }
 
 .program-chart-panel--card {
   padding: 0.75rem;
 }
 
-.program-chart-panel--card .program-chart-panel__header span,
-.program-chart-panel--card .program-chart-bar-value,
-.program-chart-panel--card .program-chart-bar-day {
+.program-chart-panel--card .program-chart-panel__header span {
   display: none;
 }
 
@@ -893,8 +935,17 @@
   font-size: 0.64rem;
 }
 
+.program-chart-panel--card .program-chart-y-axis,
 .program-chart-panel--card .program-chart-plot {
   height: 92px;
+}
+
+.program-chart-panel--card .program-chart-y-axis {
+  width: 2.7rem;
+  font-size: 0.5rem;
+}
+
+.program-chart-panel--card .program-chart-plot {
   padding-top: 0.4rem;
 }
 
@@ -903,6 +954,10 @@
   width: min(100%, 12px);
 }
 
+.program-chart-panel--card .program-chart-week-axis {
+  font-size: 0.48rem;
+}
+
 @media (max-width: 780px) {
   .program-chart-panel {
     padding: 0.75rem;
@@ -912,8 +967,21 @@
     align-items: flex-start;
   }
 
+  .program-chart-layout {
+    gap: 0.35rem;
+  }
+
+  .program-chart-y-axis,
   .program-chart-plot {
     height: 118px;
+  }
+
+  .program-chart-y-axis {
+    width: 2.8rem;
+    font-size: 0.5rem;
+  }
+
+  .program-chart-plot {
     padding-inline: 0.12rem;
   }
 
@@ -922,11 +990,7 @@
     width: min(100%, 10px);
   }
 
-  .program-chart-bar-value {
-    display: none;
-  }
-
-  .program-chart-bar-day {
+  .program-chart-week-axis {
     font-size: 0.48rem;
   }
 }
