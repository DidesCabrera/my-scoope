diff --git a/admin_analytics/templates/admin_analytics/_operations_bridge.html b/admin_analytics/templates/admin_analytics/_operations_bridge.html
new file mode 100644
index 0000000..8d08c0d
--- /dev/null
+++ b/admin_analytics/templates/admin_analytics/_operations_bridge.html
@@ -0,0 +1,11 @@
+<section class="admin-analytics-operations-bridge" aria-label="Admin Operations bridge">
+  <div class="admin-analytics-operations-bridge__icon">
+    <i data-lucide="{{ icon|default:'wrench' }}"></i>
+  </div>
+  <div>
+    <p class="section-eyebrow">Admin Operations</p>
+    <h3>{{ title }}</h3>
+    <p class="muted-text">{{ message }}</p>
+  </div>
+  <a class="btn-secondary btn-secondary--compact" href="{{ href }}">{{ cta|default:'Ir a Operations' }}</a>
+</section>
diff --git a/admin_operations/templates/admin_operations/account_detail.html b/admin_operations/templates/admin_operations/account_detail.html
index 985c07e..4137efb 100644
--- a/admin_operations/templates/admin_operations/account_detail.html
+++ b/admin_operations/templates/admin_operations/account_detail.html
@@ -18,7 +18,7 @@
       <p class="section-eyebrow">Wallet</p><h3>{{ vm.content.wallet.user_label }}</h3><p class="muted-text">{{ vm.content.wallet.email }} · {{ vm.content.wallet.subscription_label }}</p>
       <div class="admin-operations-wallet-facts"><span><strong>Balance</strong>{{ vm.content.wallet.balance }}</span><span><strong>Reservado</strong>{{ vm.content.wallet.reserved_balance }}</span><span><strong>Disponible</strong>{{ vm.content.wallet.available_credits }}</span></div>
     </div>
-    <form class="admin-operations-adjustment-form" method="post" action="{% url 'admin_operations_account_credit_adjustment' vm.content.wallet.user_id %}">
+    <form class="admin-operations-adjustment-form" method="post" action="{% url 'admin_operations_account_credit_adjustment' vm.content.wallet.user_id %}" data-admin-operations-confirm data-confirm-message="Confirmar ajuste manual de créditos?">
       {% csrf_token %}
       <label>Ajuste de créditos<input type="number" name="credits_delta" required placeholder="Ej: 10 o -5"></label>
       <label>Razón obligatoria<input type="text" name="reason" required placeholder="Motivo del ajuste..."></label>
@@ -28,12 +28,12 @@
 
   <section class="card-detail-block">
     <div class="card-title-row"><div><p class="section-eyebrow">Reservas abiertas</p><h3>Release operacional</h3></div></div>
-    {% if vm.content.reservations %}<div class="admin-operations-card-list">{% for reservation in vm.content.reservations %}<article class="admin-operations-reservation-card"><div><h4>{{ reservation.reference_label }}</h4><p class="muted-text">{{ reservation.created_label }} · {{ reservation.reason }}</p></div><form class="admin-operations-adjustment-form" method="post" action="{% url 'admin_operations_account_reservation_release' vm.content.wallet.user_id reservation.pk %}">{% csrf_token %}<label>Razón obligatoria<input type="text" name="reason" required placeholder="Motivo del release..."></label><button class="btn-secondary btn-secondary--compact" type="submit">Liberar {{ reservation.credits }} créditos</button></form></article>{% endfor %}</div>{% else %}<div class="admin-operations-empty-state"><i data-lucide="circle-check" class="home-stat-icon"></i><div><h4>Sin reservas abiertas</h4><p class="muted-text">No hay créditos reservados para liberar.</p></div></div>{% endif %}
+    {% if vm.content.reservations %}<div class="admin-operations-card-list">{% for reservation in vm.content.reservations %}<article class="admin-operations-reservation-card"><div><h4>{{ reservation.reference_label }}</h4><p class="muted-text">{{ reservation.created_label }} · {{ reservation.reason }}</p></div><form class="admin-operations-adjustment-form" method="post" action="{% url 'admin_operations_account_reservation_release' vm.content.wallet.user_id reservation.pk %}" data-admin-operations-confirm data-confirm-message="Confirmar liberación de reserva de créditos?">{% csrf_token %}<label>Razón obligatoria<input type="text" name="reason" required placeholder="Motivo del release..."></label><button class="btn-secondary btn-secondary--compact" type="submit">Liberar {{ reservation.credits }} créditos</button></form></article>{% endfor %}</div>{% else %}<div class="admin-operations-empty-state"><i data-lucide="circle-check" class="home-stat-icon"></i><div><h4>Sin reservas abiertas</h4><p class="muted-text">No hay créditos reservados para liberar.</p></div></div>{% endif %}
   </section>
 
   <section class="card-detail-block">
     <div class="card-title-row"><div><p class="section-eyebrow">Ledger</p><h3>Últimos movimientos</h3></div></div>
-    {% if vm.content.ledger_entries %}<div class="admin-operations-table-card"><table class="admin-operations-table"><thead><tr><th>Fecha</th><th>Tipo</th><th>Créditos</th><th>Reservado</th><th>Balance</th><th>Razón</th></tr></thead><tbody>{% for entry in vm.content.ledger_entries %}<tr><td>{{ entry.created_label }}</td><td>{{ entry.kind }}</td><td>{{ entry.credits_delta }}</td><td>{{ entry.reserved_delta }}</td><td>{{ entry.balance_after }} / {{ entry.reserved_balance_after }}</td><td>{{ entry.reason }}</td></tr>{% endfor %}</tbody></table></div>{% else %}<div class="admin-operations-empty-state"><i data-lucide="scroll-text" class="home-stat-icon"></i><div><h4>Sin movimientos</h4><p class="muted-text">La wallet todavía no tiene ledger entries.</p></div></div>{% endif %}
+    {% if vm.content.ledger_entries %}<div class="admin-operations-table-card"><table class="admin-operations-table"><thead><tr><th>Fecha</th><th>Tipo</th><th>Créditos</th><th>Reservado</th><th>Balance</th><th>Razón</th></tr></thead><tbody>{% for entry in vm.content.ledger_entries %}<tr><td data-label="Fecha">{{ entry.created_label }}</td><td data-label="Tipo">{{ entry.kind }}</td><td data-label="Créditos">{{ entry.credits_delta }}</td><td data-label="Reservado">{{ entry.reserved_delta }}</td><td data-label="Balance">{{ entry.balance_after }} / {{ entry.reserved_balance_after }}</td><td data-label="Razón">{{ entry.reason }}</td></tr>{% endfor %}</tbody></table></div>{% else %}<div class="admin-operations-empty-state"><i data-lucide="scroll-text" class="home-stat-icon"></i><div><h4>Sin movimientos</h4><p class="muted-text">La wallet todavía no tiene ledger entries.</p></div></div>{% endif %}
   </section>
 </div>
 {% endblock %}
diff --git a/admin_operations/templates/admin_operations/accounts.html b/admin_operations/templates/admin_operations/accounts.html
index 31aab8b..2dd41a7 100644
--- a/admin_operations/templates/admin_operations/accounts.html
+++ b/admin_operations/templates/admin_operations/accounts.html
@@ -43,7 +43,7 @@
     {% if vm.content.wallets %}
       <div class="admin-operations-table-card"><table class="admin-operations-table"><thead><tr><th>Usuario</th><th>Balance</th><th>Reservado</th><th>Disponible</th><th>Plan</th><th>Acción</th></tr></thead><tbody>
         {% for wallet in vm.content.wallets %}
-          <tr><td><strong>{{ wallet.user_label }}</strong><small>{{ wallet.email }}</small></td><td>{{ wallet.balance }}</td><td>{{ wallet.reserved_balance }}</td><td>{{ wallet.available_credits }}</td><td><span>{{ wallet.plan_snapshot_code }}</span><small>{{ wallet.subscription_label }}</small></td><td><a class="btn-secondary btn-secondary--compact" href="{{ wallet.detail_url }}">Revisar</a></td></tr>
+          <tr><td data-label="Usuario"><strong>{{ wallet.user_label }}</strong><small>{{ wallet.email }}</small></td><td data-label="Balance">{{ wallet.balance }}</td><td data-label="Reservado">{{ wallet.reserved_balance }}</td><td data-label="Disponible">{{ wallet.available_credits }}</td><td data-label="Plan"><span>{{ wallet.plan_snapshot_code }}</span><small>{{ wallet.subscription_label }}</small></td><td data-label="Acción"><a class="btn-secondary btn-secondary--compact" href="{{ wallet.detail_url }}">Revisar</a></td></tr>
         {% endfor %}
       </tbody></table></div>
     {% else %}
diff --git a/admin_operations/templates/admin_operations/ai_assistant.html b/admin_operations/templates/admin_operations/ai_assistant.html
index a01bf86..4baeaf9 100644
--- a/admin_operations/templates/admin_operations/ai_assistant.html
+++ b/admin_operations/templates/admin_operations/ai_assistant.html
@@ -65,7 +65,7 @@
               <p class="admin-operations-queue-card__helper">{{ event.provider_label }} · {{ event.model_name }} · {{ event.error_type }} · {{ event.tokens_label }} tokens · {{ event.credits_label }} créditos</p>
               <span class="admin-operations-status-pill">{{ event.metadata_state }}</span>
             </div>
-            <form class="admin-operations-action-form" method="post" action="{% url 'admin_operations_ai_event_action' event.pk %}">
+            <form class="admin-operations-action-form" method="post" action="{% url 'admin_operations_ai_event_action' event.pk %}" data-admin-operations-confirm data-confirm-message="Confirmar revisión del evento IA?">
               {% csrf_token %}
               <label>Razón obligatoria<textarea name="reason" required placeholder="Contexto de revisión, causa probable o decisión operacional..."></textarea></label>
               <div class="admin-operations-inline-actions">
@@ -97,11 +97,11 @@
           <tbody>
             {% for proposal in vm.content.proposals %}
               <tr>
-                <td><strong>{{ proposal.title }}</strong><small>{{ proposal.created_label }}</small>{% if proposal.summary %}<small>{{ proposal.summary }}</small>{% endif %}</td>
-                <td><span class="admin-operations-status-pill">{{ proposal.source }}</span></td>
-                <td><span>{{ proposal.created_by_label }}</span></td>
-                <td><span>{{ proposal.dailyplan_label }}</span><small>{{ proposal.status }}</small></td>
-                <td><a class="btn-secondary btn-secondary--compact" href="{{ proposal.detail_url }}">Revisar</a></td>
+                <td data-label="Propuesta"><strong>{{ proposal.title }}</strong><small>{{ proposal.created_label }}</small>{% if proposal.summary %}<small>{{ proposal.summary }}</small>{% endif %}</td>
+                <td data-label="Origen"><span class="admin-operations-status-pill">{{ proposal.source }}</span></td>
+                <td data-label="Usuario"><span>{{ proposal.created_by_label }}</span></td>
+                <td data-label="Contexto"><span>{{ proposal.dailyplan_label }}</span><small>{{ proposal.status }}</small></td>
+                <td data-label="Acción"><a class="btn-secondary btn-secondary--compact" href="{{ proposal.detail_url }}">Revisar</a></td>
               </tr>
             {% endfor %}
           </tbody>
@@ -131,7 +131,7 @@
               <p class="admin-operations-queue-card__helper">Uso mensual {{ quota.usage_label }} · límite diario {{ quota.daily_limit }}</p>
               <span class="admin-operations-status-pill">{% if quota.hard_blocked %}hard-blocked{% else %}sobre límite{% endif %}</span>
             </div>
-            <form class="admin-operations-action-form" method="post" action="{% url 'admin_operations_ai_quota_action' quota.pk %}">
+            <form class="admin-operations-action-form" method="post" action="{% url 'admin_operations_ai_quota_action' quota.pk %}" data-admin-operations-confirm data-confirm-message="Confirmar cambio de bloqueo de acceso IA?">
               {% csrf_token %}
               <label>Razón obligatoria<textarea name="reason" required placeholder="Motivo de bloqueo/desbloqueo..."></textarea></label>
               <div class="admin-operations-inline-actions">
diff --git a/admin_operations/templates/admin_operations/ai_proposal_detail.html b/admin_operations/templates/admin_operations/ai_proposal_detail.html
index a3b92ff..4400135 100644
--- a/admin_operations/templates/admin_operations/ai_proposal_detail.html
+++ b/admin_operations/templates/admin_operations/ai_proposal_detail.html
@@ -26,7 +26,7 @@
       <h3>{{ vm.content.dailyplan_label }}</h3>
       <p class="muted-text">{{ vm.content.summary|default:"Sin resumen disponible." }}</p>
     </div>
-    <form class="admin-operations-action-form" method="post" action="{% url 'admin_operations_ai_proposal_action' vm.content.pk %}">
+    <form class="admin-operations-action-form" method="post" action="{% url 'admin_operations_ai_proposal_action' vm.content.pk %}" data-admin-operations-confirm data-confirm-message="Confirmar decisión sobre esta propuesta IA?">
       {% csrf_token %}
       <label>Razón obligatoria<textarea name="reason" required placeholder="Motivo de aprobación o rechazo operacional..."></textarea></label>
       <div class="admin-operations-action-grid">
diff --git a/admin_operations/templates/admin_operations/audit_log.html b/admin_operations/templates/admin_operations/audit_log.html
index 1289e28..10f8e44 100644
--- a/admin_operations/templates/admin_operations/audit_log.html
+++ b/admin_operations/templates/admin_operations/audit_log.html
@@ -62,12 +62,12 @@
           <tbody>
             {% for event in vm.content.events %}
               <tr>
-                <td><strong>{{ event.created_label }}</strong><small>{{ event.actor_label }}</small></td>
-                <td><span class="admin-operations-status-pill">{{ event.action }}</span></td>
-                <td><strong>{{ event.target_label }}</strong><small>{{ event.target_type }} #{{ event.target_id }}</small></td>
-                <td><span>{{ event.status_before }}</span><small>→ {{ event.status_after }}</small></td>
-                <td><span>{{ event.reason }}</span></td>
-                <td><small>{{ event.metadata_summary }}</small></td>
+                <td data-label="Fecha / Actor"><strong>{{ event.created_label }}</strong><small>{{ event.actor_label }}</small></td>
+                <td data-label="Acción"><span class="admin-operations-status-pill">{{ event.action }}</span></td>
+                <td data-label="Target"><strong>{{ event.target_label }}</strong><small>{{ event.target_type }} #{{ event.target_id }}</small></td>
+                <td data-label="Transición"><span>{{ event.status_before }}</span><small>→ {{ event.status_after }}</small></td>
+                <td data-label="Razón"><span>{{ event.reason }}</span></td>
+                <td data-label="Metadata"><small>{{ event.metadata_summary }}</small></td>
               </tr>
             {% endfor %}
           </tbody>
diff --git a/admin_operations/templates/admin_operations/base.html b/admin_operations/templates/admin_operations/base.html
index 811a2a5..7f52bb1 100644
--- a/admin_operations/templates/admin_operations/base.html
+++ b/admin_operations/templates/admin_operations/base.html
@@ -84,6 +84,25 @@
   </div>
 
   <script>
+    (function () {
+      document.addEventListener("submit", function (event) {
+        const form = event.target;
+        if (!form.matches("[data-admin-operations-confirm]")) {
+          return;
+        }
+        const reasonField = form.querySelector("[name='reason']");
+        if (reasonField && !reasonField.value.trim()) {
+          event.preventDefault();
+          reasonField.focus();
+          return;
+        }
+        const message = form.getAttribute("data-confirm-message") || "Confirmar acción operacional en Admin Operations?";
+        if (!window.confirm(message)) {
+          event.preventDefault();
+        }
+      });
+    })();
+
     if (window.lucide) {
       window.lucide.createIcons();
     }
diff --git a/admin_operations/templates/admin_operations/food_catalog.html b/admin_operations/templates/admin_operations/food_catalog.html
index 6565eaa..741dd62 100644
--- a/admin_operations/templates/admin_operations/food_catalog.html
+++ b/admin_operations/templates/admin_operations/food_catalog.html
@@ -53,15 +53,15 @@
           <tbody>
             {% for candidate in vm.content.candidates %}
               <tr>
-                <td>
+                <td data-label="Candidato">
                   <strong>{{ candidate.title }}</strong>
                   {% if candidate.brand_name %}<span>{{ candidate.brand_name }}</span>{% endif %}
                   <small>{{ candidate.provider }} · {{ candidate.reason }}</small>
                 </td>
-                <td><span class="admin-operations-status-pill">{{ candidate.status }}</span></td>
-                <td>{{ candidate.priority }}</td>
-                <td>{{ candidate.demand_label }}</td>
-                <td><a class="btn-secondary btn-secondary--compact" href="{{ candidate.detail_url }}">Revisar</a></td>
+                <td data-label="Estado"><span class="admin-operations-status-pill">{{ candidate.status }}</span></td>
+                <td data-label="Prioridad">{{ candidate.priority }}</td>
+                <td data-label="Demanda">{{ candidate.demand_label }}</td>
+                <td data-label="Acción"><a class="btn-secondary btn-secondary--compact" href="{{ candidate.detail_url }}">Revisar</a></td>
               </tr>
             {% endfor %}
           </tbody>
@@ -97,7 +97,7 @@
               <p class="admin-operations-queue-card__helper">{{ food.macro_label }} · solver {% if food.solver_enabled %}enabled{% else %}off{% endif %}</p>
               <span class="admin-operations-status-pill">{{ food.status }}</span>
             </div>
-            <form method="post" action="{% url 'admin_operations_food_catalog_food_action' food.pk %}" class="admin-operations-inline-form">
+            <form method="post" action="{% url 'admin_operations_food_catalog_food_action' food.pk %}" class="admin-operations-inline-form" data-admin-operations-confirm data-confirm-message="Confirmar intervención sobre este alimento master?">
               {% csrf_token %}
               <label>
                 <span>Razón</span>
diff --git a/admin_operations/templates/admin_operations/food_catalog_candidate_detail.html b/admin_operations/templates/admin_operations/food_catalog_candidate_detail.html
index b999bfb..91d7c25 100644
--- a/admin_operations/templates/admin_operations/food_catalog_candidate_detail.html
+++ b/admin_operations/templates/admin_operations/food_catalog_candidate_detail.html
@@ -45,7 +45,7 @@
         <p class="muted-text">La razón queda registrada en las notas del candidato hasta que OPS06 introduzca audit log formal.</p>
       </div>
     </div>
-    <form method="post" action="{% url 'admin_operations_food_catalog_candidate_action' vm.content.candidate.pk %}" class="admin-operations-action-form">
+    <form method="post" action="{% url 'admin_operations_food_catalog_candidate_action' vm.content.candidate.pk %}" class="admin-operations-action-form" data-admin-operations-confirm data-confirm-message="Confirmar cambio de estado del candidato Food Catalog?">
       {% csrf_token %}
       <label>
         <span>Razón obligatoria</span>
diff --git a/admin_operations/templates/admin_operations/overview.html b/admin_operations/templates/admin_operations/overview.html
index 9397b4c..d649bf9 100644
--- a/admin_operations/templates/admin_operations/overview.html
+++ b/admin_operations/templates/admin_operations/overview.html
@@ -16,6 +16,21 @@
     </div>
   </section>
 
+
+
+  <section class="card-detail-block admin-operations-closure-card" aria-label="OPS08 V1 closure">
+    <div>
+      <p class="section-eyebrow">OPS08 · V1 closure</p>
+      <h3>Consola operacional V1 cerrada</h3>
+      <p class="muted-text">Food Catalog, Accounts & Credits, AI Assistant y Audit Log quedan conectados como workflows staff-only, con razón obligatoria, confirmación antes de mutar y auditoría transversal.</p>
+    </div>
+    <div class="admin-operations-closure-card__checks" aria-label="Criterios de cierre OPS08">
+      <span><i data-lucide="shield-check"></i> Staff-only</span>
+      <span><i data-lucide="mouse-pointer-click"></i> Confirmación antes de mutar</span>
+      <span><i data-lucide="scroll-text"></i> Audit log activo</span>
+    </div>
+  </section>
+
   <section class="home-stats admin-analytics-kpi-grid admin-operations-kpi-grid" aria-label="Resumen operacional OPS02">
     {% for metric in vm.content.metrics %}
       <article class="home-stat-card admin-operations-metric-card">
diff --git a/admin_operations/tests/test_v1_closure.py b/admin_operations/tests/test_v1_closure.py
new file mode 100644
index 0000000..a78b27e
--- /dev/null
+++ b/admin_operations/tests/test_v1_closure.py
@@ -0,0 +1,43 @@
+from django.contrib.auth import get_user_model
+from django.test import TestCase, override_settings
+from django.urls import reverse
+
+
+@override_settings(NUTRITION_ONBOARDING_GATE_ENABLED=False)
+class AdminOperationsV1ClosureTests(TestCase):
+    def setUp(self):
+        User = get_user_model()
+        self.staff = User.objects.create_user(
+            username="ops08-staff@example.com",
+            email="ops08-staff@example.com",
+            password="password123",
+            is_staff=True,
+        )
+        self.client.force_login(self.staff)
+
+    def test_overview_renders_ops08_closure_card(self):
+        response = self.client.get(reverse("admin_operations_overview"))
+
+        self.assertEqual(response.status_code, 200)
+        self.assertContains(response, "OPS08 · V1 closure")
+        self.assertContains(response, "Consola operacional V1 cerrada")
+        self.assertContains(response, "Confirmación antes de mutar")
+        self.assertContains(response, "Audit log activo")
+
+    def test_operations_shell_contains_shared_confirmation_script(self):
+        response = self.client.get(reverse("admin_operations_overview"))
+
+        self.assertEqual(response.status_code, 200)
+        html = response.content.decode("utf-8")
+        self.assertIn("data-admin-operations-confirm", html)
+        self.assertIn("Confirmar acción operacional", html)
+        self.assertIn("window.confirm", html)
+
+    def test_ai_mutation_forms_opt_into_confirmation_contract(self):
+        response = self.client.get(reverse("admin_operations_ai_assistant"))
+
+        self.assertEqual(response.status_code, 200)
+        html = response.content.decode("utf-8")
+        # The page can be empty, but the shared shell still carries the confirmation contract.
+        self.assertIn("data-admin-operations-confirm", html)
+        self.assertIn("Confirmar acción operacional", html)
diff --git a/docs/decisions/0078-admin-operations-v1-closure.md b/docs/decisions/0078-admin-operations-v1-closure.md
new file mode 100644
index 0000000..ca7cd8a
--- /dev/null
+++ b/docs/decisions/0078-admin-operations-v1-closure.md
@@ -0,0 +1,76 @@
+# 0078 · Admin Operations V1 closure
+
+Date: 2026-07-04
+Status: accepted
+Cycle patch: OPS08
+App: `admin_operations`
+
+## Context
+
+OPS01 through OPS07 created the operational companion to Admin Analytics:
+
+- OPS01 created the independent staff-only shell.
+- OPS02 added operational queues.
+- OPS03 added Food Catalog operations.
+- OPS04 added Accounts & Credits operations.
+- OPS05 added AI Assistant and AI/MCP proposal operations.
+- OPS06 added the transversal operational audit log.
+- OPS07 linked Admin Analytics signals to Admin Operations workflows.
+
+The final patch of the cycle should not introduce a new domain workflow. Its purpose is to
+stabilize the V1 console, improve mobile behavior, make mutation intent clearer and close
+the documentation loop.
+
+## Decision
+
+Close Admin Operations V1 with UI polish and explicit confirmation behavior for
+state-changing forms.
+
+OPS08 keeps the boundary intact:
+
+```text
+admin_analytics = observe, diagnose, alert and link
+admin_operations = review, confirm, mutate and audit
+Django Admin = raw/legacy technical admin
+```
+
+## Implemented closure work
+
+OPS08 adds:
+
+- a V1 closure card on the Operations overview;
+- explicit confirmation handling for operational mutation forms;
+- visible confirmation badges on mutation forms;
+- mobile-safe stacking for action groups and guided forms;
+- mobile table-to-card behavior using `data-label` attributes;
+- stronger empty/action layout behavior for cards, warnings and queues;
+- docs status update marking the cycle as completed.
+
+## Confirmation rule
+
+Mutation forms in Admin Operations now opt into a shared frontend confirmation contract:
+
+```html
+<form data-admin-operations-confirm data-confirm-message="...">
+```
+
+Before submission, the shell checks that a reason field is not blank when present and then
+asks the staff user to confirm the action. Server-side validation remains authoritative;
+this is a UI safety layer, not a security boundary.
+
+## Migration expectation
+
+No migration.
+
+## Outcome
+
+Admin Operations V1 is ready as a coherent internal operational console:
+
+- staff-only access;
+- real queues and guided actions;
+- reason-required mutation flows;
+- transversal audit log;
+- Analytics-to-Operations navigation;
+- mobile-friendly operational layout.
+
+Future work should be tracked as a new cycle instead of extending OPS00-OPS08.
diff --git a/docs/decisions/README.md b/docs/decisions/README.md
index 24de975..942262d 100644
--- a/docs/decisions/README.md
+++ b/docs/decisions/README.md
@@ -100,3 +100,4 @@ Consequences
 - [0075 · Admin Operations AI Assistant workflow](0075-admin-operations-ai-assistant-workflow.md)
 - [0076 · Admin Operations audit log foundation](0076-admin-operations-audit-log-foundation.md)
 - [0077 · Admin Analytics to Admin Operations cross-links](0077-admin-analytics-operations-cross-links.md)
+- [0078 · Admin Operations V1 closure](0078-admin-operations-v1-closure.md)
diff --git a/docs/planning/admin_operations_console_cycle.md b/docs/planning/admin_operations_console_cycle.md
index 147ae10..d666025 100644
--- a/docs/planning/admin_operations_console_cycle.md
+++ b/docs/planning/admin_operations_console_cycle.md
@@ -1,6 +1,6 @@
 # Admin Operations Console Cycle
 
-Status: in progress
+Status: completed
 Date: 2026-07-04
 Owner: Product / Internal Operations
 App target: `admin_operations`
@@ -196,6 +196,8 @@ Implementation note:
 
 ### OPS04 — Accounts and credits operations
 
+Status: implemented in `docs/decisions/0074-admin-operations-accounts-credits-workflow.md`.
+
 Create safe commercial/account interventions.
 
 Scope:
@@ -303,6 +305,8 @@ Implementation note:
 
 ### OPS08 — UI polish and V1 closure
 
+Status: implemented in `docs/decisions/0078-admin-operations-v1-closure.md`.
+
 Close the first operational console cycle.
 
 Scope:
@@ -315,11 +319,21 @@ Scope:
 - Final access tests and smoke checks.
 ```
 
-Migration expectation: no migration unless late model changes are added.
+Migration expectation: no migration.
 
-## MVP recommendation
+Implementation note:
 
-The minimum useful first release is:
+```text
+- /staff/operations/ now includes an OPS08 V1 closure card.
+- Mutating forms opt into data-admin-operations-confirm.
+- Confirmation is client-side UX; server validations and audit writes remain authoritative.
+- Tables gain mobile card behavior through data-label attributes.
+- The OPS00-OPS08 cycle is closed; future work should start a new cycle.
+```
+
+## V1 delivered scope
+
+The completed first release includes:
 
 ```text
 OPS00
@@ -329,25 +343,22 @@ OPS03
 OPS04
 OPS05
 OPS06
+OPS07
+OPS08
 ```
 
-This creates the operational shell, the first action queue, a Food Catalog workflow and an
-audit foundation.
-
-Accounts/credits operations are strategically important, but they should be added only when
-the audit and confirmation pattern is solid.
+This creates the operational shell, action queues, Food Catalog operations, Accounts &
+Credits operations, AI Assistant/proposal operations, transversal audit logging,
+Analytics-to-Operations navigation and V1 UI/confirmation polish.
 
-## Open questions before implementation
+## Resolved questions in V1
 
 ```text
-- Should `admin_operations` share CSS/components with `admin_analytics`, or have a small
-  shared `staff_console` base later?
-- Is there already an audit model suitable for operational actions, or should OPS06 create
-  a dedicated one?
-- Which account/credit actions are safe enough for V1?
-- Should Food Catalog curation be the first real workflow because it already has operational
-  candidate models?
-- Do staff roles need finer permissions than `is_staff` before financial actions ship?
+- `admin_operations` reuses Admin Analytics visual primitives plus its own CSS layer.
+- OPS06 created a dedicated AdminOperationAuditEvent model for transversal operational audit.
+- V1 financial actions are limited to reason-required credit adjustments and reservation releases.
+- Food Catalog was the first workflow because curation candidates already existed.
+- Fine-grained permissions remain future work; V1 uses is_staff.
 ```
 
 ## Success criteria for V1
diff --git a/notas/static/notas/css/components/admin_analytics.css b/notas/static/notas/css/components/admin_analytics.css
index 4a18b18..5a79711 100644
--- a/notas/static/notas/css/components/admin_analytics.css
+++ b/notas/static/notas/css/components/admin_analytics.css
@@ -898,3 +898,51 @@ h3 {
     width: 100%;
   }
 }
+
+/* OPS07/OPS08 Admin Analytics -> Operations bridge */
+.admin-analytics-operations-bridge {
+  display: grid;
+  grid-template-columns: auto minmax(0, 1fr) auto;
+  gap: 16px;
+  align-items: center;
+  margin-bottom: 18px;
+  padding: 16px;
+  border: 1px solid var(--border-subtle);
+  border-radius: 22px;
+  background: var(--surface-card-muted, rgba(255, 255, 255, 0.03));
+}
+
+.admin-analytics-operations-bridge__icon {
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+  width: 42px;
+  height: 42px;
+  border-radius: 16px;
+  background: var(--surface-card, rgba(255, 255, 255, 0.05));
+  color: var(--text-main);
+}
+
+.admin-analytics-operations-bridge h3,
+.admin-analytics-operations-bridge .muted-text,
+.admin-analytics-operations-bridge .section-eyebrow {
+  margin-bottom: 0;
+}
+
+.admin-analytics-operations-bridge h3 {
+  margin-top: 4px;
+}
+
+.admin-analytics-operations-bridge .btn-secondary {
+  white-space: nowrap;
+}
+
+@media (max-width: 720px) {
+  .admin-analytics-operations-bridge {
+    grid-template-columns: 1fr;
+  }
+
+  .admin-analytics-operations-bridge .btn-secondary {
+    width: 100%;
+  }
+}
diff --git a/notas/static/notas/css/components/admin_operations.css b/notas/static/notas/css/components/admin_operations.css
index c02b78d..dc35b85 100644
--- a/notas/static/notas/css/components/admin_operations.css
+++ b/notas/static/notas/css/components/admin_operations.css
@@ -573,3 +573,161 @@
   max-width: 340px;
   white-space: normal;
 }
+
+/* =========================
+   OPS08 V1 closure polish
+   ========================= */
+
+.admin-operations-closure-card {
+  display: grid;
+  grid-template-columns: minmax(0, 1fr) minmax(260px, 420px);
+  gap: 18px;
+  align-items: center;
+  border-style: solid;
+}
+
+.admin-operations-closure-card__checks {
+  display: grid;
+  gap: 10px;
+}
+
+.admin-operations-closure-card__checks span {
+  display: inline-flex;
+  align-items: center;
+  gap: 8px;
+  min-height: 38px;
+  padding: 8px 12px;
+  border: 1px solid var(--border-subtle);
+  border-radius: 999px;
+  background: var(--surface-card-muted, rgba(255, 255, 255, 0.03));
+  color: var(--text-main);
+  font-size: 0.84rem;
+  font-weight: 720;
+}
+
+.admin-operations-closure-card__checks svg {
+  width: 16px;
+  height: 16px;
+  color: var(--text-soft);
+}
+
+.admin-operations-action-form[data-admin-operations-confirm],
+.admin-operations-inline-form[data-admin-operations-confirm],
+.admin-operations-adjustment-form[data-admin-operations-confirm] {
+  position: relative;
+}
+
+.admin-operations-action-form[data-admin-operations-confirm]::before,
+.admin-operations-inline-form[data-admin-operations-confirm]::before,
+.admin-operations-adjustment-form[data-admin-operations-confirm]::before {
+  content: "Confirmación requerida";
+  display: inline-flex;
+  align-items: center;
+  width: fit-content;
+  margin-bottom: 8px;
+  padding: 5px 9px;
+  border: 1px solid var(--border-subtle);
+  border-radius: 999px;
+  color: var(--text-soft);
+  font-size: 0.72rem;
+  font-weight: 760;
+  letter-spacing: 0.04em;
+  text-transform: uppercase;
+}
+
+@media (max-width: 920px) {
+  .admin-operations-closure-card {
+    grid-template-columns: 1fr;
+  }
+}
+
+@media (max-width: 720px) {
+  .admin-operations-content {
+    padding: 16px max(12px, env(safe-area-inset-right)) calc(24px + env(safe-area-inset-bottom)) max(12px, env(safe-area-inset-left));
+  }
+
+  .admin-operations-page .card-detail-block,
+  .admin-operations-page .home-stat-card,
+  .admin-operations-warning,
+  .admin-operations-empty-state,
+  .admin-operations-food-card,
+  .admin-operations-ai-card,
+  .admin-operations-reservation-card {
+    border-radius: 18px;
+  }
+
+  .admin-operations-page .detail-actions,
+  .admin-operations-inline-actions,
+  .admin-operations-action-grid,
+  .admin-operations-search-form,
+  .admin-operations-adjustment-form {
+    width: 100%;
+  }
+
+  .admin-operations-page .detail-actions,
+  .admin-operations-inline-actions,
+  .admin-operations-action-grid,
+  .admin-operations-search-form {
+    display: grid;
+    grid-template-columns: 1fr;
+  }
+
+  .admin-operations-page .btn-secondary,
+  .admin-operations-search-form label,
+  .admin-operations-adjustment-form label,
+  .admin-operations-action-form label,
+  .admin-operations-inline-form label {
+    width: 100%;
+  }
+
+  .admin-operations-table-card {
+    overflow: visible;
+    border: 0;
+    background: transparent;
+  }
+
+  .admin-operations-table,
+  .admin-operations-table--audit {
+    min-width: 0;
+    border-collapse: separate;
+    border-spacing: 0 10px;
+  }
+
+  .admin-operations-table thead {
+    display: none;
+  }
+
+  .admin-operations-table tbody,
+  .admin-operations-table tr,
+  .admin-operations-table td {
+    display: block;
+    width: 100%;
+  }
+
+  .admin-operations-table tr {
+    padding: 14px;
+    border: 1px solid var(--border-subtle);
+    border-radius: 18px;
+    background: var(--surface-card-muted, rgba(255, 255, 255, 0.03));
+  }
+
+  .admin-operations-table td {
+    padding: 10px 0;
+    border: 0;
+  }
+
+  .admin-operations-table td + td {
+    border-top: 1px solid var(--border-subtle);
+  }
+
+  .admin-operations-table td::before {
+    content: attr(data-label);
+    display: block;
+    margin-bottom: 4px;
+    color: var(--text-soft);
+    font-size: 0.68rem;
+    font-weight: 760;
+    letter-spacing: 0.06em;
+    text-transform: uppercase;
+  }
+}
